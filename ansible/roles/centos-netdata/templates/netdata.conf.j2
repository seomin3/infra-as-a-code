# netdata configuration
#
# You can download the latest version of this file, using:
#
#  wget -O /etc/netdata/netdata.conf http://localhost:19999/netdata.conf
# or
#  curl -o /etc/netdata/netdata.conf http://localhost:19999/netdata.conf
#
# You can uncomment and change any of the options below.
# The value shown in the commented settings, is the default value.
#

# global netdata configuration

[global]
	update every = 1
	memory mode = dbengine
	page cache size = 192
	dbengine disk space = 2048
	errors flood protection period = 10
	errors to trigger flood protection = 100000
	# glibc malloc arena max for plugins = 1
	# glibc malloc arena max for netdata = 1
	hostname = {{ ansible_hostname }}
	# history = 3996
	# config directory = /etc/netdata
	# stock config directory = /usr/lib/netdata/conf.d
	# log directory = /var/log/netdata
	# web files directory = /usr/share/netdata/web
	# cache directory = /var/cache/netdata
	# lib directory = /var/lib/netdata
	# home directory = /var/cache/netdata
	# plugins directory = "/usr/libexec/netdata/plugins.d" "/etc/netdata/custom-plugins.d"
	# host access prefix =
	# memory deduplication (ksm) = yes
	# netdata cloud = coming soon
	# TZ environment variable = :/etc/localtime
	# timezone = Asia/Seoul
	# debug flags = 0x0000000000000000
	# debug log = /var/log/netdata/debug.log
	# error log = /var/log/netdata/error.log
	# access log = /var/log/netdata/access.log
	# facility log = daemon
	# run as user = netdata
	# OOM score = 1000
	# process scheduling policy = idle
	# pthread stack size = 8388608
	# cleanup obsolete charts after seconds = 3600
	# gap when lost iterations above = 1
	# cleanup orphan hosts after seconds = 3600
	# delete obsolete charts files = yes
	# delete orphan hosts files = yes
	# enable zero metrics = no

[web]
	# ssl key = /etc/netdata/ssl/key.pem
	# ssl certificate = /etc/netdata/ssl/cert.pem
	# tls version = 1.3
	# tls ciphers = none
	# ses max window = 15
	# des max window = 15
	# mode = static-threaded
	# listen backlog = 4096
	# default port = 19999
	# bind to = *
	# web files owner = netdata
	# web files group = netdata
	# disconnect idle clients after seconds = 60
	# timeout for first request = 60
	# accept a streaming request every seconds = 0
	# respect do not track policy = no
	# x-frame-options response header =
	# allow connections from = localhost *
	# allow connections by dns = heuristic
	# allow dashboard from = localhost *
	# allow dashboard by dns = heuristic
	# allow badges from = *
	# allow badges by dns = heuristic
	# allow streaming from = *
	# allow streaming by dns = heuristic
	# allow netdata.conf from = localhost fd* 10.* 192.168.* 172.16.* 172.17.* 172.18.* 172.19.* 172.20.* 172.21.* 172.22.* 172.23.* 172.24.* 172.25.* 172.26.* 172.27.* 172.28.* 172.29.* 172.30.* 172.31.*
	# allow netdata.conf by dns = no
	# allow management from = localhost
	# allow management by dns = heuristic
	# enable gzip compression = yes
	# gzip compression strategy = default
	# gzip compression level = 3
	# web server threads = 6
	# web server max sockets = 256
	# custom dashboard_info.js =

[plugins]
	# PATH environment variable = /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/usr/sbin:/usr/local/bin:/usr/local/sbin
	# PYTHONPATH environment variable =
	# proc = yes
	# diskspace = yes
	# cgroups = yes
	# tc = yes
	# idlejitter = yes
	# enable running new plugins = yes
	# check for new plugins every = 60
	# slabinfo = no
	# ebpf_process = no
	# go.d = yes
	# charts.d = yes
	# fping = yes
	# ioping = yes
	# node.d = yes
	# python.d = yes
	# apps = yes
	# perf = yes

[health]
	# silencers file = /var/lib/netdata/health.silencers.json
	# enabled = yes
	# default repeat warning = never
	# default repeat critical = never
	# in memory max health log entries = 1000
	# script to execute on alarm = /usr/libexec/netdata/plugins.d/alarm-notify.sh
	# stock health configuration directory = /usr/lib/netdata/conf.d/health.d
	# health configuration directory = /etc/netdata/health.d
	# rotate log every lines = 2000
	# run at least every seconds = 10
	# postpone alarms during hibernation for seconds = 60

[registry]
	# enabled = no
	# registry db directory = /var/lib/netdata/registry
	# netdata unique id file = /var/lib/netdata/registry/netdata.public.unique.id
	# registry db file = /var/lib/netdata/registry/registry.db
	# registry log file = /var/lib/netdata/registry/registry-log.db
	# registry save db every new entries = 1000000
	# registry expire idle persons days = 365
	# registry domain =
	# registry to announce = https://registry.my-netdata.io
	# registry hostname = nssf-os-controller01
	# verify browser cookies support = yes
	# max URL length = 1024
	# max URL name length = 50
	# netdata management api key file = /var/lib/netdata/netdata.api.key
	# allow from = *
	# allow by dns = heuristic

[cloud]
	# cloud base url = https://netdata.cloud

[backend]
	# host tags =
	# enabled = no
	# data source = average
	# type = graphite
	# destination = localhost
	# prefix = netdata
	# hostname = nssf-os-controller01
	# update every = 10
	# buffer on failures = 10
	# timeout ms = 20000
	# send names instead of ids = yes
	# send charts matching = *
	# send hosts matching = localhost *

[statsd]
	# enabled = yes
	# update every (flushInterval) = 1
	# udp messages to process at once = 10
	# create private charts for metrics matching = *
	# max private charts allowed = 200
	# max private charts hard limit = 1000
	# private charts memory mode = dbengine
	# private charts history = 3996
	# decimal detail = 1000
	# disconnect idle tcp clients after seconds = 600
	# private charts hidden = no
	# histograms and timers percentile (percentThreshold) = 95.00000
	# add dimension for number of events received = yes
	# gaps on gauges (deleteGauges) = no
	# gaps on counters (deleteCounters) = no
	# gaps on meters (deleteMeters) = no
	# gaps on sets (deleteSets) = no
	# gaps on histograms (deleteHistograms) = no
	# gaps on timers (deleteTimers) = no
	# statsd server max TCP sockets = 256
	# listen backlog = 4096
	# default port = 8125
	# bind to = udp:localhost tcp:localhost


# per plugin configuration

[plugin:proc]
	# netdata server resources = yes
	# /proc/pagetypeinfo = no
	# /proc/stat = yes
	# /proc/uptime = yes
	# /proc/loadavg = yes
	# /proc/sys/kernel/random/entropy_avail = yes
	# /proc/pressure = yes
	# /proc/interrupts = yes
	# /proc/softirqs = yes
	# /proc/vmstat = yes
	# /proc/meminfo = yes
	# /sys/kernel/mm/ksm = yes
	# /sys/block/zram = yes
	# /sys/devices/system/edac/mc = yes
	# /sys/devices/system/node = yes
	# /proc/net/dev = yes
	# /proc/net/sockstat = yes
	# /proc/net/sockstat6 = yes
	# /proc/net/netstat = yes
	# /proc/net/snmp = yes
	# /proc/net/snmp6 = yes
	# /proc/net/sctp/snmp = yes
	# /proc/net/softnet_stat = yes
	# /proc/net/ip_vs/stats = yes
	# /proc/net/stat/conntrack = yes
	# /proc/net/stat/synproxy = yes
	# /proc/diskstats = yes
	# /proc/mdstat = yes
	# /proc/net/rpc/nfsd = yes
	# /proc/net/rpc/nfs = yes
	# /proc/spl/kstat/zfs/arcstats = yes
	# /sys/fs/btrfs = yes
	# ipc = yes
	# /sys/class/power_supply = yes

[plugin:proc:diskspace]
	# remove charts of unmounted disks = yes
	# update every = 1
	# check for new mount points every = 15
	# exclude space metrics on paths = /proc/* /sys/* /var/run/user/* /run/user/* /snap/* /var/lib/docker/*
	# exclude space metrics on filesystems = *gvfs *gluster* *s3fs *ipfs *davfs2 *httpfs *sshfs *gdfs *moosefs fusectl
	# space usage for all disks = auto
	# inodes usage for all disks = auto

[plugin:tc]
	# script to run to get tc values = /usr/libexec/netdata/plugins.d/tc-qos-helper.sh
	# enable new interfaces detected at runtime = yes
	# enable traffic charts for all interfaces = auto
	# enable packets charts for all interfaces = auto
	# enable dropped charts for all interfaces = auto
	# enable tokens charts for all interfaces = no
	# enable ctokens charts for all interfaces = no
	# enable show all classes and qdiscs for all interfaces = no
	# qos for ens6f3 = yes
	# traffic chart for ens6f3 = auto
	# packets chart for ens6f3 = auto
	# dropped packets chart for ens6f3 = auto
	# tokens chart for ens6f3 = no
	# ctokens chart for ens6f3 = no
	# show all classes for ens6f3 = no
	# cleanup unused classes every = 120
	# qos for eno49 = yes
	# traffic chart for eno49 = auto
	# packets chart for eno49 = auto
	# dropped packets chart for eno49 = auto
	# tokens chart for eno49 = no
	# ctokens chart for eno49 = no
	# show all classes for eno49 = no
	# qos for eno50 = yes
	# traffic chart for eno50 = auto
	# packets chart for eno50 = auto
	# dropped packets chart for eno50 = auto
	# tokens chart for eno50 = no
	# ctokens chart for eno50 = no
	# show all classes for eno50 = no

[plugin:cgroups]
	# cgroups plugin resource charts = yes
	# update every = 1
	# check for new cgroups every = 10
	# use unified cgroups = no
	# containers priority = 40000
	# enable cpuacct stat (total CPU) = auto
	# enable cpuacct usage (per core CPU) = auto
	# enable memory (used mem including cache) = auto
	# enable detailed memory = auto
	# enable memory limits fail count = auto
	# enable swap memory = auto
	# enable blkio bandwidth = auto
	# enable blkio operations = auto
	# enable blkio throttle bandwidth = auto
	# enable blkio throttle operations = auto
	# enable blkio queued operations = auto
	# enable blkio merged operations = auto
	# enable cpu pressure = auto
	# enable io some pressure = auto
	# enable io full pressure = auto
	# enable memory some pressure = auto
	# enable memory full pressure = auto
	# recheck zero blkio every iterations = 10
	# recheck zero memory failcnt every iterations = 10
	# recheck zero detailed memory every iterations = 10
	# enable systemd services = yes
	# enable systemd services detailed memory = no
	# report used memory without cache = yes
	# path to /sys/fs/cgroup/cpuacct = /sys/fs/cgroup/cpu,cpuacct
	# path to /sys/fs/cgroup/cpuset = /sys/fs/cgroup/cpuset
	# path to /sys/fs/cgroup/blkio = /sys/fs/cgroup/blkio
	# path to /sys/fs/cgroup/memory = /sys/fs/cgroup/memory
	# path to /sys/fs/cgroup/devices = /sys/fs/cgroup/devices
	# max cgroups to allow = 1000
	# max cgroups depth to monitor = 0
	# enable new cgroups detected at run time = yes
	# enable by default cgroups matching =  !*/init.scope  !/system.slice/run-*.scope  *.scope  /machine.slice/*.service  !*/vcpu*  !*/emulator  !*.mount  !*.partition  !*.service  !*.socket  !*.slice  !*.swap  !*.user  !/  !/docker  !/libvirt  !/lxc  !/lxc/*/*  !/lxc.monitor  !/lxc.pivot  !/lxc.payload  !/machine  !/qemu  !/system  !/systemd  !/user  *
	# search for cgroups in subpaths matching =  !*/init.scope  !*-qemu  !*.libvirt-qemu  !/init.scope  !/system  !/systemd  !/user  !/user.slice  !/lxc/*/*  !/lxc.monitor  !/lxc.payload/*/*  *
	# script to get cgroup names = /usr/libexec/netdata/plugins.d/cgroup-name.sh
	# script to get cgroup network interfaces = /usr/libexec/netdata/plugins.d/cgroup-network
	# run script to rename cgroups matching =  !/  !*.mount  !*.socket  !*.partition  /machine.slice/*.service  !*.service  !*.slice  !*.swap  !*.user  !init.scope  !*.scope/vcpu*  !*.scope/emulator  *.scope  *docker*  *lxc*  *qemu*  *kubepods*  *.libvirt-qemu  *
	# cgroups to match as systemd services =  !/system.slice/*/*.service  /system.slice/*.service
	# enable cgroup / = no
	# search for cgroups under / = yes
	# enable cgroup system.slice = no
	# enable cgroup user.slice = no
	# enable cgroup machine.slice = no
	# search for cgroups under /machine.slice = yes
	# enable cgroup qemu_perflabems01 = yes
	# search for cgroups under /machine.slice/machine-qemu\x2d23\x2dperf\x2dlab\x2dems01.scope = yes
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu15 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu14 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu13 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu12 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu11 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu10 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu9 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu8 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu7 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu6 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu5 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu4 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu3 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu2 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu1 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/vcpu0 = no
	# enable cgroup machine.slice/machine-qemu/x2d23/x2dperf/x2dlab/x2dems01.scope/emulator = no
	# search for cgroups under /system.slice = yes
	# enable cgroup system.slice/proc-sys-fs-binfmt_misc.mount = no
	# enable cgroup system.slice/sys-fs-fuse-connections.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2df7139d9b/x2d7d68/x2d4646/x2daf81/x2dd509e89f56e8.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2d84c92d79/x2db127/x2d4c4e/x2da6e2/x2dc03f10864193.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2dc7eb055a/x2d1941/x2d4851/x2d8525/x2db18ebad9f4f6.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2df0b52693/x2d1c2a/x2d4c32/x2d9f9e/x2d39bcf80d23a6.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2de75f9c0d/x2d536f/x2d4fc2/x2d9d39/x2d9999566e3086.mount = no
	# enable cgroup system.slice/run-netns-qdhcp/x2d6afbda35/x2d131b/x2d445e/x2d9038/x2d2f418221182e.mount = no
	# enable cgroup system.slice/run-netns.mount = no
	# enable cgroup system.slice/run-user-0.mount = no
	# enable cgroup system.slice/var-lib-cinder-mnt-33ee5855953c4fdeee94b908a6bd50ec.mount = no
	# enable cgroup system.slice/system-epmd.slice = no
	# search for cgroups under /system.slice/system-epmd.slice = yes
	# enable cgroup system.slice/system-epmd.slice/epmd_0.0.0.0.service = no
	# enable cgroup system.slice/var-lib-nfs-rpc_pipefs.mount = no
	# enable cgroup system.slice/var-lib-glance.mount = no
	# enable cgroup system.slice/var-lib-nova.mount = no
	# enable cgroup system.slice/dev-disk-by/x2did-wwn/x2d0x600508b1001c4e483301e0858db58544/x2dpart5.swap = no
	# enable cgroup system.slice/dev-sda5.swap = no
	# enable cgroup system.slice/dev-disk-by/x2did-scsi/x2d3600508b1001c4e483301e0858db58544/x2dpart5.swap = no
	# enable cgroup system.slice/dev-disk-by/x2dpath-pci/x2d0000_02_00.0/x2dscsi/x2d0_1_0_0/x2dpart5.swap = no
	# enable cgroup system.slice/boot.mount = no
	# enable cgroup system.slice/system-lvm2/x2dpvscan.slice = no
	# enable cgroup system.slice/dev-disk-by/x2duuid-ca7cb38c/x2d74f0/x2d49d8/x2dbebc/x2d543d010a1af5.swap = no
	# enable cgroup system.slice/sys-kernel-config.mount = no
	# enable cgroup system.slice/-.mount = no
	# enable cgroup system.slice/proc-fs-nfsd.mount = no
	# enable cgroup system.slice/dev-mqueue.mount = no
	# enable cgroup system.slice/dev-hugepages.mount = no
	# enable cgroup system.slice/system-getty.slice = no
	# enable cgroup system.slice/system-selinux/x2dpolicy/x2dmigrate/x2dlocal/x2dchanges.slice = no
	# enable cgroup system.slice/sys-kernel-debug.mount = no
	# meminfo filename to monitor = /proc/meminfo

[plugin:idlejitter]
	# loop time in ms = 20

[plugin:charts.d]
	# update every = 1
	# command options =

[plugin:fping]
	# update every = 1
	# command options =

[plugin:node.d]
	# update every = 1
	# command options =

[plugin:python.d]
	# update every = 1
	# command options =

[plugin:apps]
	# update every = 1
	# command options =

[plugin:go.d]
	# update every = 1
	# command options =

[plugin:proc:/proc/stat]
	# cpu utilization = yes
	# per cpu core utilization = yes
	# cpu interrupts = yes
	# context switches = yes
	# processes started = yes
	# processes running = yes
	# keep per core files open = no
	# keep cpuidle files open = no
	# core_throttle_count = no
	# package_throttle_count = no
	# cpu frequency = no
	# cpu idle states = no
	# core_throttle_count filename to monitor = /sys/devices/system/cpu/%s/thermal_throttle/core_throttle_count
	# package_throttle_count filename to monitor = /sys/devices/system/cpu/%s/thermal_throttle/package_throttle_count
	# scaling_cur_freq filename to monitor = /sys/devices/system/cpu/%s/cpufreq/scaling_cur_freq
	# time_in_state filename to monitor = /sys/devices/system/cpu/%s/cpufreq/stats/time_in_state
	# schedstat filename to monitor = /proc/schedstat
	# cpuidle name filename to monitor = /sys/devices/system/cpu/cpu%zu/cpuidle/state%zu/name
	# cpuidle time filename to monitor = /sys/devices/system/cpu/cpu%zu/cpuidle/state%zu/time
	# filename to monitor = /proc/stat

[plugin:proc:diskspace:/dev]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/sys/kernel/security]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/dev/shm]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/run]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/sys/fs/cgroup]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/systemd]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/pstore]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/perf_event]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/cpuset]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/cpu,cpuacct]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/devices]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/net_cls,net_prio]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/hugetlb]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/memory]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/pids]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/blkio]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/fs/cgroup/freezer]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/sys/kernel/config]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/dev/hugepages]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/proc/fs/nfsd]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/boot]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/var/lib/nova]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/var/lib/cinder/mnt/33ee5855953c4fdeee94b908a6bd50ec]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/run/user/0]
	# space usage = no
	# inodes usage = no

[plugin:proc:diskspace:/run/netns]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:diskspace:/proc/sys/fs/binfmt_misc]
	# space usage = no
	# inodes usage = no

[plugin:proc:/proc/uptime]
	# filename to monitor = /proc/uptime

[plugin:proc:/proc/loadavg]
	# filename to monitor = /proc/loadavg
	# enable load average = yes
	# enable total processes = yes

[plugin:proc:/proc/sys/kernel/random/entropy_avail]
	# filename to monitor = /proc/sys/kernel/random/entropy_avail

[plugin:proc:/proc/interrupts]
	# interrupts per core = auto
	# filename to monitor = /proc/interrupts

[plugin:proc:/proc/softirqs]
	# interrupts per core = auto
	# filename to monitor = /proc/softirqs

[plugin:proc:/proc/vmstat]
	# swap i/o = auto
	# disk i/o = yes
	# memory page faults = yes
	# system-wide numa metric summary = auto
	# filename to monitor = /proc/vmstat

[plugin:proc:/sys/devices/system/node]
	# directory to monitor = /sys/devices/system/node
	# enable per-node numa metrics = auto

[plugin:proc:/proc/meminfo]
	# system ram = yes
	# system swap = auto
	# hardware corrupted ECC = auto
	# committed memory = yes
	# writeback memory = yes
	# kernel memory = yes
	# slab memory = yes
	# hugepages = auto
	# transparent hugepages = auto
	# filename to monitor = /proc/meminfo

[plugin:proc:/sys/kernel/mm/ksm]
	# /sys/kernel/mm/ksm/pages_shared = /sys/kernel/mm/ksm/pages_shared
	# /sys/kernel/mm/ksm/pages_sharing = /sys/kernel/mm/ksm/pages_sharing
	# /sys/kernel/mm/ksm/pages_unshared = /sys/kernel/mm/ksm/pages_unshared
	# /sys/kernel/mm/ksm/pages_volatile = /sys/kernel/mm/ksm/pages_volatile

[plugin:proc:/sys/devices/system/edac/mc]
	# directory to monitor = /sys/devices/system/edac/mc
	# enable ECC memory correctable errors = auto
	# enable ECC memory uncorrectable errors = auto

[plugin:proc:/proc/net/dev]
	# filename to monitor = /proc/net/dev
	# path to get virtual interfaces = /sys/devices/virtual/net/%s
	# path to get net device speed = /sys/class/net/%s/speed
	# path to get net device duplex = /sys/class/net/%s/duplex
	# path to get net device operstate = /sys/class/net/%s/operstate
	# enable new interfaces detected at runtime = auto
	# bandwidth for all interfaces = auto
	# packets for all interfaces = auto
	# errors for all interfaces = auto
	# drops for all interfaces = auto
	# fifo for all interfaces = auto
	# compressed packets for all interfaces = auto
	# frames, collisions, carrier counters for all interfaces = auto
	# disable by default interfaces matching = lo fireqos* *-ifb
	# refresh interface speed every seconds = 10
	# refresh interface duplex every seconds = 10
	# refresh interface operstate every seconds = 10

[plugin:proc:/proc/net/dev:vxlan_sys_4789]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens6f2]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:eno52]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens6f3]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ovs-system]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:br-int]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:br-tun]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:bond0]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:eno49]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:eno51]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:lo]
	# enabled = no
	# virtual = yes

[plugin:proc:/proc/net/dev:br-ex]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens6f0]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens9f0]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens6f1]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:ens9f1]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:eno50]
	# enabled = yes
	# virtual = no
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/sockstat]
	# ipv4 sockets = auto
	# ipv4 TCP sockets = auto
	# ipv4 TCP memory = auto
	# ipv4 UDP sockets = auto
	# ipv4 UDP memory = auto
	# ipv4 UDPLITE sockets = auto
	# ipv4 RAW sockets = auto
	# ipv4 FRAG sockets = auto
	# ipv4 FRAG memory = auto
	# update constants every = 60
	# filename to monitor = /proc/net/sockstat

[plugin:proc:/proc/net/sockstat6]
	# ipv6 TCP sockets = auto
	# ipv6 UDP sockets = auto
	# ipv6 UDPLITE sockets = auto
	# ipv6 RAW sockets = auto
	# ipv6 FRAG sockets = auto
	# filename to monitor = /proc/net/sockstat6

[plugin:proc:/proc/net/netstat]
	# bandwidth = auto
	# input errors = auto
	# multicast bandwidth = auto
	# broadcast bandwidth = auto
	# multicast packets = auto
	# broadcast packets = auto
	# ECN packets = auto
	# TCP reorders = auto
	# TCP SYN cookies = auto
	# TCP out-of-order queue = auto
	# TCP connection aborts = auto
	# TCP memory pressures = auto
	# TCP SYN queue = auto
	# TCP accept queue = auto
	# filename to monitor = /proc/net/netstat

[plugin:proc:/proc/net/snmp]
	# ipv4 packets = auto
	# ipv4 fragments sent = auto
	# ipv4 fragments assembly = auto
	# ipv4 errors = auto
	# ipv4 TCP connections = auto
	# ipv4 TCP packets = auto
	# ipv4 TCP errors = auto
	# ipv4 TCP opens = auto
	# ipv4 TCP handshake issues = auto
	# ipv4 UDP packets = auto
	# ipv4 UDP errors = auto
	# ipv4 ICMP packets = auto
	# ipv4 ICMP messages = auto
	# ipv4 UDPLite packets = auto
	# filename to monitor = /proc/net/snmp

[plugin:proc:/proc/net/snmp6]
	# ipv6 packets = auto
	# ipv6 fragments sent = auto
	# ipv6 fragments assembly = auto
	# ipv6 errors = auto
	# ipv6 UDP packets = auto
	# ipv6 UDP errors = auto
	# ipv6 UDPlite packets = auto
	# ipv6 UDPlite errors = auto
	# bandwidth = auto
	# multicast bandwidth = auto
	# broadcast bandwidth = auto
	# multicast packets = auto
	# icmp = auto
	# icmp redirects = auto
	# icmp errors = auto
	# icmp echos = auto
	# icmp group membership = auto
	# icmp router = auto
	# icmp neighbor = auto
	# icmp mldv2 = auto
	# icmp types = auto
	# ect = auto
	# filename to monitor = /proc/net/snmp6

[plugin:proc:/proc/net/sctp/snmp]
	# established associations = auto
	# association transitions = auto
	# fragmentation = auto
	# packets = auto
	# packet errors = auto
	# chunk types = auto
	# filename to monitor = /proc/net/sctp/snmp

[plugin:proc:/proc/net/softnet_stat]
	# softnet_stat per core = yes
	# filename to monitor = /proc/net/softnet_stat

[plugin:proc:/proc/net/ip_vs_stats]
	# IPVS bandwidth = yes
	# IPVS connections = yes
	# IPVS packets = yes
	# filename to monitor = /proc/net/ip_vs_stats

[plugin:proc:/proc/net/stat/nf_conntrack]
	# filename to monitor = /proc/net/stat/nf_conntrack
	# netfilter new connections = yes
	# netfilter connection changes = yes
	# netfilter connection expectations = yes
	# netfilter connection searches = yes
	# netfilter errors = yes
	# netfilter connections = yes

[plugin:proc:/proc/sys/net/netfilter/nf_conntrack_max]
	# filename to monitor = /proc/sys/net/netfilter/nf_conntrack_max
	# read every seconds = 10

[plugin:proc:/proc/net/stat/synproxy]
	# SYNPROXY entries = auto
	# SYNPROXY cookies = auto
	# SYNPROXY SYN received = auto
	# SYNPROXY connections reopened = auto
	# filename to monitor = /proc/net/stat/synproxy

[plugin:proc:/proc/diskstats]
	# enable new disks detected at runtime = yes
	# performance metrics for physical disks = auto
	# performance metrics for virtual disks = auto
	# performance metrics for partitions = no
	# bandwidth for all disks = auto
	# operations for all disks = auto
	# merged operations for all disks = auto
	# i/o time for all disks = auto
	# queued operations for all disks = auto
	# utilization percentage for all disks = auto
	# backlog for all disks = auto
	# bcache for all disks = auto
	# bcache priority stats update every = 0
	# remove charts of removed disks = yes
	# path to get block device = /sys/block/%s
	# path to get block device bcache = /sys/block/%s/bcache
	# path to get virtual block device = /sys/devices/virtual/block/%s
	# path to get block device infos = /sys/dev/block/%lu:%lu/%s
	# path to device mapper = /dev/mapper
	# path to /dev/disk/by-label = /dev/disk/by-label
	# path to /dev/disk/by-id = /dev/disk/by-id
	# path to /dev/vx/dsk = /dev/vx/dsk
	# name disks by id = no
	# preferred disk ids = *
	# exclude disks = loop* ram*
	# filename to monitor = /proc/diskstats
	# performance metrics for disks with major 8 = yes
	# performance metrics for disks with major 253 = yes

[plugin:proc:/proc/diskstats:sda]
	# enable = yes
	# enable performance metrics = yes
	# bandwidth = auto
	# operations = auto
	# merged operations = auto
	# i/o time = auto
	# queued operations = auto
	# utilization percentage = auto
	# backlog = auto

[plugin:proc:/proc/diskstats:sda1]
	# enable = yes
	# enable performance metrics = no
	# bandwidth = no
	# operations = no
	# merged operations = no
	# i/o time = no
	# queued operations = no
	# utilization percentage = no
	# backlog = no

[plugin:proc:/proc/diskstats:sda2]
	# enable = yes
	# enable performance metrics = no
	# bandwidth = no
	# operations = no
	# merged operations = no
	# i/o time = no
	# queued operations = no
	# utilization percentage = no
	# backlog = no

[plugin:proc:/proc/diskstats:sda3]
	# enable = yes
	# enable performance metrics = no
	# bandwidth = no
	# operations = no
	# merged operations = no
	# i/o time = no
	# queued operations = no
	# utilization percentage = no
	# backlog = no

[plugin:proc:/proc/diskstats:sda4]
	# enable = yes
	# enable performance metrics = no
	# bandwidth = no
	# operations = no
	# merged operations = no
	# i/o time = no
	# queued operations = no
	# utilization percentage = no
	# backlog = no

[plugin:proc:/proc/diskstats:sda5]
	# enable = yes
	# enable performance metrics = no
	# bandwidth = no
	# operations = no
	# merged operations = no
	# i/o time = no
	# queued operations = no
	# utilization percentage = no
	# backlog = no

[plugin:proc:/proc/diskstats:vg00-var_lib_nova]
	# enable = yes
	# enable performance metrics = yes
	# bandwidth = auto
	# operations = auto
	# merged operations = auto
	# i/o time = auto
	# queued operations = auto
	# utilization percentage = auto
	# backlog = auto

[plugin:proc:/proc/mdstat]
	# faulty devices = yes
	# nonredundant arrays availability = yes
	# mismatch count = auto
	# disk stats = yes
	# operation status = yes
	# make charts obsolete = yes
	# filename to monitor = /proc/mdstat
	# mismatch_cnt filename to monitor = /sys/block/%s/md/mismatch_cnt

[plugin:proc:/proc/net/rpc/nfsd]
	# filename to monitor = /proc/net/rpc/nfsd
	# read cache = yes
	# file handles = yes
	# I/O = yes
	# threads = yes
	# read ahead = yes
	# network = yes
	# rpc = yes
	# NFS v2 procedures = yes
	# NFS v3 procedures = yes
	# NFS v4 procedures = yes
	# NFS v4 operations = yes

[plugin:proc:/proc/net/rpc/nfs]
	# filename to monitor = /proc/net/rpc/nfs
	# network = yes
	# rpc = yes
	# NFS v2 procedures = yes
	# NFS v3 procedures = yes
	# NFS v4 procedures = yes

[plugin:proc:/proc/spl/kstat/zfs/arcstats]
	# filename to monitor = /proc/spl/kstat/zfs/arcstats

[plugin:proc:/sys/fs/btrfs]
	# path to monitor = /sys/fs/btrfs
	# check for btrfs changes every = 60
	# physical disks allocation = auto
	# data allocation = auto
	# metadata allocation = auto
	# system allocation = auto

[plugin:proc:ipc]
	# message queues = yes
	# semaphore totals = yes
	# shared memory totals = yes
	# msg filename to monitor = /proc/sysvipc/msg
	# shm filename to monitor = /proc/sysvipc/shm
	# max dimensions in memory allowed = 50

[plugin:proc:/sys/class/power_supply]
	# battery capacity = yes
	# battery charge = no
	# battery energy = no
	# power supply voltage = no
	# keep files open = auto
	# directory to monitor = /sys/class/power_supply

[plugin:ioping]
	# update every = 1
	# command options =

[plugin:perf]
	# update every = 1
	# command options =

[plugin:proc:/proc/pressure]
	# base path of pressure metrics = /proc/pressure
	# enable cpu some pressure = yes
	# enable memory some pressure = yes
	# enable memory full pressure = yes
	# enable io some pressure = yes
	# enable io full pressure = yes

[plugin:proc:diskspace:/var/lib/glance]
	# space usage = auto
	# inodes usage = auto

[plugin:proc:/proc/net/dev:bond1]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:virbr0-nic]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:virbr0]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:br-bond1]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:bond2]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:br-bond2]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto

[plugin:proc:/proc/net/dev:macvtap0]
	# enabled = yes
	# virtual = yes
	# bandwidth = auto
	# packets = auto
	# errors = auto
	# drops = auto
	# fifo = auto
	# compressed = auto
	# events = auto


# per chart configuration

[system.idlejitter]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.idlejitter
	# chart type = area
	# type = system
	# family = idlejitter
	# units = microseconds lost/s
	# context = system.idlejitter
	# priority = 800
	# name = system.idlejitter
	# title = CPU Idle Jitter
	# dim min name = min
	# dim min algorithm = absolute
	# dim min multiplier = 1
	# dim min divisor = 1
	# dim max name = max
	# dim max algorithm = absolute
	# dim max multiplier = 1
	# dim max divisor = 1
	# dim average name = average
	# dim average algorithm = absolute
	# dim average multiplier = 1
	# dim average divisor = 1

[netdata.statsd_metrics]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_metrics
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = metrics
	# context = netdata.statsd_metrics
	# priority = 132010
	# name = netdata.statsd_metrics
	# title = Metrics in the netdata statsd database
	# dim gauges name = gauges
	# dim gauges algorithm = absolute
	# dim gauges multiplier = 1
	# dim gauges divisor = 1
	# dim counters name = counters
	# dim counters algorithm = absolute
	# dim counters multiplier = 1
	# dim counters divisor = 1
	# dim timers name = timers
	# dim timers algorithm = absolute
	# dim timers multiplier = 1
	# dim timers divisor = 1
	# dim meters name = meters
	# dim meters algorithm = absolute
	# dim meters multiplier = 1
	# dim meters divisor = 1
	# dim histograms name = histograms
	# dim histograms algorithm = absolute
	# dim histograms multiplier = 1
	# dim histograms divisor = 1
	# dim sets name = sets
	# dim sets algorithm = absolute
	# dim sets multiplier = 1
	# dim sets divisor = 1

[netdata.statsd_useful_metrics]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_useful_metrics
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = metrics
	# context = netdata.statsd_useful_metrics
	# priority = 132010
	# name = netdata.statsd_useful_metrics
	# title = Useful metrics in the netdata statsd database
	# dim gauges name = gauges
	# dim gauges algorithm = absolute
	# dim gauges multiplier = 1
	# dim gauges divisor = 1
	# dim counters name = counters
	# dim counters algorithm = absolute
	# dim counters multiplier = 1
	# dim counters divisor = 1
	# dim timers name = timers
	# dim timers algorithm = absolute
	# dim timers multiplier = 1
	# dim timers divisor = 1
	# dim meters name = meters
	# dim meters algorithm = absolute
	# dim meters multiplier = 1
	# dim meters divisor = 1
	# dim histograms name = histograms
	# dim histograms algorithm = absolute
	# dim histograms multiplier = 1
	# dim histograms divisor = 1
	# dim sets name = sets
	# dim sets algorithm = absolute
	# dim sets multiplier = 1
	# dim sets divisor = 1

[netdata.statsd_events]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_events
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = events/s
	# context = netdata.statsd_events
	# priority = 132011
	# name = netdata.statsd_events
	# title = Events processed by the netdata statsd server
	# dim gauges name = gauges
	# dim gauges algorithm = incremental
	# dim gauges multiplier = 1
	# dim gauges divisor = 1
	# dim counters name = counters
	# dim counters algorithm = incremental
	# dim counters multiplier = 1
	# dim counters divisor = 1
	# dim timers name = timers
	# dim timers algorithm = incremental
	# dim timers multiplier = 1
	# dim timers divisor = 1
	# dim meters name = meters
	# dim meters algorithm = incremental
	# dim meters multiplier = 1
	# dim meters divisor = 1
	# dim histograms name = histograms
	# dim histograms algorithm = incremental
	# dim histograms multiplier = 1
	# dim histograms divisor = 1
	# dim sets name = sets
	# dim sets algorithm = incremental
	# dim sets multiplier = 1
	# dim sets divisor = 1
	# dim unknown name = unknown
	# dim unknown algorithm = incremental
	# dim unknown multiplier = 1
	# dim unknown divisor = 1
	# dim errors name = errors
	# dim errors algorithm = incremental
	# dim errors multiplier = 1
	# dim errors divisor = 1

[netdata.statsd_reads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_reads
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = reads/s
	# context = netdata.statsd_reads
	# priority = 132012
	# name = netdata.statsd_reads
	# title = Read operations made by the netdata statsd server
	# dim tcp name = tcp
	# dim tcp algorithm = incremental
	# dim tcp multiplier = 1
	# dim tcp divisor = 1
	# dim udp name = udp
	# dim udp algorithm = incremental
	# dim udp multiplier = 1
	# dim udp divisor = 1

[netdata.statsd_bytes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_bytes
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = kilobits/s
	# context = netdata.statsd_bytes
	# priority = 132013
	# name = netdata.statsd_bytes
	# title = Bytes read by the netdata statsd server
	# dim tcp name = tcp
	# dim tcp algorithm = incremental
	# dim tcp multiplier = 8
	# dim tcp divisor = 1000
	# dim udp name = udp
	# dim udp algorithm = incremental
	# dim udp multiplier = 8
	# dim udp divisor = 1000

[netdata.statsd_packets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.statsd_packets
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = packets/s
	# context = netdata.statsd_packets
	# priority = 132014
	# name = netdata.statsd_packets
	# title = Network packets processed by the netdata statsd server
	# dim tcp name = tcp
	# dim tcp algorithm = incremental
	# dim tcp multiplier = 1
	# dim tcp divisor = 1
	# dim udp name = udp
	# dim udp algorithm = incremental
	# dim udp multiplier = 1
	# dim udp divisor = 1

[netdata.tcp_connects]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.tcp_connects
	# chart type = line
	# type = netdata
	# family = statsd
	# units = events
	# context = netdata.tcp_connects
	# priority = 132015
	# name = netdata.tcp_connects
	# title = statsd server TCP connects and disconnects
	# dim connects name = connects
	# dim connects algorithm = incremental
	# dim connects multiplier = 1
	# dim connects divisor = 1
	# dim disconnects name = disconnects
	# dim disconnects algorithm = incremental
	# dim disconnects multiplier = -1
	# dim disconnects divisor = 1

[netdata.tcp_connected]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.tcp_connected
	# chart type = line
	# type = netdata
	# family = statsd
	# units = sockets
	# context = netdata.tcp_connected
	# priority = 132016
	# name = netdata.tcp_connected
	# title = statsd server TCP connected sockets
	# dim connected name = connected
	# dim connected algorithm = absolute
	# dim connected multiplier = 1
	# dim connected divisor = 1

[netdata.private_charts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.private_charts
	# chart type = area
	# type = netdata
	# family = statsd
	# units = charts
	# context = netdata.private_charts
	# priority = 132020
	# name = netdata.private_charts
	# title = Private metric charts created by the netdata statsd server
	# dim charts name = charts
	# dim charts algorithm = absolute
	# dim charts multiplier = 1
	# dim charts divisor = 1

[netdata.plugin_statsd_charting_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_statsd_charting_cpu
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = milliseconds/s
	# context = netdata.statsd_cpu
	# priority = 132001
	# name = netdata.plugin_statsd_charting_cpu
	# title = NetData statsd charting thread CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.plugin_statsd_collector1_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_statsd_collector1_cpu
	# chart type = stacked
	# type = netdata
	# family = statsd
	# units = milliseconds/s
	# context = netdata.statsd_cpu
	# priority = 132002
	# name = netdata.plugin_statsd_collector1_cpu
	# title = NetData statsd collector thread No 1 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.plugin_tc_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_tc_cpu
	# chart type = stacked
	# type = netdata
	# family = tc.helper
	# units = milliseconds/s
	# context = netdata.plugin_tc_cpu
	# priority = 135000
	# name = netdata.plugin_tc_cpu
	# title = NetData TC CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.plugin_tc_time]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_tc_time
	# chart type = area
	# type = netdata
	# family = tc.helper
	# units = milliseconds/run
	# context = netdata.plugin_tc_time
	# priority = 135001
	# name = netdata.plugin_tc_time
	# title = NetData TC script execution
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[netdata.plugin_cgroups_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_cgroups_cpu
	# chart type = stacked
	# type = netdata
	# family = cgroups
	# units = milliseconds/s
	# context = netdata.plugin_cgroups_cpu
	# priority = 132000
	# name = netdata.plugin_cgroups_cpu
	# title = NetData CGroups Plugin CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[disk_space._dev]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._dev
	# chart type = stacked
	# type = disk_space
	# family = /dev
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._dev
	# title = Disk Space Usage for /dev [devtmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._dev]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._dev
	# chart type = stacked
	# type = disk_inodes
	# family = /dev
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._dev
	# title = Disk Files (inodes) Usage for /dev [devtmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[system.cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.cpu
	# chart type = stacked
	# type = system
	# family = cpu
	# units = percentage
	# context = system.cpu
	# priority = 100
	# name = system.cpu
	# title = Total CPU utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._dev_shm]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._dev_shm
	# chart type = stacked
	# type = disk_space
	# family = /dev/shm
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._dev_shm
	# title = Disk Space Usage for /dev/shm [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._dev_shm]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._dev_shm
	# chart type = stacked
	# type = disk_inodes
	# family = /dev/shm
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._dev_shm
	# title = Disk Files (inodes) Usage for /dev/shm [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[cpu.cpu0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1001
	# name = cpu.cpu0
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._run]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._run
	# chart type = stacked
	# type = disk_space
	# family = /run
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._run
	# title = Disk Space Usage for /run [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._run]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._run
	# chart type = stacked
	# type = disk_inodes
	# family = /run
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._run
	# title = Disk Files (inodes) Usage for /run [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[cpu.cpu1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1002
	# name = cpu.cpu1
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._
	# chart type = stacked
	# type = disk_space
	# family = /
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._
	# title = Disk Space Usage for / [/dev/sda3]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._
	# chart type = stacked
	# type = disk_inodes
	# family = /
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._
	# title = Disk Files (inodes) Usage for / [/dev/sda3]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[cpu.cpu2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1003
	# name = cpu.cpu2
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._boot]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._boot
	# chart type = stacked
	# type = disk_space
	# family = /boot
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._boot
	# title = Disk Space Usage for /boot [/dev/sda1]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._boot]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._boot
	# chart type = stacked
	# type = disk_inodes
	# family = /boot
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._boot
	# title = Disk Files (inodes) Usage for /boot [/dev/sda1]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[cpu.cpu3]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1004
	# name = cpu.cpu3
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._var_lib_nova]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._var_lib_nova
	# chart type = stacked
	# type = disk_space
	# family = /var/lib/nova
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._var_lib_nova
	# title = Disk Space Usage for /var/lib/nova [/dev/mapper/VG00-var_lib_nova]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._var_lib_nova]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._var_lib_nova
	# chart type = stacked
	# type = disk_inodes
	# family = /var/lib/nova
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._var_lib_nova
	# title = Disk Files (inodes) Usage for /var/lib/nova [/dev/mapper/VG00-var_lib_nova]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[cpu.cpu4]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu4
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1005
	# name = cpu.cpu4
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_space._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec
	# chart type = stacked
	# type = disk_space
	# family = /var/lib/cinder/mnt/33ee5855953c4fdeee94b908a6bd50ec
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec
	# title = Disk Space Usage for /var/lib/cinder/mnt/33ee5855953c4fdeee94b908a6bd50ec [192.168.11.84:/var/lib/nova/volumes]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1048576
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1048576
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1048576
	# dim reserved_for_root divisor = 1073741824

[cpu.cpu5]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu5
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1006
	# name = cpu.cpu5
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_inodes._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec
	# chart type = stacked
	# type = disk_inodes
	# family = /var/lib/cinder/mnt/33ee5855953c4fdeee94b908a6bd50ec
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._var_lib_cinder_mnt_33ee5855953c4fdeee94b908a6bd50ec
	# title = Disk Files (inodes) Usage for /var/lib/cinder/mnt/33ee5855953c4fdeee94b908a6bd50ec [192.168.11.84:/var/lib/nova/volumes]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[disk_space._run_netns]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._run_netns
	# chart type = stacked
	# type = disk_space
	# family = /run/netns
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._run_netns
	# title = Disk Space Usage for /run/netns [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[cpu.cpu6]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu6
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1007
	# name = cpu.cpu6
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[disk_inodes._run_netns]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._run_netns
	# chart type = stacked
	# type = disk_inodes
	# family = /run/netns
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._run_netns
	# title = Disk Files (inodes) Usage for /run/netns [tmpfs]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[netdata.plugin_diskspace]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_diskspace
	# chart type = stacked
	# type = netdata
	# family = diskspace
	# units = milliseconds/s
	# context = netdata.plugin_diskspace
	# priority = 132020
	# name = netdata.plugin_diskspace
	# title = NetData Disk Space Plugin CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[cpu.cpu7]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu7
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1008
	# name = cpu.cpu7
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[netdata.plugin_diskspace_dt]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_diskspace_dt
	# chart type = area
	# type = netdata
	# family = diskspace
	# units = milliseconds/run
	# context = netdata.plugin_diskspace_dt
	# priority = 132021
	# name = netdata.plugin_diskspace_dt
	# title = NetData Disk Space Plugin Duration
	# dim duration name = duration
	# dim duration algorithm = absolute
	# dim duration multiplier = 1
	# dim duration divisor = 1000

[cpu.cpu8]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu8
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1009
	# name = cpu.cpu8
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu9]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu9
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1010
	# name = cpu.cpu9
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu10]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu10
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1011
	# name = cpu.cpu10
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu11]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu11
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1012
	# name = cpu.cpu11
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu12]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu12
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1013
	# name = cpu.cpu12
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu13]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu13
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1014
	# name = cpu.cpu13
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu14]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu14
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1015
	# name = cpu.cpu14
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu15]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu15
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1016
	# name = cpu.cpu15
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu16]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu16
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1017
	# name = cpu.cpu16
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu17]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu17
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1018
	# name = cpu.cpu17
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu18]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu18
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1019
	# name = cpu.cpu18
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu19]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu19
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1020
	# name = cpu.cpu19
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu20]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu20
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1021
	# name = cpu.cpu20
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu21]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu21
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1022
	# name = cpu.cpu21
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu22]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu22
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1023
	# name = cpu.cpu22
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu23]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu23
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1024
	# name = cpu.cpu23
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu24]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu24
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1025
	# name = cpu.cpu24
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu25]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu25
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1026
	# name = cpu.cpu25
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu26]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu26
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1027
	# name = cpu.cpu26
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu27]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu27
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1028
	# name = cpu.cpu27
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu28]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu28
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1029
	# name = cpu.cpu28
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu29]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu29
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1030
	# name = cpu.cpu29
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu30]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu30
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1031
	# name = cpu.cpu30
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu31]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu31
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1032
	# name = cpu.cpu31
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu32]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu32
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1033
	# name = cpu.cpu32
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu33]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu33
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1034
	# name = cpu.cpu33
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu34]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu34
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1035
	# name = cpu.cpu34
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu35]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu35
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1036
	# name = cpu.cpu35
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu36]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu36
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1037
	# name = cpu.cpu36
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu37]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu37
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1038
	# name = cpu.cpu37
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu38]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu38
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1039
	# name = cpu.cpu38
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu39]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu39
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1040
	# name = cpu.cpu39
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu40]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu40
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1041
	# name = cpu.cpu40
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu41]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu41
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1042
	# name = cpu.cpu41
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu42]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu42
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1043
	# name = cpu.cpu42
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu43]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu43
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1044
	# name = cpu.cpu43
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu44]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu44
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1045
	# name = cpu.cpu44
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu45]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu45
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1046
	# name = cpu.cpu45
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu46]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu46
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1047
	# name = cpu.cpu46
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu47]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu47
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1048
	# name = cpu.cpu47
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu48]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu48
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1049
	# name = cpu.cpu48
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu49]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu49
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1050
	# name = cpu.cpu49
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu50]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu50
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1051
	# name = cpu.cpu50
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu51]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu51
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1052
	# name = cpu.cpu51
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu52]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu52
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1053
	# name = cpu.cpu52
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu53]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu53
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1054
	# name = cpu.cpu53
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu54]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu54
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1055
	# name = cpu.cpu54
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu55]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu55
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1056
	# name = cpu.cpu55
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu56]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu56
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1057
	# name = cpu.cpu56
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu57]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu57
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1058
	# name = cpu.cpu57
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu58]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu58
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1059
	# name = cpu.cpu58
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu59]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu59
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1060
	# name = cpu.cpu59
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu60]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu60
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1061
	# name = cpu.cpu60
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu61]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu61
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1062
	# name = cpu.cpu61
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu62]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu62
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1063
	# name = cpu.cpu62
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu63]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu63
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1064
	# name = cpu.cpu63
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu64]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu64
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1065
	# name = cpu.cpu64
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu65]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu65
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1066
	# name = cpu.cpu65
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu66]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu66
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1067
	# name = cpu.cpu66
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu67]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu67
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1068
	# name = cpu.cpu67
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu68]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu68
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1069
	# name = cpu.cpu68
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu69]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu69
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1070
	# name = cpu.cpu69
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu70]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu70
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1071
	# name = cpu.cpu70
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu71]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu71
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1072
	# name = cpu.cpu71
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu72]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu72
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1073
	# name = cpu.cpu72
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu73]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu73
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1074
	# name = cpu.cpu73
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu74]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu74
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1075
	# name = cpu.cpu74
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu75]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu75
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1076
	# name = cpu.cpu75
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu76]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu76
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1077
	# name = cpu.cpu76
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu77]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu77
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1078
	# name = cpu.cpu77
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu78]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu78
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1079
	# name = cpu.cpu78
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu79]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu79
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1080
	# name = cpu.cpu79
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu80]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu80
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1081
	# name = cpu.cpu80
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu81]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu81
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1082
	# name = cpu.cpu81
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu82]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu82
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1083
	# name = cpu.cpu82
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu83]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu83
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1084
	# name = cpu.cpu83
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu84]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu84
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1085
	# name = cpu.cpu84
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu85]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu85
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1086
	# name = cpu.cpu85
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu86]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu86
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1087
	# name = cpu.cpu86
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu87]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu87
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1088
	# name = cpu.cpu87
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu88]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu88
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1089
	# name = cpu.cpu88
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu89]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu89
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1090
	# name = cpu.cpu89
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu90]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu90
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1091
	# name = cpu.cpu90
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu91]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu91
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1092
	# name = cpu.cpu91
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu92]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu92
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1093
	# name = cpu.cpu92
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu93]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu93
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1094
	# name = cpu.cpu93
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu94]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu94
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1095
	# name = cpu.cpu94
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[cpu.cpu95]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu95
	# chart type = stacked
	# type = cpu
	# family = utilization
	# units = percentage
	# context = cpu.cpu
	# priority = 1096
	# name = cpu.cpu95
	# title = Core utilization
	# dim guest_nice name = guest_nice
	# dim guest_nice algorithm = percentage-of-incremental-row
	# dim guest_nice multiplier = 1
	# dim guest_nice divisor = 1
	# dim guest name = guest
	# dim guest algorithm = percentage-of-incremental-row
	# dim guest multiplier = 1
	# dim guest divisor = 1
	# dim steal name = steal
	# dim steal algorithm = percentage-of-incremental-row
	# dim steal multiplier = 1
	# dim steal divisor = 1
	# dim softirq name = softirq
	# dim softirq algorithm = percentage-of-incremental-row
	# dim softirq multiplier = 1
	# dim softirq divisor = 1
	# dim irq name = irq
	# dim irq algorithm = percentage-of-incremental-row
	# dim irq multiplier = 1
	# dim irq divisor = 1
	# dim user name = user
	# dim user algorithm = percentage-of-incremental-row
	# dim user multiplier = 1
	# dim user divisor = 1
	# dim system name = system
	# dim system algorithm = percentage-of-incremental-row
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim nice name = nice
	# dim nice algorithm = percentage-of-incremental-row
	# dim nice multiplier = 1
	# dim nice divisor = 1
	# dim iowait name = iowait
	# dim iowait algorithm = percentage-of-incremental-row
	# dim iowait multiplier = 1
	# dim iowait divisor = 1
	# dim idle name = idle
	# dim idle algorithm = percentage-of-incremental-row
	# dim idle multiplier = 1
	# dim idle divisor = 1

[system.intr]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.intr
	# chart type = line
	# type = system
	# family = interrupts
	# units = interrupts/s
	# context = system.intr
	# priority = 900
	# name = system.intr
	# title = CPU Interrupts
	# dim interrupts name = interrupts
	# dim interrupts algorithm = incremental
	# dim interrupts multiplier = 1
	# dim interrupts divisor = 1

[system.ctxt]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ctxt
	# chart type = line
	# type = system
	# family = processes
	# units = context switches/s
	# context = system.ctxt
	# priority = 800
	# name = system.ctxt
	# title = CPU Context Switches
	# dim switches name = switches
	# dim switches algorithm = incremental
	# dim switches multiplier = 1
	# dim switches divisor = 1

[system.forks]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.forks
	# chart type = line
	# type = system
	# family = processes
	# units = processes/s
	# context = system.forks
	# priority = 700
	# name = system.forks
	# title = Started Processes
	# dim started name = started
	# dim started algorithm = incremental
	# dim started multiplier = 1
	# dim started divisor = 1

[system.processes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.processes
	# chart type = line
	# type = system
	# family = processes
	# units = processes
	# context = system.processes
	# priority = 600
	# name = system.processes
	# title = System Processes
	# dim running name = running
	# dim running algorithm = absolute
	# dim running multiplier = 1
	# dim running divisor = 1
	# dim blocked name = blocked
	# dim blocked algorithm = absolute
	# dim blocked multiplier = -1
	# dim blocked divisor = 1

[system.uptime]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.uptime
	# chart type = line
	# type = system
	# family = uptime
	# units = seconds
	# context = system.uptime
	# priority = 1000
	# name = system.uptime
	# title = System Uptime
	# dim uptime name = uptime
	# dim uptime algorithm = absolute
	# dim uptime multiplier = 1
	# dim uptime divisor = 1000

[system.load]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.load
	# chart type = line
	# type = system
	# family = load
	# units = load
	# context = system.load
	# priority = 100
	# name = system.load
	# title = System Load Average
	# dim load1 name = load1
	# dim load1 algorithm = absolute
	# dim load1 multiplier = 1
	# dim load1 divisor = 1000
	# dim load5 name = load5
	# dim load5 algorithm = absolute
	# dim load5 multiplier = 1
	# dim load5 divisor = 1000
	# dim load15 name = load15
	# dim load15 algorithm = absolute
	# dim load15 multiplier = 1
	# dim load15 divisor = 1000

[system.active_processes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.active_processes
	# chart type = line
	# type = system
	# family = processes
	# units = processes
	# context = system.active_processes
	# priority = 750
	# name = system.active_processes
	# title = System Active Processes
	# dim active name = active
	# dim active algorithm = absolute
	# dim active multiplier = 1
	# dim active divisor = 1

[system.entropy]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.entropy
	# chart type = line
	# type = system
	# family = entropy
	# units = entropy
	# context = system.entropy
	# priority = 1000
	# name = system.entropy
	# title = Available Entropy
	# dim entropy name = entropy
	# dim entropy algorithm = absolute
	# dim entropy multiplier = 1
	# dim entropy divisor = 1

[system.interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.interrupts
	# chart type = stacked
	# type = system
	# family = interrupts
	# units = interrupts/s
	# context = system.interrupts
	# priority = 1000
	# name = system.interrupts
	# title = System interrupts
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = hpilo_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 73 name = hpsa0-msix0_73
	# dim 73 algorithm = incremental
	# dim 73 multiplier = 1
	# dim 73 divisor = 1
	# dim 74 name = hpsa0-msix1_74
	# dim 74 algorithm = incremental
	# dim 74 multiplier = 1
	# dim 74 divisor = 1
	# dim 75 name = hpsa0-msix2_75
	# dim 75 algorithm = incremental
	# dim 75 multiplier = 1
	# dim 75 divisor = 1
	# dim 76 name = hpsa0-msix3_76
	# dim 76 algorithm = incremental
	# dim 76 multiplier = 1
	# dim 76 divisor = 1
	# dim 77 name = hpsa0-msix4_77
	# dim 77 algorithm = incremental
	# dim 77 multiplier = 1
	# dim 77 divisor = 1
	# dim 78 name = hpsa0-msix5_78
	# dim 78 algorithm = incremental
	# dim 78 multiplier = 1
	# dim 78 divisor = 1
	# dim 79 name = hpsa0-msix6_79
	# dim 79 algorithm = incremental
	# dim 79 multiplier = 1
	# dim 79 divisor = 1
	# dim 80 name = hpsa0-msix7_80
	# dim 80 algorithm = incremental
	# dim 80 multiplier = 1
	# dim 80 divisor = 1
	# dim 81 name = hpsa0-msix8_81
	# dim 81 algorithm = incremental
	# dim 81 multiplier = 1
	# dim 81 divisor = 1
	# dim 82 name = hpsa0-msix9_82
	# dim 82 algorithm = incremental
	# dim 82 multiplier = 1
	# dim 82 divisor = 1
	# dim 83 name = hpsa0-msix10_83
	# dim 83 algorithm = incremental
	# dim 83 multiplier = 1
	# dim 83 divisor = 1
	# dim 84 name = hpsa0-msix11_84
	# dim 84 algorithm = incremental
	# dim 84 multiplier = 1
	# dim 84 divisor = 1
	# dim 85 name = hpsa0-msix12_85
	# dim 85 algorithm = incremental
	# dim 85 multiplier = 1
	# dim 85 divisor = 1
	# dim 86 name = hpsa0-msix13_86
	# dim 86 algorithm = incremental
	# dim 86 multiplier = 1
	# dim 86 divisor = 1
	# dim 87 name = hpsa0-msix14_87
	# dim 87 algorithm = incremental
	# dim 87 multiplier = 1
	# dim 87 divisor = 1
	# dim 88 name = hpsa0-msix15_88
	# dim 88 algorithm = incremental
	# dim 88 multiplier = 1
	# dim 88 divisor = 1
	# dim 89 name = hpsa0-msix16_89
	# dim 89 algorithm = incremental
	# dim 89 multiplier = 1
	# dim 89 divisor = 1
	# dim 90 name = hpsa0-msix17_90
	# dim 90 algorithm = incremental
	# dim 90 multiplier = 1
	# dim 90 divisor = 1
	# dim 91 name = hpsa0-msix18_91
	# dim 91 algorithm = incremental
	# dim 91 multiplier = 1
	# dim 91 divisor = 1
	# dim 92 name = hpsa0-msix19_92
	# dim 92 algorithm = incremental
	# dim 92 multiplier = 1
	# dim 92 divisor = 1
	# dim 93 name = hpsa0-msix20_93
	# dim 93 algorithm = incremental
	# dim 93 multiplier = 1
	# dim 93 divisor = 1
	# dim 94 name = hpsa0-msix21_94
	# dim 94 algorithm = incremental
	# dim 94 multiplier = 1
	# dim 94 divisor = 1
	# dim 95 name = hpsa0-msix22_95
	# dim 95 algorithm = incremental
	# dim 95 multiplier = 1
	# dim 95 divisor = 1
	# dim 96 name = hpsa0-msix23_96
	# dim 96 algorithm = incremental
	# dim 96 multiplier = 1
	# dim 96 divisor = 1
	# dim 97 name = hpsa0-msix24_97
	# dim 97 algorithm = incremental
	# dim 97 multiplier = 1
	# dim 97 divisor = 1
	# dim 98 name = hpsa0-msix25_98
	# dim 98 algorithm = incremental
	# dim 98 multiplier = 1
	# dim 98 divisor = 1
	# dim 99 name = hpsa0-msix26_99
	# dim 99 algorithm = incremental
	# dim 99 multiplier = 1
	# dim 99 divisor = 1
	# dim 100 name = hpsa0-msix27_100
	# dim 100 algorithm = incremental
	# dim 100 multiplier = 1
	# dim 100 divisor = 1
	# dim 101 name = hpsa0-msix28_101
	# dim 101 algorithm = incremental
	# dim 101 multiplier = 1
	# dim 101 divisor = 1
	# dim 102 name = hpsa0-msix29_102
	# dim 102 algorithm = incremental
	# dim 102 multiplier = 1
	# dim 102 divisor = 1
	# dim 103 name = hpsa0-msix30_103
	# dim 103 algorithm = incremental
	# dim 103 multiplier = 1
	# dim 103 divisor = 1
	# dim 104 name = hpsa0-msix31_104
	# dim 104 algorithm = incremental
	# dim 104 multiplier = 1
	# dim 104 divisor = 1
	# dim 105 name = hpsa0-msix32_105
	# dim 105 algorithm = incremental
	# dim 105 multiplier = 1
	# dim 105 divisor = 1
	# dim 106 name = hpsa0-msix33_106
	# dim 106 algorithm = incremental
	# dim 106 multiplier = 1
	# dim 106 divisor = 1
	# dim 107 name = hpsa0-msix34_107
	# dim 107 algorithm = incremental
	# dim 107 multiplier = 1
	# dim 107 divisor = 1
	# dim 108 name = hpsa0-msix35_108
	# dim 108 algorithm = incremental
	# dim 108 multiplier = 1
	# dim 108 divisor = 1
	# dim 109 name = hpsa0-msix36_109
	# dim 109 algorithm = incremental
	# dim 109 multiplier = 1
	# dim 109 divisor = 1
	# dim 110 name = hpsa0-msix37_110
	# dim 110 algorithm = incremental
	# dim 110 multiplier = 1
	# dim 110 divisor = 1
	# dim 111 name = hpsa0-msix38_111
	# dim 111 algorithm = incremental
	# dim 111 multiplier = 1
	# dim 111 divisor = 1
	# dim 112 name = hpsa0-msix39_112
	# dim 112 algorithm = incremental
	# dim 112 multiplier = 1
	# dim 112 divisor = 1
	# dim 113 name = hpsa0-msix40_113
	# dim 113 algorithm = incremental
	# dim 113 multiplier = 1
	# dim 113 divisor = 1
	# dim 114 name = hpsa0-msix41_114
	# dim 114 algorithm = incremental
	# dim 114 multiplier = 1
	# dim 114 divisor = 1
	# dim 115 name = hpsa0-msix42_115
	# dim 115 algorithm = incremental
	# dim 115 multiplier = 1
	# dim 115 divisor = 1
	# dim 116 name = hpsa0-msix43_116
	# dim 116 algorithm = incremental
	# dim 116 multiplier = 1
	# dim 116 divisor = 1
	# dim 117 name = hpsa0-msix44_117
	# dim 117 algorithm = incremental
	# dim 117 multiplier = 1
	# dim 117 divisor = 1
	# dim 118 name = hpsa0-msix45_118
	# dim 118 algorithm = incremental
	# dim 118 multiplier = 1
	# dim 118 divisor = 1
	# dim 119 name = hpsa0-msix46_119
	# dim 119 algorithm = incremental
	# dim 119 multiplier = 1
	# dim 119 divisor = 1
	# dim 120 name = hpsa0-msix47_120
	# dim 120 algorithm = incremental
	# dim 120 multiplier = 1
	# dim 120 divisor = 1
	# dim 121 name = hpsa0-msix48_121
	# dim 121 algorithm = incremental
	# dim 121 multiplier = 1
	# dim 121 divisor = 1
	# dim 122 name = hpsa0-msix49_122
	# dim 122 algorithm = incremental
	# dim 122 multiplier = 1
	# dim 122 divisor = 1
	# dim 123 name = hpsa0-msix50_123
	# dim 123 algorithm = incremental
	# dim 123 multiplier = 1
	# dim 123 divisor = 1
	# dim 124 name = hpsa0-msix51_124
	# dim 124 algorithm = incremental
	# dim 124 multiplier = 1
	# dim 124 divisor = 1
	# dim 125 name = hpsa0-msix52_125
	# dim 125 algorithm = incremental
	# dim 125 multiplier = 1
	# dim 125 divisor = 1
	# dim 126 name = hpsa0-msix53_126
	# dim 126 algorithm = incremental
	# dim 126 multiplier = 1
	# dim 126 divisor = 1
	# dim 127 name = hpsa0-msix54_127
	# dim 127 algorithm = incremental
	# dim 127 multiplier = 1
	# dim 127 divisor = 1
	# dim 128 name = hpsa0-msix55_128
	# dim 128 algorithm = incremental
	# dim 128 multiplier = 1
	# dim 128 divisor = 1
	# dim 129 name = hpsa0-msix56_129
	# dim 129 algorithm = incremental
	# dim 129 multiplier = 1
	# dim 129 divisor = 1
	# dim 130 name = hpsa0-msix57_130
	# dim 130 algorithm = incremental
	# dim 130 multiplier = 1
	# dim 130 divisor = 1
	# dim 131 name = hpsa0-msix58_131
	# dim 131 algorithm = incremental
	# dim 131 multiplier = 1
	# dim 131 divisor = 1
	# dim 132 name = hpsa0-msix59_132
	# dim 132 algorithm = incremental
	# dim 132 multiplier = 1
	# dim 132 divisor = 1
	# dim 133 name = hpsa0-msix60_133
	# dim 133 algorithm = incremental
	# dim 133 multiplier = 1
	# dim 133 divisor = 1
	# dim 134 name = hpsa0-msix61_134
	# dim 134 algorithm = incremental
	# dim 134 multiplier = 1
	# dim 134 divisor = 1
	# dim 135 name = hpsa0-msix62_135
	# dim 135 algorithm = incremental
	# dim 135 multiplier = 1
	# dim 135 divisor = 1
	# dim 136 name = hpsa0-msix63_136
	# dim 136 algorithm = incremental
	# dim 136 multiplier = 1
	# dim 136 divisor = 1
	# dim 201 name = qla2xxx1_default_201
	# dim 201 algorithm = incremental
	# dim 201 multiplier = 1
	# dim 201 divisor = 1
	# dim 267 name = qla2xxx2_default_267
	# dim 267 algorithm = incremental
	# dim 267 multiplier = 1
	# dim 267 divisor = 1
	# dim 269 name = qla2xxx3_default_269
	# dim 269 algorithm = incremental
	# dim 269 multiplier = 1
	# dim 269 divisor = 1
	# dim 271 name = qla2xxx4_default_271
	# dim 271 algorithm = incremental
	# dim 271 multiplier = 1
	# dim 271 divisor = 1
	# dim 274 name = ioat-msix_274
	# dim 274 algorithm = incremental
	# dim 274 multiplier = 1
	# dim 274 divisor = 1
	# dim 276 name = ioat-msix_276
	# dim 276 algorithm = incremental
	# dim 276 multiplier = 1
	# dim 276 divisor = 1
	# dim 277 name = ioat-msix_277
	# dim 277 algorithm = incremental
	# dim 277 multiplier = 1
	# dim 277 divisor = 1
	# dim 278 name = ioat-msix_278
	# dim 278 algorithm = incremental
	# dim 278 multiplier = 1
	# dim 278 divisor = 1
	# dim 279 name = ioat-msix_279
	# dim 279 algorithm = incremental
	# dim 279 multiplier = 1
	# dim 279 divisor = 1
	# dim 280 name = ioat-msix_280
	# dim 280 algorithm = incremental
	# dim 280 multiplier = 1
	# dim 280 divisor = 1
	# dim 281 name = ioat-msix_281
	# dim 281 algorithm = incremental
	# dim 281 multiplier = 1
	# dim 281 divisor = 1
	# dim 282 name = ioat-msix_282
	# dim 282 algorithm = incremental
	# dim 282 multiplier = 1
	# dim 282 divisor = 1
	# dim 284 name = ioat-msix_284
	# dim 284 algorithm = incremental
	# dim 284 multiplier = 1
	# dim 284 divisor = 1
	# dim 286 name = ioat-msix_286
	# dim 286 algorithm = incremental
	# dim 286 multiplier = 1
	# dim 286 divisor = 1
	# dim 287 name = ioat-msix_287
	# dim 287 algorithm = incremental
	# dim 287 multiplier = 1
	# dim 287 divisor = 1
	# dim 288 name = ioat-msix_288
	# dim 288 algorithm = incremental
	# dim 288 multiplier = 1
	# dim 288 divisor = 1
	# dim 289 name = ioat-msix_289
	# dim 289 algorithm = incremental
	# dim 289 multiplier = 1
	# dim 289 divisor = 1
	# dim 290 name = ioat-msix_290
	# dim 290 algorithm = incremental
	# dim 290 multiplier = 1
	# dim 290 divisor = 1
	# dim 291 name = ioat-msix_291
	# dim 291 algorithm = incremental
	# dim 291 multiplier = 1
	# dim 291 divisor = 1
	# dim 292 name = ioat-msix_292
	# dim 292 algorithm = incremental
	# dim 292 multiplier = 1
	# dim 292 divisor = 1
	# dim 294 name = ioat-msix_294
	# dim 294 algorithm = incremental
	# dim 294 multiplier = 1
	# dim 294 divisor = 1
	# dim 296 name = ioat-msix_296
	# dim 296 algorithm = incremental
	# dim 296 multiplier = 1
	# dim 296 divisor = 1
	# dim 297 name = ioat-msix_297
	# dim 297 algorithm = incremental
	# dim 297 multiplier = 1
	# dim 297 divisor = 1
	# dim 298 name = ioat-msix_298
	# dim 298 algorithm = incremental
	# dim 298 multiplier = 1
	# dim 298 divisor = 1
	# dim 299 name = ioat-msix_299
	# dim 299 algorithm = incremental
	# dim 299 multiplier = 1
	# dim 299 divisor = 1
	# dim 300 name = ioat-msix_300
	# dim 300 algorithm = incremental
	# dim 300 multiplier = 1
	# dim 300 divisor = 1
	# dim 301 name = ioat-msix_301
	# dim 301 algorithm = incremental
	# dim 301 multiplier = 1
	# dim 301 divisor = 1
	# dim 302 name = ioat-msix_302
	# dim 302 algorithm = incremental
	# dim 302 multiplier = 1
	# dim 302 divisor = 1
	# dim 304 name = ioat-msix_304
	# dim 304 algorithm = incremental
	# dim 304 multiplier = 1
	# dim 304 divisor = 1
	# dim 306 name = ioat-msix_306
	# dim 306 algorithm = incremental
	# dim 306 multiplier = 1
	# dim 306 divisor = 1
	# dim 307 name = ioat-msix_307
	# dim 307 algorithm = incremental
	# dim 307 multiplier = 1
	# dim 307 divisor = 1
	# dim 308 name = ioat-msix_308
	# dim 308 algorithm = incremental
	# dim 308 multiplier = 1
	# dim 308 divisor = 1
	# dim 309 name = ioat-msix_309
	# dim 309 algorithm = incremental
	# dim 309 multiplier = 1
	# dim 309 divisor = 1
	# dim 310 name = ioat-msix_310
	# dim 310 algorithm = incremental
	# dim 310 multiplier = 1
	# dim 310 divisor = 1
	# dim 311 name = ioat-msix_311
	# dim 311 algorithm = incremental
	# dim 311 multiplier = 1
	# dim 311 divisor = 1
	# dim 312 name = ioat-msix_312
	# dim 312 algorithm = incremental
	# dim 312 multiplier = 1
	# dim 312 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim 323 name = ens6f3-tx-0_323
	# dim 323 algorithm = incremental
	# dim 323 multiplier = 1
	# dim 323 divisor = 1
	# dim 324 name = ens6f3-rx-1_324
	# dim 324 algorithm = incremental
	# dim 324 multiplier = 1
	# dim 324 divisor = 1
	# dim 325 name = ens6f3-rx-2_325
	# dim 325 algorithm = incremental
	# dim 325 multiplier = 1
	# dim 325 divisor = 1
	# dim 326 name = ens6f3-rx-3_326
	# dim 326 algorithm = incremental
	# dim 326 multiplier = 1
	# dim 326 divisor = 1
	# dim 327 name = ens6f3-rx-4_327
	# dim 327 algorithm = incremental
	# dim 327 multiplier = 1
	# dim 327 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu0_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1100
	# name = cpu.cpu0_interrupts
	# title = CPU0 Interrupts
	# dim 0 name = timer_0
	# dim 0 algorithm = incremental
	# dim 0 multiplier = 1
	# dim 0 divisor = 1
	# dim 1 name = i8042_1
	# dim 1 algorithm = incremental
	# dim 1 multiplier = 1
	# dim 1 divisor = 1
	# dim 8 name = rtc0_8
	# dim 8 algorithm = incremental
	# dim 8 multiplier = 1
	# dim 8 divisor = 1
	# dim 9 name = acpi_9
	# dim 9 algorithm = incremental
	# dim 9 multiplier = 1
	# dim 9 divisor = 1
	# dim 12 name = i8042_12
	# dim 12 algorithm = incremental
	# dim 12 multiplier = 1
	# dim 12 divisor = 1
	# dim 16 name = hpilo_16
	# dim 16 algorithm = incremental
	# dim 16 multiplier = 1
	# dim 16 divisor = 1
	# dim 23 name = ehci_hcd:usb2_23
	# dim 23 algorithm = incremental
	# dim 23 multiplier = 1
	# dim 23 divisor = 1
	# dim 73 name = hpsa0-msix0_73
	# dim 73 algorithm = incremental
	# dim 73 multiplier = 1
	# dim 73 divisor = 1
	# dim 274 name = ioat-msix_274
	# dim 274 algorithm = incremental
	# dim 274 multiplier = 1
	# dim 274 divisor = 1
	# dim 276 name = ioat-msix_276
	# dim 276 algorithm = incremental
	# dim 276 multiplier = 1
	# dim 276 divisor = 1
	# dim 277 name = ioat-msix_277
	# dim 277 algorithm = incremental
	# dim 277 multiplier = 1
	# dim 277 divisor = 1
	# dim 278 name = ioat-msix_278
	# dim 278 algorithm = incremental
	# dim 278 multiplier = 1
	# dim 278 divisor = 1
	# dim 279 name = ioat-msix_279
	# dim 279 algorithm = incremental
	# dim 279 multiplier = 1
	# dim 279 divisor = 1
	# dim 280 name = ioat-msix_280
	# dim 280 algorithm = incremental
	# dim 280 multiplier = 1
	# dim 280 divisor = 1
	# dim 281 name = ioat-msix_281
	# dim 281 algorithm = incremental
	# dim 281 multiplier = 1
	# dim 281 divisor = 1
	# dim 282 name = ioat-msix_282
	# dim 282 algorithm = incremental
	# dim 282 multiplier = 1
	# dim 282 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RTR name = RTR
	# dim RTR algorithm = incremental
	# dim RTR multiplier = 1
	# dim RTR divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu1_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1101
	# name = cpu.cpu1_interrupts
	# title = CPU1 Interrupts
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu2_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1102
	# name = cpu.cpu2_interrupts
	# title = CPU2 Interrupts
	# dim 74 name = hpsa0-msix1_74
	# dim 74 algorithm = incremental
	# dim 74 multiplier = 1
	# dim 74 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu3_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1103
	# name = cpu.cpu3_interrupts
	# title = CPU3 Interrupts
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu4_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu4_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1104
	# name = cpu.cpu4_interrupts
	# title = CPU4 Interrupts
	# dim 75 name = hpsa0-msix2_75
	# dim 75 algorithm = incremental
	# dim 75 multiplier = 1
	# dim 75 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu5_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu5_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1105
	# name = cpu.cpu5_interrupts
	# title = CPU5 Interrupts
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu6_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu6_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1106
	# name = cpu.cpu6_interrupts
	# title = CPU6 Interrupts
	# dim 76 name = hpsa0-msix3_76
	# dim 76 algorithm = incremental
	# dim 76 multiplier = 1
	# dim 76 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu7_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu7_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1107
	# name = cpu.cpu7_interrupts
	# title = CPU7 Interrupts
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu8_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu8_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1108
	# name = cpu.cpu8_interrupts
	# title = CPU8 Interrupts
	# dim 77 name = hpsa0-msix4_77
	# dim 77 algorithm = incremental
	# dim 77 multiplier = 1
	# dim 77 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu9_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu9_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1109
	# name = cpu.cpu9_interrupts
	# title = CPU9 Interrupts
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu10_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu10_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1110
	# name = cpu.cpu10_interrupts
	# title = CPU10 Interrupts
	# dim 78 name = hpsa0-msix5_78
	# dim 78 algorithm = incremental
	# dim 78 multiplier = 1
	# dim 78 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu11_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu11_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1111
	# name = cpu.cpu11_interrupts
	# title = CPU11 Interrupts
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu12_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu12_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1112
	# name = cpu.cpu12_interrupts
	# title = CPU12 Interrupts
	# dim 89 name = hpsa0-msix16_89
	# dim 89 algorithm = incremental
	# dim 89 multiplier = 1
	# dim 89 divisor = 1
	# dim 284 name = ioat-msix_284
	# dim 284 algorithm = incremental
	# dim 284 multiplier = 1
	# dim 284 divisor = 1
	# dim 286 name = ioat-msix_286
	# dim 286 algorithm = incremental
	# dim 286 multiplier = 1
	# dim 286 divisor = 1
	# dim 287 name = ioat-msix_287
	# dim 287 algorithm = incremental
	# dim 287 multiplier = 1
	# dim 287 divisor = 1
	# dim 288 name = ioat-msix_288
	# dim 288 algorithm = incremental
	# dim 288 multiplier = 1
	# dim 288 divisor = 1
	# dim 289 name = ioat-msix_289
	# dim 289 algorithm = incremental
	# dim 289 multiplier = 1
	# dim 289 divisor = 1
	# dim 290 name = ioat-msix_290
	# dim 290 algorithm = incremental
	# dim 290 multiplier = 1
	# dim 290 divisor = 1
	# dim 291 name = ioat-msix_291
	# dim 291 algorithm = incremental
	# dim 291 multiplier = 1
	# dim 291 divisor = 1
	# dim 292 name = ioat-msix_292
	# dim 292 algorithm = incremental
	# dim 292 multiplier = 1
	# dim 292 divisor = 1
	# dim 323 name = ens6f3-tx-0_323
	# dim 323 algorithm = incremental
	# dim 323 multiplier = 1
	# dim 323 divisor = 1
	# dim 324 name = ens6f3-rx-1_324
	# dim 324 algorithm = incremental
	# dim 324 multiplier = 1
	# dim 324 divisor = 1
	# dim 325 name = ens6f3-rx-2_325
	# dim 325 algorithm = incremental
	# dim 325 multiplier = 1
	# dim 325 divisor = 1
	# dim 326 name = ens6f3-rx-3_326
	# dim 326 algorithm = incremental
	# dim 326 multiplier = 1
	# dim 326 divisor = 1
	# dim 327 name = ens6f3-rx-4_327
	# dim 327 algorithm = incremental
	# dim 327 multiplier = 1
	# dim 327 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu13_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu13_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1113
	# name = cpu.cpu13_interrupts
	# title = CPU13 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu14_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu14_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1114
	# name = cpu.cpu14_interrupts
	# title = CPU14 Interrupts
	# dim 90 name = hpsa0-msix17_90
	# dim 90 algorithm = incremental
	# dim 90 multiplier = 1
	# dim 90 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu15_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu15_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1115
	# name = cpu.cpu15_interrupts
	# title = CPU15 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu16_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu16_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1116
	# name = cpu.cpu16_interrupts
	# title = CPU16 Interrupts
	# dim 91 name = hpsa0-msix18_91
	# dim 91 algorithm = incremental
	# dim 91 multiplier = 1
	# dim 91 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu17_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu17_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1117
	# name = cpu.cpu17_interrupts
	# title = CPU17 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu18_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu18_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1118
	# name = cpu.cpu18_interrupts
	# title = CPU18 Interrupts
	# dim 92 name = hpsa0-msix19_92
	# dim 92 algorithm = incremental
	# dim 92 multiplier = 1
	# dim 92 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu19_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu19_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1119
	# name = cpu.cpu19_interrupts
	# title = CPU19 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu20_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu20_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1120
	# name = cpu.cpu20_interrupts
	# title = CPU20 Interrupts
	# dim 93 name = hpsa0-msix20_93
	# dim 93 algorithm = incremental
	# dim 93 multiplier = 1
	# dim 93 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu21_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu21_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1121
	# name = cpu.cpu21_interrupts
	# title = CPU21 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu22_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu22_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1122
	# name = cpu.cpu22_interrupts
	# title = CPU22 Interrupts
	# dim 94 name = hpsa0-msix21_94
	# dim 94 algorithm = incremental
	# dim 94 multiplier = 1
	# dim 94 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu23_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu23_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1123
	# name = cpu.cpu23_interrupts
	# title = CPU23 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu24_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu24_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1124
	# name = cpu.cpu24_interrupts
	# title = CPU24 Interrupts
	# dim 105 name = hpsa0-msix32_105
	# dim 105 algorithm = incremental
	# dim 105 multiplier = 1
	# dim 105 divisor = 1
	# dim 294 name = ioat-msix_294
	# dim 294 algorithm = incremental
	# dim 294 multiplier = 1
	# dim 294 divisor = 1
	# dim 296 name = ioat-msix_296
	# dim 296 algorithm = incremental
	# dim 296 multiplier = 1
	# dim 296 divisor = 1
	# dim 297 name = ioat-msix_297
	# dim 297 algorithm = incremental
	# dim 297 multiplier = 1
	# dim 297 divisor = 1
	# dim 298 name = ioat-msix_298
	# dim 298 algorithm = incremental
	# dim 298 multiplier = 1
	# dim 298 divisor = 1
	# dim 299 name = ioat-msix_299
	# dim 299 algorithm = incremental
	# dim 299 multiplier = 1
	# dim 299 divisor = 1
	# dim 300 name = ioat-msix_300
	# dim 300 algorithm = incremental
	# dim 300 multiplier = 1
	# dim 300 divisor = 1
	# dim 301 name = ioat-msix_301
	# dim 301 algorithm = incremental
	# dim 301 multiplier = 1
	# dim 301 divisor = 1
	# dim 302 name = ioat-msix_302
	# dim 302 algorithm = incremental
	# dim 302 multiplier = 1
	# dim 302 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu25_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu25_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1125
	# name = cpu.cpu25_interrupts
	# title = CPU25 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu26_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu26_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1126
	# name = cpu.cpu26_interrupts
	# title = CPU26 Interrupts
	# dim 106 name = hpsa0-msix33_106
	# dim 106 algorithm = incremental
	# dim 106 multiplier = 1
	# dim 106 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu27_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu27_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1127
	# name = cpu.cpu27_interrupts
	# title = CPU27 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu28_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu28_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1128
	# name = cpu.cpu28_interrupts
	# title = CPU28 Interrupts
	# dim 107 name = hpsa0-msix34_107
	# dim 107 algorithm = incremental
	# dim 107 multiplier = 1
	# dim 107 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu29_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu29_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1129
	# name = cpu.cpu29_interrupts
	# title = CPU29 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu30_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu30_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1130
	# name = cpu.cpu30_interrupts
	# title = CPU30 Interrupts
	# dim 108 name = hpsa0-msix35_108
	# dim 108 algorithm = incremental
	# dim 108 multiplier = 1
	# dim 108 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu31_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu31_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1131
	# name = cpu.cpu31_interrupts
	# title = CPU31 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu32_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu32_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1132
	# name = cpu.cpu32_interrupts
	# title = CPU32 Interrupts
	# dim 109 name = hpsa0-msix36_109
	# dim 109 algorithm = incremental
	# dim 109 multiplier = 1
	# dim 109 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu33_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu33_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1133
	# name = cpu.cpu33_interrupts
	# title = CPU33 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu34_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu34_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1134
	# name = cpu.cpu34_interrupts
	# title = CPU34 Interrupts
	# dim 110 name = hpsa0-msix37_110
	# dim 110 algorithm = incremental
	# dim 110 multiplier = 1
	# dim 110 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu35_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu35_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1135
	# name = cpu.cpu35_interrupts
	# title = CPU35 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu36_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu36_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1136
	# name = cpu.cpu36_interrupts
	# title = CPU36 Interrupts
	# dim 121 name = hpsa0-msix48_121
	# dim 121 algorithm = incremental
	# dim 121 multiplier = 1
	# dim 121 divisor = 1
	# dim 201 name = qla2xxx1_default_201
	# dim 201 algorithm = incremental
	# dim 201 multiplier = 1
	# dim 201 divisor = 1
	# dim 267 name = qla2xxx2_default_267
	# dim 267 algorithm = incremental
	# dim 267 multiplier = 1
	# dim 267 divisor = 1
	# dim 269 name = qla2xxx3_default_269
	# dim 269 algorithm = incremental
	# dim 269 multiplier = 1
	# dim 269 divisor = 1
	# dim 271 name = qla2xxx4_default_271
	# dim 271 algorithm = incremental
	# dim 271 multiplier = 1
	# dim 271 divisor = 1
	# dim 304 name = ioat-msix_304
	# dim 304 algorithm = incremental
	# dim 304 multiplier = 1
	# dim 304 divisor = 1
	# dim 306 name = ioat-msix_306
	# dim 306 algorithm = incremental
	# dim 306 multiplier = 1
	# dim 306 divisor = 1
	# dim 307 name = ioat-msix_307
	# dim 307 algorithm = incremental
	# dim 307 multiplier = 1
	# dim 307 divisor = 1
	# dim 308 name = ioat-msix_308
	# dim 308 algorithm = incremental
	# dim 308 multiplier = 1
	# dim 308 divisor = 1
	# dim 309 name = ioat-msix_309
	# dim 309 algorithm = incremental
	# dim 309 multiplier = 1
	# dim 309 divisor = 1
	# dim 310 name = ioat-msix_310
	# dim 310 algorithm = incremental
	# dim 310 multiplier = 1
	# dim 310 divisor = 1
	# dim 311 name = ioat-msix_311
	# dim 311 algorithm = incremental
	# dim 311 multiplier = 1
	# dim 311 divisor = 1
	# dim 312 name = ioat-msix_312
	# dim 312 algorithm = incremental
	# dim 312 multiplier = 1
	# dim 312 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu37_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu37_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1137
	# name = cpu.cpu37_interrupts
	# title = CPU37 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu38_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu38_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1138
	# name = cpu.cpu38_interrupts
	# title = CPU38 Interrupts
	# dim 122 name = hpsa0-msix49_122
	# dim 122 algorithm = incremental
	# dim 122 multiplier = 1
	# dim 122 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu39_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu39_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1139
	# name = cpu.cpu39_interrupts
	# title = CPU39 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu40_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu40_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1140
	# name = cpu.cpu40_interrupts
	# title = CPU40 Interrupts
	# dim 123 name = hpsa0-msix50_123
	# dim 123 algorithm = incremental
	# dim 123 multiplier = 1
	# dim 123 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu41_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu41_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1141
	# name = cpu.cpu41_interrupts
	# title = CPU41 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu42_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu42_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1142
	# name = cpu.cpu42_interrupts
	# title = CPU42 Interrupts
	# dim 124 name = hpsa0-msix51_124
	# dim 124 algorithm = incremental
	# dim 124 multiplier = 1
	# dim 124 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu43_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu43_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1143
	# name = cpu.cpu43_interrupts
	# title = CPU43 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu44_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu44_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1144
	# name = cpu.cpu44_interrupts
	# title = CPU44 Interrupts
	# dim 125 name = hpsa0-msix52_125
	# dim 125 algorithm = incremental
	# dim 125 multiplier = 1
	# dim 125 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu45_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu45_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1145
	# name = cpu.cpu45_interrupts
	# title = CPU45 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu46_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu46_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1146
	# name = cpu.cpu46_interrupts
	# title = CPU46 Interrupts
	# dim 126 name = hpsa0-msix53_126
	# dim 126 algorithm = incremental
	# dim 126 multiplier = 1
	# dim 126 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu47_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu47_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1147
	# name = cpu.cpu47_interrupts
	# title = CPU47 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu48_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu48_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1148
	# name = cpu.cpu48_interrupts
	# title = CPU48 Interrupts
	# dim 79 name = hpsa0-msix6_79
	# dim 79 algorithm = incremental
	# dim 79 multiplier = 1
	# dim 79 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu49_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu49_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1149
	# name = cpu.cpu49_interrupts
	# title = CPU49 Interrupts
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu50_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu50_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1150
	# name = cpu.cpu50_interrupts
	# title = CPU50 Interrupts
	# dim 80 name = hpsa0-msix7_80
	# dim 80 algorithm = incremental
	# dim 80 multiplier = 1
	# dim 80 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu51_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu51_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1151
	# name = cpu.cpu51_interrupts
	# title = CPU51 Interrupts
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu52_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu52_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1152
	# name = cpu.cpu52_interrupts
	# title = CPU52 Interrupts
	# dim 81 name = hpsa0-msix8_81
	# dim 81 algorithm = incremental
	# dim 81 multiplier = 1
	# dim 81 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu53_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu53_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1153
	# name = cpu.cpu53_interrupts
	# title = CPU53 Interrupts
	# dim 82 name = hpsa0-msix9_82
	# dim 82 algorithm = incremental
	# dim 82 multiplier = 1
	# dim 82 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu54_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu54_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1154
	# name = cpu.cpu54_interrupts
	# title = CPU54 Interrupts
	# dim 83 name = hpsa0-msix10_83
	# dim 83 algorithm = incremental
	# dim 83 multiplier = 1
	# dim 83 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu55_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu55_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1155
	# name = cpu.cpu55_interrupts
	# title = CPU55 Interrupts
	# dim 84 name = hpsa0-msix11_84
	# dim 84 algorithm = incremental
	# dim 84 multiplier = 1
	# dim 84 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu56_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu56_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1156
	# name = cpu.cpu56_interrupts
	# title = CPU56 Interrupts
	# dim 85 name = hpsa0-msix12_85
	# dim 85 algorithm = incremental
	# dim 85 multiplier = 1
	# dim 85 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu57_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu57_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1157
	# name = cpu.cpu57_interrupts
	# title = CPU57 Interrupts
	# dim 86 name = hpsa0-msix13_86
	# dim 86 algorithm = incremental
	# dim 86 multiplier = 1
	# dim 86 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu58_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu58_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1158
	# name = cpu.cpu58_interrupts
	# title = CPU58 Interrupts
	# dim 87 name = hpsa0-msix14_87
	# dim 87 algorithm = incremental
	# dim 87 multiplier = 1
	# dim 87 divisor = 1
	# dim 313 name = eno49-tx-0_313
	# dim 313 algorithm = incremental
	# dim 313 multiplier = 1
	# dim 313 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu59_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu59_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1159
	# name = cpu.cpu59_interrupts
	# title = CPU59 Interrupts
	# dim 88 name = hpsa0-msix15_88
	# dim 88 algorithm = incremental
	# dim 88 multiplier = 1
	# dim 88 divisor = 1
	# dim 314 name = eno49-rx-1_314
	# dim 314 algorithm = incremental
	# dim 314 multiplier = 1
	# dim 314 divisor = 1
	# dim 315 name = eno49-rx-2_315
	# dim 315 algorithm = incremental
	# dim 315 multiplier = 1
	# dim 315 divisor = 1
	# dim 316 name = eno49-rx-3_316
	# dim 316 algorithm = incremental
	# dim 316 multiplier = 1
	# dim 316 divisor = 1
	# dim 317 name = eno49-rx-4_317
	# dim 317 algorithm = incremental
	# dim 317 multiplier = 1
	# dim 317 divisor = 1
	# dim 318 name = eno50-tx-0_318
	# dim 318 algorithm = incremental
	# dim 318 multiplier = 1
	# dim 318 divisor = 1
	# dim 319 name = eno50-rx-1_319
	# dim 319 algorithm = incremental
	# dim 319 multiplier = 1
	# dim 319 divisor = 1
	# dim 320 name = eno50-rx-2_320
	# dim 320 algorithm = incremental
	# dim 320 multiplier = 1
	# dim 320 divisor = 1
	# dim 321 name = eno50-rx-3_321
	# dim 321 algorithm = incremental
	# dim 321 multiplier = 1
	# dim 321 divisor = 1
	# dim 322 name = eno50-rx-4_322
	# dim 322 algorithm = incremental
	# dim 322 multiplier = 1
	# dim 322 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu60_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu60_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1160
	# name = cpu.cpu60_interrupts
	# title = CPU60 Interrupts
	# dim 95 name = hpsa0-msix22_95
	# dim 95 algorithm = incremental
	# dim 95 multiplier = 1
	# dim 95 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu61_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu61_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1161
	# name = cpu.cpu61_interrupts
	# title = CPU61 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu62_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu62_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1162
	# name = cpu.cpu62_interrupts
	# title = CPU62 Interrupts
	# dim 96 name = hpsa0-msix23_96
	# dim 96 algorithm = incremental
	# dim 96 multiplier = 1
	# dim 96 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu63_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu63_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1163
	# name = cpu.cpu63_interrupts
	# title = CPU63 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu64_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu64_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1164
	# name = cpu.cpu64_interrupts
	# title = CPU64 Interrupts
	# dim 97 name = hpsa0-msix24_97
	# dim 97 algorithm = incremental
	# dim 97 multiplier = 1
	# dim 97 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu65_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu65_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1165
	# name = cpu.cpu65_interrupts
	# title = CPU65 Interrupts
	# dim 98 name = hpsa0-msix25_98
	# dim 98 algorithm = incremental
	# dim 98 multiplier = 1
	# dim 98 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu66_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu66_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1166
	# name = cpu.cpu66_interrupts
	# title = CPU66 Interrupts
	# dim 99 name = hpsa0-msix26_99
	# dim 99 algorithm = incremental
	# dim 99 multiplier = 1
	# dim 99 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu67_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu67_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1167
	# name = cpu.cpu67_interrupts
	# title = CPU67 Interrupts
	# dim 100 name = hpsa0-msix27_100
	# dim 100 algorithm = incremental
	# dim 100 multiplier = 1
	# dim 100 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu68_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu68_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1168
	# name = cpu.cpu68_interrupts
	# title = CPU68 Interrupts
	# dim 101 name = hpsa0-msix28_101
	# dim 101 algorithm = incremental
	# dim 101 multiplier = 1
	# dim 101 divisor = 1
	# dim 323 name = ens6f3-tx-0_323
	# dim 323 algorithm = incremental
	# dim 323 multiplier = 1
	# dim 323 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu69_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu69_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1169
	# name = cpu.cpu69_interrupts
	# title = CPU69 Interrupts
	# dim 102 name = hpsa0-msix29_102
	# dim 102 algorithm = incremental
	# dim 102 multiplier = 1
	# dim 102 divisor = 1
	# dim 324 name = ens6f3-rx-1_324
	# dim 324 algorithm = incremental
	# dim 324 multiplier = 1
	# dim 324 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu70_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu70_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1170
	# name = cpu.cpu70_interrupts
	# title = CPU70 Interrupts
	# dim 103 name = hpsa0-msix30_103
	# dim 103 algorithm = incremental
	# dim 103 multiplier = 1
	# dim 103 divisor = 1
	# dim 325 name = ens6f3-rx-2_325
	# dim 325 algorithm = incremental
	# dim 325 multiplier = 1
	# dim 325 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu71_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu71_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1171
	# name = cpu.cpu71_interrupts
	# title = CPU71 Interrupts
	# dim 104 name = hpsa0-msix31_104
	# dim 104 algorithm = incremental
	# dim 104 multiplier = 1
	# dim 104 divisor = 1
	# dim 326 name = ens6f3-rx-3_326
	# dim 326 algorithm = incremental
	# dim 326 multiplier = 1
	# dim 326 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu72_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu72_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1172
	# name = cpu.cpu72_interrupts
	# title = CPU72 Interrupts
	# dim 111 name = hpsa0-msix38_111
	# dim 111 algorithm = incremental
	# dim 111 multiplier = 1
	# dim 111 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu73_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu73_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1173
	# name = cpu.cpu73_interrupts
	# title = CPU73 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu74_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu74_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1174
	# name = cpu.cpu74_interrupts
	# title = CPU74 Interrupts
	# dim 112 name = hpsa0-msix39_112
	# dim 112 algorithm = incremental
	# dim 112 multiplier = 1
	# dim 112 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu75_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu75_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1175
	# name = cpu.cpu75_interrupts
	# title = CPU75 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu76_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu76_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1176
	# name = cpu.cpu76_interrupts
	# title = CPU76 Interrupts
	# dim 113 name = hpsa0-msix40_113
	# dim 113 algorithm = incremental
	# dim 113 multiplier = 1
	# dim 113 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu77_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu77_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1177
	# name = cpu.cpu77_interrupts
	# title = CPU77 Interrupts
	# dim 114 name = hpsa0-msix41_114
	# dim 114 algorithm = incremental
	# dim 114 multiplier = 1
	# dim 114 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu78_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu78_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1178
	# name = cpu.cpu78_interrupts
	# title = CPU78 Interrupts
	# dim 115 name = hpsa0-msix42_115
	# dim 115 algorithm = incremental
	# dim 115 multiplier = 1
	# dim 115 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu79_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu79_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1179
	# name = cpu.cpu79_interrupts
	# title = CPU79 Interrupts
	# dim 116 name = hpsa0-msix43_116
	# dim 116 algorithm = incremental
	# dim 116 multiplier = 1
	# dim 116 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu80_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu80_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1180
	# name = cpu.cpu80_interrupts
	# title = CPU80 Interrupts
	# dim 117 name = hpsa0-msix44_117
	# dim 117 algorithm = incremental
	# dim 117 multiplier = 1
	# dim 117 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu81_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu81_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1181
	# name = cpu.cpu81_interrupts
	# title = CPU81 Interrupts
	# dim 118 name = hpsa0-msix45_118
	# dim 118 algorithm = incremental
	# dim 118 multiplier = 1
	# dim 118 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu82_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu82_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1182
	# name = cpu.cpu82_interrupts
	# title = CPU82 Interrupts
	# dim 119 name = hpsa0-msix46_119
	# dim 119 algorithm = incremental
	# dim 119 multiplier = 1
	# dim 119 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu83_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu83_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1183
	# name = cpu.cpu83_interrupts
	# title = CPU83 Interrupts
	# dim 120 name = hpsa0-msix47_120
	# dim 120 algorithm = incremental
	# dim 120 multiplier = 1
	# dim 120 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu84_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu84_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1184
	# name = cpu.cpu84_interrupts
	# title = CPU84 Interrupts
	# dim 127 name = hpsa0-msix54_127
	# dim 127 algorithm = incremental
	# dim 127 multiplier = 1
	# dim 127 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu85_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu85_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1185
	# name = cpu.cpu85_interrupts
	# title = CPU85 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu86_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu86_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1186
	# name = cpu.cpu86_interrupts
	# title = CPU86 Interrupts
	# dim 128 name = hpsa0-msix55_128
	# dim 128 algorithm = incremental
	# dim 128 multiplier = 1
	# dim 128 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu87_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu87_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1187
	# name = cpu.cpu87_interrupts
	# title = CPU87 Interrupts
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu88_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu88_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1188
	# name = cpu.cpu88_interrupts
	# title = CPU88 Interrupts
	# dim 129 name = hpsa0-msix56_129
	# dim 129 algorithm = incremental
	# dim 129 multiplier = 1
	# dim 129 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu89_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu89_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1189
	# name = cpu.cpu89_interrupts
	# title = CPU89 Interrupts
	# dim 130 name = hpsa0-msix57_130
	# dim 130 algorithm = incremental
	# dim 130 multiplier = 1
	# dim 130 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu90_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu90_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1190
	# name = cpu.cpu90_interrupts
	# title = CPU90 Interrupts
	# dim 131 name = hpsa0-msix58_131
	# dim 131 algorithm = incremental
	# dim 131 multiplier = 1
	# dim 131 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu91_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu91_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1191
	# name = cpu.cpu91_interrupts
	# title = CPU91 Interrupts
	# dim 132 name = hpsa0-msix59_132
	# dim 132 algorithm = incremental
	# dim 132 multiplier = 1
	# dim 132 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu92_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu92_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1192
	# name = cpu.cpu92_interrupts
	# title = CPU92 Interrupts
	# dim 133 name = hpsa0-msix60_133
	# dim 133 algorithm = incremental
	# dim 133 multiplier = 1
	# dim 133 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu93_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu93_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1193
	# name = cpu.cpu93_interrupts
	# title = CPU93 Interrupts
	# dim 134 name = hpsa0-msix61_134
	# dim 134 algorithm = incremental
	# dim 134 multiplier = 1
	# dim 134 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu94_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu94_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1194
	# name = cpu.cpu94_interrupts
	# title = CPU94 Interrupts
	# dim 135 name = hpsa0-msix62_135
	# dim 135 algorithm = incremental
	# dim 135 multiplier = 1
	# dim 135 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[cpu.cpu95_interrupts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu95_interrupts
	# chart type = stacked
	# type = cpu
	# family = interrupts
	# units = interrupts/s
	# context = cpu.interrupts
	# priority = 1195
	# name = cpu.cpu95_interrupts
	# title = CPU95 Interrupts
	# dim 136 name = hpsa0-msix63_136
	# dim 136 algorithm = incremental
	# dim 136 multiplier = 1
	# dim 136 divisor = 1
	# dim NMI name = NMI
	# dim NMI algorithm = incremental
	# dim NMI multiplier = 1
	# dim NMI divisor = 1
	# dim LOC name = LOC
	# dim LOC algorithm = incremental
	# dim LOC multiplier = 1
	# dim LOC divisor = 1
	# dim PMI name = PMI
	# dim PMI algorithm = incremental
	# dim PMI multiplier = 1
	# dim PMI divisor = 1
	# dim IWI name = IWI
	# dim IWI algorithm = incremental
	# dim IWI multiplier = 1
	# dim IWI divisor = 1
	# dim RES name = RES
	# dim RES algorithm = incremental
	# dim RES multiplier = 1
	# dim RES divisor = 1
	# dim CAL name = CAL
	# dim CAL algorithm = incremental
	# dim CAL multiplier = 1
	# dim CAL divisor = 1
	# dim TLB name = TLB
	# dim TLB algorithm = incremental
	# dim TLB multiplier = 1
	# dim TLB divisor = 1
	# dim MCP name = MCP
	# dim MCP algorithm = incremental
	# dim MCP multiplier = 1
	# dim MCP divisor = 1
	# dim PIN name = PIN
	# dim PIN algorithm = incremental
	# dim PIN multiplier = 1
	# dim PIN divisor = 1

[system.softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.softirqs
	# chart type = stacked
	# type = system
	# family = softirqs
	# units = softirqs/s
	# context = system.softirqs
	# priority = 950
	# name = system.softirqs
	# title = System softirqs
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu0_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3000
	# name = cpu.cpu0_softirqs
	# title = CPU0 softirqs
	# dim HI name = HI
	# dim HI algorithm = incremental
	# dim HI multiplier = 1
	# dim HI divisor = 1
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu1_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3001
	# name = cpu.cpu1_softirqs
	# title = CPU1 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu2_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3002
	# name = cpu.cpu2_softirqs
	# title = CPU2 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu3_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3003
	# name = cpu.cpu3_softirqs
	# title = CPU3 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu4_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu4_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3004
	# name = cpu.cpu4_softirqs
	# title = CPU4 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu5_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu5_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3005
	# name = cpu.cpu5_softirqs
	# title = CPU5 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu6_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu6_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3006
	# name = cpu.cpu6_softirqs
	# title = CPU6 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu7_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu7_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3007
	# name = cpu.cpu7_softirqs
	# title = CPU7 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu8_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu8_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3008
	# name = cpu.cpu8_softirqs
	# title = CPU8 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu9_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu9_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3009
	# name = cpu.cpu9_softirqs
	# title = CPU9 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu10_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu10_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3010
	# name = cpu.cpu10_softirqs
	# title = CPU10 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu11_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu11_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3011
	# name = cpu.cpu11_softirqs
	# title = CPU11 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu12_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu12_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3012
	# name = cpu.cpu12_softirqs
	# title = CPU12 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu13_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu13_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3013
	# name = cpu.cpu13_softirqs
	# title = CPU13 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu14_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu14_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3014
	# name = cpu.cpu14_softirqs
	# title = CPU14 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu15_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu15_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3015
	# name = cpu.cpu15_softirqs
	# title = CPU15 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu16_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu16_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3016
	# name = cpu.cpu16_softirqs
	# title = CPU16 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu17_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu17_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3017
	# name = cpu.cpu17_softirqs
	# title = CPU17 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu18_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu18_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3018
	# name = cpu.cpu18_softirqs
	# title = CPU18 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu19_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu19_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3019
	# name = cpu.cpu19_softirqs
	# title = CPU19 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu20_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu20_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3020
	# name = cpu.cpu20_softirqs
	# title = CPU20 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu21_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu21_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3021
	# name = cpu.cpu21_softirqs
	# title = CPU21 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu22_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu22_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3022
	# name = cpu.cpu22_softirqs
	# title = CPU22 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu23_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu23_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3023
	# name = cpu.cpu23_softirqs
	# title = CPU23 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu24_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu24_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3024
	# name = cpu.cpu24_softirqs
	# title = CPU24 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu25_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu25_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3025
	# name = cpu.cpu25_softirqs
	# title = CPU25 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu26_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu26_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3026
	# name = cpu.cpu26_softirqs
	# title = CPU26 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu27_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu27_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3027
	# name = cpu.cpu27_softirqs
	# title = CPU27 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu28_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu28_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3028
	# name = cpu.cpu28_softirqs
	# title = CPU28 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu29_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu29_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3029
	# name = cpu.cpu29_softirqs
	# title = CPU29 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu30_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu30_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3030
	# name = cpu.cpu30_softirqs
	# title = CPU30 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu31_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu31_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3031
	# name = cpu.cpu31_softirqs
	# title = CPU31 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu32_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu32_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3032
	# name = cpu.cpu32_softirqs
	# title = CPU32 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu33_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu33_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3033
	# name = cpu.cpu33_softirqs
	# title = CPU33 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu34_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu34_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3034
	# name = cpu.cpu34_softirqs
	# title = CPU34 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu35_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu35_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3035
	# name = cpu.cpu35_softirqs
	# title = CPU35 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu36_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu36_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3036
	# name = cpu.cpu36_softirqs
	# title = CPU36 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu37_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu37_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3037
	# name = cpu.cpu37_softirqs
	# title = CPU37 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu38_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu38_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3038
	# name = cpu.cpu38_softirqs
	# title = CPU38 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu39_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu39_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3039
	# name = cpu.cpu39_softirqs
	# title = CPU39 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu40_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu40_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3040
	# name = cpu.cpu40_softirqs
	# title = CPU40 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu41_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu41_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3041
	# name = cpu.cpu41_softirqs
	# title = CPU41 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu42_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu42_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3042
	# name = cpu.cpu42_softirqs
	# title = CPU42 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu43_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu43_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3043
	# name = cpu.cpu43_softirqs
	# title = CPU43 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu44_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu44_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3044
	# name = cpu.cpu44_softirqs
	# title = CPU44 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu45_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu45_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3045
	# name = cpu.cpu45_softirqs
	# title = CPU45 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu46_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu46_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3046
	# name = cpu.cpu46_softirqs
	# title = CPU46 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu47_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu47_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3047
	# name = cpu.cpu47_softirqs
	# title = CPU47 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu48_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu48_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3048
	# name = cpu.cpu48_softirqs
	# title = CPU48 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu49_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu49_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3049
	# name = cpu.cpu49_softirqs
	# title = CPU49 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu50_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu50_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3050
	# name = cpu.cpu50_softirqs
	# title = CPU50 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu51_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu51_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3051
	# name = cpu.cpu51_softirqs
	# title = CPU51 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu52_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu52_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3052
	# name = cpu.cpu52_softirqs
	# title = CPU52 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu53_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu53_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3053
	# name = cpu.cpu53_softirqs
	# title = CPU53 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu54_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu54_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3054
	# name = cpu.cpu54_softirqs
	# title = CPU54 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu55_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu55_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3055
	# name = cpu.cpu55_softirqs
	# title = CPU55 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu56_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu56_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3056
	# name = cpu.cpu56_softirqs
	# title = CPU56 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu57_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu57_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3057
	# name = cpu.cpu57_softirqs
	# title = CPU57 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu58_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu58_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3058
	# name = cpu.cpu58_softirqs
	# title = CPU58 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu59_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu59_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3059
	# name = cpu.cpu59_softirqs
	# title = CPU59 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu60_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu60_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3060
	# name = cpu.cpu60_softirqs
	# title = CPU60 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu61_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu61_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3061
	# name = cpu.cpu61_softirqs
	# title = CPU61 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu62_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu62_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3062
	# name = cpu.cpu62_softirqs
	# title = CPU62 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu63_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu63_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3063
	# name = cpu.cpu63_softirqs
	# title = CPU63 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu64_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu64_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3064
	# name = cpu.cpu64_softirqs
	# title = CPU64 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu65_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu65_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3065
	# name = cpu.cpu65_softirqs
	# title = CPU65 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu66_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu66_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3066
	# name = cpu.cpu66_softirqs
	# title = CPU66 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu67_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu67_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3067
	# name = cpu.cpu67_softirqs
	# title = CPU67 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu68_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu68_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3068
	# name = cpu.cpu68_softirqs
	# title = CPU68 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu69_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu69_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3069
	# name = cpu.cpu69_softirqs
	# title = CPU69 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu70_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu70_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3070
	# name = cpu.cpu70_softirqs
	# title = CPU70 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu71_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu71_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3071
	# name = cpu.cpu71_softirqs
	# title = CPU71 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu72_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu72_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3072
	# name = cpu.cpu72_softirqs
	# title = CPU72 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu73_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu73_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3073
	# name = cpu.cpu73_softirqs
	# title = CPU73 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[netdata.apps_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_cpu
	# chart type = stacked
	# type = netdata
	# family = apps.plugin
	# units = milliseconds/s
	# context = netdata.apps_cpu
	# priority = 140000
	# name = netdata.apps_cpu
	# title = Apps Plugin CPU
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[cpu.cpu74_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu74_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3074
	# name = cpu.cpu74_softirqs
	# title = CPU74 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[netdata.apps_sizes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_sizes
	# chart type = line
	# type = netdata
	# family = apps.plugin
	# units = files/s
	# context = netdata.apps_sizes
	# priority = 140001
	# name = netdata.apps_sizes
	# title = Apps Plugin Files
	# dim calls name = calls
	# dim calls algorithm = incremental
	# dim calls multiplier = 1
	# dim calls divisor = 1
	# dim files name = files
	# dim files algorithm = incremental
	# dim files multiplier = 1
	# dim files divisor = 1
	# dim filenames name = filenames
	# dim filenames algorithm = incremental
	# dim filenames multiplier = 1
	# dim filenames divisor = 1
	# dim inode_changes name = inode_changes
	# dim inode_changes algorithm = incremental
	# dim inode_changes multiplier = 1
	# dim inode_changes divisor = 1
	# dim link_changes name = link_changes
	# dim link_changes algorithm = incremental
	# dim link_changes multiplier = 1
	# dim link_changes divisor = 1
	# dim pids name = pids
	# dim pids algorithm = absolute
	# dim pids multiplier = 1
	# dim pids divisor = 1
	# dim fds name = fds
	# dim fds algorithm = absolute
	# dim fds multiplier = 1
	# dim fds divisor = 1
	# dim targets name = targets
	# dim targets algorithm = absolute
	# dim targets multiplier = 1
	# dim targets divisor = 1
	# dim new_pids name = new pids
	# dim new_pids algorithm = incremental
	# dim new_pids multiplier = 1
	# dim new_pids divisor = 1

[cpu.cpu75_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu75_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3075
	# name = cpu.cpu75_softirqs
	# title = CPU75 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[netdata.apps_fix]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_fix
	# chart type = line
	# type = netdata
	# family = apps.plugin
	# units = percentage
	# context = netdata.apps_fix
	# priority = 140002
	# name = netdata.apps_fix
	# title = Apps Plugin Normalization Ratios
	# dim utime name = utime
	# dim utime algorithm = absolute
	# dim utime multiplier = 1
	# dim utime divisor = 10000
	# dim stime name = stime
	# dim stime algorithm = absolute
	# dim stime multiplier = 1
	# dim stime divisor = 10000
	# dim gtime name = gtime
	# dim gtime algorithm = absolute
	# dim gtime multiplier = 1
	# dim gtime divisor = 10000
	# dim minflt name = minflt
	# dim minflt algorithm = absolute
	# dim minflt multiplier = 1
	# dim minflt divisor = 10000
	# dim majflt name = majflt
	# dim majflt algorithm = absolute
	# dim majflt multiplier = 1
	# dim majflt divisor = 10000

[cpu.cpu76_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu76_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3076
	# name = cpu.cpu76_softirqs
	# title = CPU76 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[netdata.apps_children_fix]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.apps_children_fix
	# chart type = line
	# type = netdata
	# family = apps.plugin
	# units = percentage
	# context = netdata.apps_children_fix
	# priority = 140003
	# name = netdata.apps_children_fix
	# title = Apps Plugin Exited Children Normalization Ratios
	# dim cutime name = cutime
	# dim cutime algorithm = absolute
	# dim cutime multiplier = 1
	# dim cutime divisor = 10000
	# dim cstime name = cstime
	# dim cstime algorithm = absolute
	# dim cstime multiplier = 1
	# dim cstime divisor = 10000
	# dim cgtime name = cgtime
	# dim cgtime algorithm = absolute
	# dim cgtime multiplier = 1
	# dim cgtime divisor = 10000
	# dim cminflt name = cminflt
	# dim cminflt algorithm = absolute
	# dim cminflt multiplier = 1
	# dim cminflt divisor = 10000
	# dim cmajflt name = cmajflt
	# dim cmajflt algorithm = absolute
	# dim cmajflt multiplier = 1
	# dim cmajflt divisor = 10000

[cpu.cpu77_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu77_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3077
	# name = cpu.cpu77_softirqs
	# title = CPU77 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu
	# chart type = stacked
	# type = apps
	# family = cpu
	# units = percentage
	# context = apps.cpu
	# priority = 20001
	# name = apps.cpu
	# title = Apps CPU Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10000

[cpu.cpu78_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu78_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3078
	# name = cpu.cpu78_softirqs
	# title = CPU78 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu79_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu79_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3079
	# name = cpu.cpu79_softirqs
	# title = CPU79 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu80_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu80_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3080
	# name = cpu.cpu80_softirqs
	# title = CPU80 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu81_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu81_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3081
	# name = cpu.cpu81_softirqs
	# title = CPU81 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.mem
	# chart type = stacked
	# type = apps
	# family = mem
	# units = MiB
	# context = apps.mem
	# priority = 20003
	# name = apps.mem
	# title = Apps Real Memory (w/o shared)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1024
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1024
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1024
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1024
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1024
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1024
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1024
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1024
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1024
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1024
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1024
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1024
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1024
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1024
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1024
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1024

[cpu.cpu82_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu82_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3082
	# name = cpu.cpu82_softirqs
	# title = CPU82 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu83_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu83_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3083
	# name = cpu.cpu83_softirqs
	# title = CPU83 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu84_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu84_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3084
	# name = cpu.cpu84_softirqs
	# title = CPU84 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.vmem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.vmem
	# chart type = stacked
	# type = apps
	# family = mem
	# units = MiB
	# context = apps.vmem
	# priority = 20005
	# name = apps.vmem
	# title = Apps Virtual Memory Size
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1024
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1024
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1024
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1024
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1024
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1024
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1024
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1024
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1024
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1024
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1024
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1024
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1024
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1024
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1024
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1024

[cpu.cpu85_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu85_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3085
	# name = cpu.cpu85_softirqs
	# title = CPU85 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu86_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu86_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3086
	# name = cpu.cpu86_softirqs
	# title = CPU86 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu87_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu87_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3087
	# name = cpu.cpu87_softirqs
	# title = CPU87 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.threads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.threads
	# chart type = stacked
	# type = apps
	# family = processes
	# units = threads
	# context = apps.threads
	# priority = 20006
	# name = apps.threads
	# title = Apps Threads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[cpu.cpu88_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu88_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3088
	# name = cpu.cpu88_softirqs
	# title = CPU88 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu89_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu89_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3089
	# name = cpu.cpu89_softirqs
	# title = CPU89 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu90_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu90_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3090
	# name = cpu.cpu90_softirqs
	# title = CPU90 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.processes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.processes
	# chart type = stacked
	# type = apps
	# family = processes
	# units = processes
	# context = apps.processes
	# priority = 20007
	# name = apps.processes
	# title = Apps Processes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[cpu.cpu91_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu91_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3091
	# name = cpu.cpu91_softirqs
	# title = CPU91 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu92_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu92_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3092
	# name = cpu.cpu92_softirqs
	# title = CPU92 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu93_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu93_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3093
	# name = cpu.cpu93_softirqs
	# title = CPU93 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[apps.cpu_user]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu_user
	# chart type = stacked
	# type = apps
	# family = cpu
	# units = percentage
	# context = apps.cpu_user
	# priority = 20020
	# name = apps.cpu_user
	# title = Apps CPU User Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10000

[cpu.cpu94_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu94_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3094
	# name = cpu.cpu94_softirqs
	# title = CPU94 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[cpu.cpu95_softirqs]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu95_softirqs
	# chart type = stacked
	# type = cpu
	# family = softirqs
	# units = softirqs/s
	# context = cpu.softirqs
	# priority = 3095
	# name = cpu.cpu95_softirqs
	# title = CPU95 softirqs
	# dim TIMER name = TIMER
	# dim TIMER algorithm = incremental
	# dim TIMER multiplier = 1
	# dim TIMER divisor = 1
	# dim NET_TX name = NET_TX
	# dim NET_TX algorithm = incremental
	# dim NET_TX multiplier = 1
	# dim NET_TX divisor = 1
	# dim NET_RX name = NET_RX
	# dim NET_RX algorithm = incremental
	# dim NET_RX multiplier = 1
	# dim NET_RX divisor = 1
	# dim BLOCK name = BLOCK
	# dim BLOCK algorithm = incremental
	# dim BLOCK multiplier = 1
	# dim BLOCK divisor = 1
	# dim TASKLET name = TASKLET
	# dim TASKLET algorithm = incremental
	# dim TASKLET multiplier = 1
	# dim TASKLET divisor = 1
	# dim SCHED name = SCHED
	# dim SCHED algorithm = incremental
	# dim SCHED multiplier = 1
	# dim SCHED divisor = 1
	# dim RCU name = RCU
	# dim RCU algorithm = incremental
	# dim RCU multiplier = 1
	# dim RCU divisor = 1

[system.pgpgio]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.pgpgio
	# chart type = area
	# type = system
	# family = disk
	# units = KiB/s
	# context = system.pgpgio
	# priority = 151
	# name = system.pgpgio
	# title = Memory Paged from/to disk
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 1
	# dim in divisor = 1
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -1
	# dim out divisor = 1

[mem.pgfaults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.pgfaults
	# chart type = line
	# type = mem
	# family = system
	# units = faults/s
	# context = mem.pgfaults
	# priority = 1030
	# name = mem.pgfaults
	# title = Memory Page Faults
	# dim minor name = minor
	# dim minor algorithm = incremental
	# dim minor multiplier = 1
	# dim minor divisor = 1
	# dim major name = major
	# dim major algorithm = incremental
	# dim major multiplier = -1
	# dim major divisor = 1

[apps.cpu_system]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.cpu_system
	# chart type = stacked
	# type = apps
	# family = cpu
	# units = percentage
	# context = apps.cpu_system
	# priority = 20021
	# name = apps.cpu_system
	# title = Apps CPU System Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10000

[mem.numa]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.numa
	# chart type = line
	# type = mem
	# family = numa
	# units = events/s
	# context = mem.numa
	# priority = 1400
	# name = mem.numa
	# title = NUMA events
	# dim local name = local
	# dim local algorithm = incremental
	# dim local multiplier = 1
	# dim local divisor = 1
	# dim foreign name = foreign
	# dim foreign algorithm = incremental
	# dim foreign multiplier = 1
	# dim foreign divisor = 1
	# dim interleave name = interleave
	# dim interleave algorithm = incremental
	# dim interleave multiplier = 1
	# dim interleave divisor = 1
	# dim other name = other
	# dim other algorithm = incremental
	# dim other multiplier = 1
	# dim other divisor = 1
	# dim pte_updates name = pte_updates
	# dim pte_updates algorithm = incremental
	# dim pte_updates multiplier = 1
	# dim pte_updates divisor = 1
	# dim huge_pte_updates name = huge_pte_updates
	# dim huge_pte_updates algorithm = incremental
	# dim huge_pte_updates multiplier = 1
	# dim huge_pte_updates divisor = 1
	# dim hint_faults name = hint_faults
	# dim hint_faults algorithm = incremental
	# dim hint_faults multiplier = 1
	# dim hint_faults divisor = 1
	# dim hint_faults_local name = hint_faults_local
	# dim hint_faults_local algorithm = incremental
	# dim hint_faults_local multiplier = 1
	# dim hint_faults_local divisor = 1
	# dim pages_migrated name = pages_migrated
	# dim pages_migrated algorithm = incremental
	# dim pages_migrated multiplier = 1
	# dim pages_migrated divisor = 1

[system.ram]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ram
	# chart type = stacked
	# type = system
	# family = ram
	# units = MiB
	# context = system.ram
	# priority = 200
	# name = system.ram
	# title = System RAM
	# dim free name = free
	# dim free algorithm = absolute
	# dim free multiplier = 1
	# dim free divisor = 1024
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1024
	# dim cached name = cached
	# dim cached algorithm = absolute
	# dim cached multiplier = 1
	# dim cached divisor = 1024
	# dim buffers name = buffers
	# dim buffers algorithm = absolute
	# dim buffers multiplier = 1
	# dim buffers divisor = 1024

[mem.available]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.available
	# chart type = area
	# type = mem
	# family = system
	# units = MiB
	# context = mem.available
	# priority = 1010
	# name = mem.available
	# title = Available RAM for applications
	# dim MemAvailable name = avail
	# dim MemAvailable algorithm = absolute
	# dim MemAvailable multiplier = 1
	# dim MemAvailable divisor = 1024

[apps.swap]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.swap
	# chart type = stacked
	# type = apps
	# family = swap
	# units = MiB
	# context = apps.swap
	# priority = 20011
	# name = apps.swap
	# title = Apps Swap Memory
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1024
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1024
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1024
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1024
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1024
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1024
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1024
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1024
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1024
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1024
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1024
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1024
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1024
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1024
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1024
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1024
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1024
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1024
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1024
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1024
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1024

[system.swap]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.swap
	# chart type = stacked
	# type = system
	# family = swap
	# units = MiB
	# context = system.swap
	# priority = 201
	# name = system.swap
	# title = System Swap
	# dim free name = free
	# dim free algorithm = absolute
	# dim free multiplier = 1
	# dim free divisor = 1024
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1024

[mem.committed]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.committed
	# chart type = area
	# type = mem
	# family = system
	# units = MiB
	# context = mem.committed
	# priority = 1020
	# name = mem.committed
	# title = Committed (Allocated) Memory
	# dim Committed_AS name = Committed_AS
	# dim Committed_AS algorithm = absolute
	# dim Committed_AS multiplier = 1
	# dim Committed_AS divisor = 1024

[mem.writeback]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.writeback
	# chart type = line
	# type = mem
	# family = kernel
	# units = MiB
	# context = mem.writeback
	# priority = 1100
	# name = mem.writeback
	# title = Writeback Memory
	# dim Dirty name = Dirty
	# dim Dirty algorithm = absolute
	# dim Dirty multiplier = 1
	# dim Dirty divisor = 1024
	# dim Writeback name = Writeback
	# dim Writeback algorithm = absolute
	# dim Writeback multiplier = 1
	# dim Writeback divisor = 1024
	# dim FuseWriteback name = FuseWriteback
	# dim FuseWriteback algorithm = absolute
	# dim FuseWriteback multiplier = 1
	# dim FuseWriteback divisor = 1024
	# dim NfsWriteback name = NfsWriteback
	# dim NfsWriteback algorithm = absolute
	# dim NfsWriteback multiplier = 1
	# dim NfsWriteback divisor = 1024
	# dim Bounce name = Bounce
	# dim Bounce algorithm = absolute
	# dim Bounce multiplier = 1
	# dim Bounce divisor = 1024

[mem.kernel]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.kernel
	# chart type = stacked
	# type = mem
	# family = kernel
	# units = MiB
	# context = mem.kernel
	# priority = 1101
	# name = mem.kernel
	# title = Memory Used by Kernel
	# dim Slab name = Slab
	# dim Slab algorithm = absolute
	# dim Slab multiplier = 1
	# dim Slab divisor = 1024
	# dim KernelStack name = KernelStack
	# dim KernelStack algorithm = absolute
	# dim KernelStack multiplier = 1
	# dim KernelStack divisor = 1024
	# dim PageTables name = PageTables
	# dim PageTables algorithm = absolute
	# dim PageTables multiplier = 1
	# dim PageTables divisor = 1024
	# dim VmallocUsed name = VmallocUsed
	# dim VmallocUsed algorithm = absolute
	# dim VmallocUsed multiplier = 1
	# dim VmallocUsed divisor = 1024

[apps.major_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.major_faults
	# chart type = stacked
	# type = apps
	# family = swap
	# units = page faults/s
	# context = apps.major_faults
	# priority = 20012
	# name = apps.major_faults
	# title = Apps Major Page Faults (swap read)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10000

[mem.slab]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.slab
	# chart type = stacked
	# type = mem
	# family = slab
	# units = MiB
	# context = mem.slab
	# priority = 1200
	# name = mem.slab
	# title = Reclaimable Kernel Memory
	# dim reclaimable name = reclaimable
	# dim reclaimable algorithm = absolute
	# dim reclaimable multiplier = 1
	# dim reclaimable divisor = 1024
	# dim unreclaimable name = unreclaimable
	# dim unreclaimable algorithm = absolute
	# dim unreclaimable multiplier = 1
	# dim unreclaimable divisor = 1024

[mem.transparent_hugepages]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.transparent_hugepages
	# chart type = stacked
	# type = mem
	# family = hugepages
	# units = MiB
	# context = mem.transparent_hugepages
	# priority = 1250
	# name = mem.transparent_hugepages
	# title = Transparent HugePages Memory
	# dim anonymous name = anonymous
	# dim anonymous algorithm = absolute
	# dim anonymous multiplier = 1
	# dim anonymous divisor = 1024
	# dim shmem name = shmem
	# dim shmem algorithm = absolute
	# dim shmem multiplier = 1
	# dim shmem divisor = 1024

[mem.node3]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.node3
	# chart type = line
	# type = mem
	# family = numa
	# units = events/s
	# context = mem.node3
	# priority = 1410
	# name = mem.node3
	# title = NUMA events
	# dim numa_hit name = hit
	# dim numa_hit algorithm = incremental
	# dim numa_hit multiplier = 1
	# dim numa_hit divisor = 1
	# dim numa_miss name = miss
	# dim numa_miss algorithm = incremental
	# dim numa_miss multiplier = 1
	# dim numa_miss divisor = 1
	# dim local_node name = local
	# dim local_node algorithm = incremental
	# dim local_node multiplier = 1
	# dim local_node divisor = 1
	# dim numa_foreign name = foreign
	# dim numa_foreign algorithm = incremental
	# dim numa_foreign multiplier = 1
	# dim numa_foreign divisor = 1
	# dim interleave_hit name = interleave
	# dim interleave_hit algorithm = incremental
	# dim interleave_hit multiplier = 1
	# dim interleave_hit divisor = 1
	# dim other_node name = other
	# dim other_node algorithm = incremental
	# dim other_node multiplier = 1
	# dim other_node divisor = 1

[apps.minor_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.minor_faults
	# chart type = stacked
	# type = apps
	# family = mem
	# units = page faults/s
	# context = apps.minor_faults
	# priority = 20011
	# name = apps.minor_faults
	# title = Apps Minor Page Faults
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10000

[mem.node2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.node2
	# chart type = line
	# type = mem
	# family = numa
	# units = events/s
	# context = mem.node2
	# priority = 1410
	# name = mem.node2
	# title = NUMA events
	# dim numa_hit name = hit
	# dim numa_hit algorithm = incremental
	# dim numa_hit multiplier = 1
	# dim numa_hit divisor = 1
	# dim numa_miss name = miss
	# dim numa_miss algorithm = incremental
	# dim numa_miss multiplier = 1
	# dim numa_miss divisor = 1
	# dim local_node name = local
	# dim local_node algorithm = incremental
	# dim local_node multiplier = 1
	# dim local_node divisor = 1
	# dim numa_foreign name = foreign
	# dim numa_foreign algorithm = incremental
	# dim numa_foreign multiplier = 1
	# dim numa_foreign divisor = 1
	# dim interleave_hit name = interleave
	# dim interleave_hit algorithm = incremental
	# dim interleave_hit multiplier = 1
	# dim interleave_hit divisor = 1
	# dim other_node name = other
	# dim other_node algorithm = incremental
	# dim other_node multiplier = 1
	# dim other_node divisor = 1

[mem.node1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.node1
	# chart type = line
	# type = mem
	# family = numa
	# units = events/s
	# context = mem.node1
	# priority = 1410
	# name = mem.node1
	# title = NUMA events
	# dim numa_hit name = hit
	# dim numa_hit algorithm = incremental
	# dim numa_hit multiplier = 1
	# dim numa_hit divisor = 1
	# dim numa_miss name = miss
	# dim numa_miss algorithm = incremental
	# dim numa_miss multiplier = 1
	# dim numa_miss divisor = 1
	# dim local_node name = local
	# dim local_node algorithm = incremental
	# dim local_node multiplier = 1
	# dim local_node divisor = 1
	# dim numa_foreign name = foreign
	# dim numa_foreign algorithm = incremental
	# dim numa_foreign multiplier = 1
	# dim numa_foreign divisor = 1
	# dim interleave_hit name = interleave
	# dim interleave_hit algorithm = incremental
	# dim interleave_hit multiplier = 1
	# dim interleave_hit divisor = 1
	# dim other_node name = other
	# dim other_node algorithm = incremental
	# dim other_node multiplier = 1
	# dim other_node divisor = 1

[mem.node0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/mem.node0
	# chart type = line
	# type = mem
	# family = numa
	# units = events/s
	# context = mem.node0
	# priority = 1410
	# name = mem.node0
	# title = NUMA events
	# dim numa_hit name = hit
	# dim numa_hit algorithm = incremental
	# dim numa_hit multiplier = 1
	# dim numa_hit divisor = 1
	# dim numa_miss name = miss
	# dim numa_miss algorithm = incremental
	# dim numa_miss multiplier = 1
	# dim numa_miss divisor = 1
	# dim local_node name = local
	# dim local_node algorithm = incremental
	# dim local_node multiplier = 1
	# dim local_node divisor = 1
	# dim numa_foreign name = foreign
	# dim numa_foreign algorithm = incremental
	# dim numa_foreign multiplier = 1
	# dim numa_foreign divisor = 1
	# dim interleave_hit name = interleave
	# dim interleave_hit algorithm = incremental
	# dim interleave_hit multiplier = 1
	# dim interleave_hit divisor = 1
	# dim other_node name = other
	# dim other_node algorithm = incremental
	# dim other_node multiplier = 1
	# dim other_node divisor = 1

[apps.preads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.preads
	# chart type = stacked
	# type = apps
	# family = disk
	# units = KiB/s
	# context = apps.preads
	# priority = 20002
	# name = apps.preads
	# title = Apps Disk Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10240000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10240000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10240000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10240000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10240000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10240000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10240000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10240000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10240000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10240000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10240000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10240000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10240000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10240000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10240000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10240000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10240000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10240000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10240000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10240000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10240000

[net.vxlan_sys_4789]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.vxlan_sys_4789
	# chart type = area
	# type = net
	# family = vxlan_sys_4789
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.vxlan_sys_4789
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.vxlan_sys_4789]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.vxlan_sys_4789
	# chart type = line
	# type = net_packets
	# family = vxlan_sys_4789
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.vxlan_sys_4789
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[apps.pwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.pwrites
	# chart type = stacked
	# type = apps
	# family = disk
	# units = KiB/s
	# context = apps.pwrites
	# priority = 20002
	# name = apps.pwrites
	# title = Apps Disk Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10240000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10240000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10240000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10240000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10240000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10240000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10240000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10240000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10240000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10240000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10240000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10240000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10240000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10240000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10240000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10240000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10240000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10240000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10240000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10240000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10240000

[net_errors.br-int]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_errors.br_int
	# chart type = line
	# type = net_errors
	# family = br-int
	# units = errors/s
	# context = net.errors
	# priority = 7002
	# name = net_errors.br_int
	# title = Interface Errors
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net_drops.br-int]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.br_int
	# chart type = line
	# type = net_drops
	# family = br-int
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.br_int
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.bond0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.bond0
	# chart type = area
	# type = net
	# family = bond0
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.bond0
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.bond0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.bond0
	# chart type = line
	# type = net_packets
	# family = bond0
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.bond0
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[apps.lreads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.lreads
	# chart type = stacked
	# type = apps
	# family = disk
	# units = KiB/s
	# context = apps.lreads
	# priority = 20042
	# name = apps.lreads
	# title = Apps Disk Logical Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10240000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10240000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10240000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10240000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10240000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10240000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10240000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10240000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10240000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10240000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10240000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10240000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10240000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10240000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10240000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10240000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10240000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10240000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10240000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10240000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10240000

[net.eno49]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.eno49
	# chart type = area
	# type = net
	# family = eno49
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.eno49
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.eno49]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.eno49
	# chart type = line
	# type = net_packets
	# family = eno49
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.eno49
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net.br-ex]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.br_ex
	# chart type = area
	# type = net
	# family = br-ex
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.br_ex
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.br-ex]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.br_ex
	# chart type = line
	# type = net_packets
	# family = br-ex
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.br_ex
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[apps.lwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.lwrites
	# chart type = stacked
	# type = apps
	# family = disk
	# units = KiB/s
	# context = apps.lwrites
	# priority = 20042
	# name = apps.lwrites
	# title = Apps I/O Logical Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 10240000
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 10240000
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 10240000
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 10240000
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 10240000
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 10240000
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 10240000
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 10240000
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 10240000
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 10240000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 10240000
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 10240000
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 10240000
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 10240000
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 10240000
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 10240000
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 10240000
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 10240000
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 10240000
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 10240000
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 10240000

[net_drops.br-ex]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.br_ex
	# chart type = line
	# type = net_drops
	# family = br-ex
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.br_ex
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[system.net]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.net
	# chart type = area
	# type = system
	# family = network
	# units = kilobits/s
	# context = system.net
	# priority = 500
	# name = system.net
	# title = Physical Network Interfaces Aggregated Bandwidth
	# dim InOctets name = received
	# dim InOctets algorithm = incremental
	# dim InOctets multiplier = 8
	# dim InOctets divisor = 1000
	# dim OutOctets name = sent
	# dim OutOctets algorithm = incremental
	# dim OutOctets multiplier = -8
	# dim OutOctets divisor = 1000

[apps.files]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.files
	# chart type = stacked
	# type = apps
	# family = disk
	# units = open files
	# context = apps.files
	# priority = 20050
	# name = apps.files
	# title = Apps Open Files
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[ipv4.sockstat_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.sockstat_sockets
	# chart type = line
	# type = ipv4
	# family = sockets
	# units = sockets
	# context = ipv4.sockstat_sockets
	# priority = 5100
	# name = ipv4.sockstat_sockets
	# title = IPv4 Sockets Used
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1

[ipv4.sockstat_tcp_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.sockstat_tcp_sockets
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = sockets
	# context = ipv4.sockstat_tcp_sockets
	# priority = 5201
	# name = ipv4.sockstat_tcp_sockets
	# title = IPv4 TCP Sockets
	# dim alloc name = alloc
	# dim alloc algorithm = absolute
	# dim alloc multiplier = 1
	# dim alloc divisor = 1
	# dim orphan name = orphan
	# dim orphan algorithm = absolute
	# dim orphan multiplier = 1
	# dim orphan divisor = 1
	# dim inuse name = inuse
	# dim inuse algorithm = absolute
	# dim inuse multiplier = 1
	# dim inuse divisor = 1
	# dim timewait name = timewait
	# dim timewait algorithm = absolute
	# dim timewait multiplier = 1
	# dim timewait divisor = 1

[ipv4.sockstat_tcp_mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.sockstat_tcp_mem
	# chart type = area
	# type = ipv4
	# family = tcp
	# units = KiB
	# context = ipv4.sockstat_tcp_mem
	# priority = 5290
	# name = ipv4.sockstat_tcp_mem
	# title = IPv4 TCP Sockets Memory
	# dim mem name = mem
	# dim mem algorithm = absolute
	# dim mem multiplier = 4096
	# dim mem divisor = 1024

[ipv4.sockstat_udp_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.sockstat_udp_sockets
	# chart type = line
	# type = ipv4
	# family = udp
	# units = sockets
	# context = ipv4.sockstat_udp_sockets
	# priority = 5300
	# name = ipv4.sockstat_udp_sockets
	# title = IPv4 UDP Sockets
	# dim inuse name = inuse
	# dim inuse algorithm = absolute
	# dim inuse multiplier = 1
	# dim inuse divisor = 1

[ipv4.sockstat_udp_mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.sockstat_udp_mem
	# chart type = area
	# type = ipv4
	# family = udp
	# units = KiB
	# context = ipv4.sockstat_udp_mem
	# priority = 5390
	# name = ipv4.sockstat_udp_mem
	# title = IPv4 UDP Sockets Memory
	# dim mem name = mem
	# dim mem algorithm = absolute
	# dim mem multiplier = 4096
	# dim mem divisor = 1024

[apps.sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.sockets
	# chart type = stacked
	# type = apps
	# family = net
	# units = open sockets
	# context = apps.sockets
	# priority = 20051
	# name = apps.sockets
	# title = Apps Open Sockets
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[ipv6.sockstat6_tcp_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.sockstat6_tcp_sockets
	# chart type = line
	# type = ipv6
	# family = tcp6
	# units = sockets
	# context = ipv6.sockstat6_tcp_sockets
	# priority = 6500
	# name = ipv6.sockstat6_tcp_sockets
	# title = IPv6 TCP Sockets
	# dim inuse name = inuse
	# dim inuse algorithm = absolute
	# dim inuse multiplier = 1
	# dim inuse divisor = 1

[ipv6.sockstat6_udp_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.sockstat6_udp_sockets
	# chart type = line
	# type = ipv6
	# family = udp6
	# units = sockets
	# context = ipv6.sockstat6_udp_sockets
	# priority = 6600
	# name = ipv6.sockstat6_udp_sockets
	# title = IPv6 UDP Sockets
	# dim inuse name = inuse
	# dim inuse algorithm = absolute
	# dim inuse multiplier = 1
	# dim inuse divisor = 1

[ip.tcpconnaborts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.tcpconnaborts
	# chart type = line
	# type = ip
	# family = tcp
	# units = connections/s
	# context = ip.tcpconnaborts
	# priority = 4210
	# name = ip.tcpconnaborts
	# title = TCP Connection Aborts
	# dim TCPAbortOnData name = baddata
	# dim TCPAbortOnData algorithm = incremental
	# dim TCPAbortOnData multiplier = 1
	# dim TCPAbortOnData divisor = 1
	# dim TCPAbortOnClose name = userclosed
	# dim TCPAbortOnClose algorithm = incremental
	# dim TCPAbortOnClose multiplier = 1
	# dim TCPAbortOnClose divisor = 1
	# dim TCPAbortOnMemory name = nomemory
	# dim TCPAbortOnMemory algorithm = incremental
	# dim TCPAbortOnMemory multiplier = 1
	# dim TCPAbortOnMemory divisor = 1
	# dim TCPAbortOnTimeout name = timeout
	# dim TCPAbortOnTimeout algorithm = incremental
	# dim TCPAbortOnTimeout multiplier = 1
	# dim TCPAbortOnTimeout divisor = 1
	# dim TCPAbortOnLinger name = linger
	# dim TCPAbortOnLinger algorithm = incremental
	# dim TCPAbortOnLinger multiplier = 1
	# dim TCPAbortOnLinger divisor = 1
	# dim TCPAbortFailed name = failed
	# dim TCPAbortFailed algorithm = incremental
	# dim TCPAbortFailed multiplier = -1
	# dim TCPAbortFailed divisor = 1

[apps.pipes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.pipes
	# chart type = stacked
	# type = apps
	# family = processes
	# units = open pipes
	# context = apps.pipes
	# priority = 20053
	# name = apps.pipes
	# title = Apps Pipes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[ip.tcpofo]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.tcpofo
	# chart type = line
	# type = ip
	# family = tcp
	# units = packets/s
	# context = ip.tcpofo
	# priority = 4250
	# name = ip.tcpofo
	# title = TCP Out-Of-Order Queue
	# dim TCPOFOQueue name = inqueue
	# dim TCPOFOQueue algorithm = incremental
	# dim TCPOFOQueue multiplier = 1
	# dim TCPOFOQueue divisor = 1
	# dim TCPOFODrop name = dropped
	# dim TCPOFODrop algorithm = incremental
	# dim TCPOFODrop multiplier = -1
	# dim TCPOFODrop divisor = 1
	# dim TCPOFOMerge name = merged
	# dim TCPOFOMerge algorithm = incremental
	# dim TCPOFOMerge multiplier = 1
	# dim TCPOFOMerge divisor = 1
	# dim OfoPruned name = pruned
	# dim OfoPruned algorithm = incremental
	# dim OfoPruned multiplier = -1
	# dim OfoPruned divisor = 1

[ip.tcpsyncookies]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.tcpsyncookies
	# chart type = line
	# type = ip
	# family = tcp
	# units = packets/s
	# context = ip.tcpsyncookies
	# priority = 4260
	# name = ip.tcpsyncookies
	# title = TCP SYN Cookies
	# dim SyncookiesRecv name = received
	# dim SyncookiesRecv algorithm = incremental
	# dim SyncookiesRecv multiplier = 1
	# dim SyncookiesRecv divisor = 1
	# dim SyncookiesSent name = sent
	# dim SyncookiesSent algorithm = incremental
	# dim SyncookiesSent multiplier = -1
	# dim SyncookiesSent divisor = 1
	# dim SyncookiesFailed name = failed
	# dim SyncookiesFailed algorithm = incremental
	# dim SyncookiesFailed multiplier = -1
	# dim SyncookiesFailed divisor = 1

[ip.tcp_accept_queue]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.tcp_accept_queue
	# chart type = line
	# type = ip
	# family = tcp
	# units = packets/s
	# context = ip.tcp_accept_queue
	# priority = 4216
	# name = ip.tcp_accept_queue
	# title = TCP Accept Queue Issues
	# dim ListenOverflows name = overflows
	# dim ListenOverflows algorithm = incremental
	# dim ListenOverflows multiplier = 1
	# dim ListenOverflows divisor = 1
	# dim ListenDrops name = drops
	# dim ListenDrops algorithm = incremental
	# dim ListenDrops multiplier = 1
	# dim ListenDrops divisor = 1

[system.ip]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ip
	# chart type = area
	# type = system
	# family = network
	# units = kilobits/s
	# context = system.ip
	# priority = 501
	# name = system.ip
	# title = IP Bandwidth
	# dim InOctets name = received
	# dim InOctets algorithm = incremental
	# dim InOctets multiplier = 8
	# dim InOctets divisor = 1000
	# dim OutOctets name = sent
	# dim OutOctets algorithm = incremental
	# dim OutOctets multiplier = -8
	# dim OutOctets divisor = 1000

[users.cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu
	# chart type = stacked
	# type = users
	# family = cpu
	# units = percentage
	# context = users.cpu
	# priority = 20001
	# name = users.cpu
	# title = Users CPU Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[ip.mcast]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.mcast
	# chart type = area
	# type = ip
	# family = multicast
	# units = kilobits/s
	# context = ip.mcast
	# priority = 4600
	# name = ip.mcast
	# title = IP Multicast Bandwidth
	# dim InMcastOctets name = received
	# dim InMcastOctets algorithm = incremental
	# dim InMcastOctets multiplier = 8
	# dim InMcastOctets divisor = 1000
	# dim OutMcastOctets name = sent
	# dim OutMcastOctets algorithm = incremental
	# dim OutMcastOctets multiplier = -8
	# dim OutMcastOctets divisor = 1000

[ip.bcast]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.bcast
	# chart type = area
	# type = ip
	# family = broadcast
	# units = kilobits/s
	# context = ip.bcast
	# priority = 4500
	# name = ip.bcast
	# title = IP Broadcast Bandwidth
	# dim InBcastOctets name = received
	# dim InBcastOctets algorithm = incremental
	# dim InBcastOctets multiplier = 8
	# dim InBcastOctets divisor = 1000
	# dim OutBcastOctets name = sent
	# dim OutBcastOctets algorithm = incremental
	# dim OutBcastOctets multiplier = -8
	# dim OutBcastOctets divisor = 1000

[ip.mcastpkts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.mcastpkts
	# chart type = line
	# type = ip
	# family = multicast
	# units = packets/s
	# context = ip.mcastpkts
	# priority = 4610
	# name = ip.mcastpkts
	# title = IP Multicast Packets
	# dim InMcastPkts name = received
	# dim InMcastPkts algorithm = incremental
	# dim InMcastPkts multiplier = 1
	# dim InMcastPkts divisor = 1
	# dim OutMcastPkts name = sent
	# dim OutMcastPkts algorithm = incremental
	# dim OutMcastPkts multiplier = -1
	# dim OutMcastPkts divisor = 1

[ip.bcastpkts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.bcastpkts
	# chart type = line
	# type = ip
	# family = broadcast
	# units = packets/s
	# context = ip.bcastpkts
	# priority = 4510
	# name = ip.bcastpkts
	# title = IP Broadcast Packets
	# dim InBcastPkts name = received
	# dim InBcastPkts algorithm = incremental
	# dim InBcastPkts multiplier = 1
	# dim InBcastPkts divisor = 1
	# dim OutBcastPkts name = sent
	# dim OutBcastPkts algorithm = incremental
	# dim OutBcastPkts multiplier = -1
	# dim OutBcastPkts divisor = 1

[users.mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.mem
	# chart type = stacked
	# type = users
	# family = mem
	# units = MiB
	# context = users.mem
	# priority = 20003
	# name = users.mem
	# title = Users Real Memory (w/o shared)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[ip.ecnpkts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.ecnpkts
	# chart type = line
	# type = ip
	# family = ecn
	# units = packets/s
	# context = ip.ecnpkts
	# priority = 4700
	# name = ip.ecnpkts
	# title = IP ECN Statistics
	# dim InCEPkts name = CEP
	# dim InCEPkts algorithm = incremental
	# dim InCEPkts multiplier = 1
	# dim InCEPkts divisor = 1
	# dim InNoECTPkts name = NoECTP
	# dim InNoECTPkts algorithm = incremental
	# dim InNoECTPkts multiplier = -1
	# dim InNoECTPkts divisor = 1
	# dim InECT0Pkts name = ECTP0
	# dim InECT0Pkts algorithm = incremental
	# dim InECT0Pkts multiplier = 1
	# dim InECT0Pkts divisor = 1
	# dim InECT1Pkts name = ECTP1
	# dim InECT1Pkts algorithm = incremental
	# dim InECT1Pkts multiplier = 1
	# dim InECT1Pkts divisor = 1

[ipv4.packets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.packets
	# chart type = line
	# type = ipv4
	# family = packets
	# units = packets/s
	# context = ipv4.packets
	# priority = 5130
	# name = ipv4.packets
	# title = IPv4 Packets
	# dim InReceives name = received
	# dim InReceives algorithm = incremental
	# dim InReceives multiplier = 1
	# dim InReceives divisor = 1
	# dim OutRequests name = sent
	# dim OutRequests algorithm = incremental
	# dim OutRequests multiplier = -1
	# dim OutRequests divisor = 1
	# dim ForwDatagrams name = forwarded
	# dim ForwDatagrams algorithm = incremental
	# dim ForwDatagrams multiplier = 1
	# dim ForwDatagrams divisor = 1
	# dim InDelivers name = delivered
	# dim InDelivers algorithm = incremental
	# dim InDelivers multiplier = 1
	# dim InDelivers divisor = 1

[ipv4.errors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.errors
	# chart type = line
	# type = ipv4
	# family = errors
	# units = packets/s
	# context = ipv4.errors
	# priority = 5150
	# name = ipv4.errors
	# title = IPv4 Errors
	# dim InDiscards name = InDiscards
	# dim InDiscards algorithm = incremental
	# dim InDiscards multiplier = 1
	# dim InDiscards divisor = 1
	# dim OutDiscards name = OutDiscards
	# dim OutDiscards algorithm = incremental
	# dim OutDiscards multiplier = -1
	# dim OutDiscards divisor = 1
	# dim InHdrErrors name = InHdrErrors
	# dim InHdrErrors algorithm = incremental
	# dim InHdrErrors multiplier = 1
	# dim InHdrErrors divisor = 1
	# dim OutNoRoutes name = OutNoRoutes
	# dim OutNoRoutes algorithm = incremental
	# dim OutNoRoutes multiplier = -1
	# dim OutNoRoutes divisor = 1
	# dim InAddrErrors name = InAddrErrors
	# dim InAddrErrors algorithm = incremental
	# dim InAddrErrors multiplier = 1
	# dim InAddrErrors divisor = 1
	# dim InUnknownProtos name = InUnknownProtos
	# dim InUnknownProtos algorithm = incremental
	# dim InUnknownProtos multiplier = 1
	# dim InUnknownProtos divisor = 1

[ipv4.icmp]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.icmp
	# chart type = line
	# type = ipv4
	# family = icmp
	# units = packets/s
	# context = ipv4.icmp
	# priority = 5170
	# name = ipv4.icmp
	# title = IPv4 ICMP Packets
	# dim InMsgs name = received
	# dim InMsgs algorithm = incremental
	# dim InMsgs multiplier = 1
	# dim InMsgs divisor = 1
	# dim OutMsgs name = sent
	# dim OutMsgs algorithm = incremental
	# dim OutMsgs multiplier = -1
	# dim OutMsgs divisor = 1

[users.vmem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.vmem
	# chart type = stacked
	# type = users
	# family = mem
	# units = MiB
	# context = users.vmem
	# priority = 20005
	# name = users.vmem
	# title = Users Virtual Memory Size
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[ipv4.icmp_errors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.icmp_errors
	# chart type = line
	# type = ipv4
	# family = icmp
	# units = packets/s
	# context = ipv4.icmp_errors
	# priority = 5171
	# name = ipv4.icmp_errors
	# title = IPv4 ICMP Errors
	# dim InErrors name = InErrors
	# dim InErrors algorithm = incremental
	# dim InErrors multiplier = 1
	# dim InErrors divisor = 1
	# dim OutErrors name = OutErrors
	# dim OutErrors algorithm = incremental
	# dim OutErrors multiplier = -1
	# dim OutErrors divisor = 1
	# dim InCsumErrors name = InCsumErrors
	# dim InCsumErrors algorithm = incremental
	# dim InCsumErrors multiplier = 1
	# dim InCsumErrors divisor = 1

[ipv4.icmpmsg]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.icmpmsg
	# chart type = line
	# type = ipv4
	# family = icmp
	# units = packets/s
	# context = ipv4.icmpmsg
	# priority = 5172
	# name = ipv4.icmpmsg
	# title = IPv4 ICMP Messages
	# dim InType0 name = InEchoReps
	# dim InType0 algorithm = incremental
	# dim InType0 multiplier = 1
	# dim InType0 divisor = 1
	# dim OutType0 name = OutEchoReps
	# dim OutType0 algorithm = incremental
	# dim OutType0 multiplier = -1
	# dim OutType0 divisor = 1
	# dim InType3 name = InDestUnreachs
	# dim InType3 algorithm = incremental
	# dim InType3 multiplier = 1
	# dim InType3 divisor = 1
	# dim OutType3 name = OutDestUnreachs
	# dim OutType3 algorithm = incremental
	# dim OutType3 multiplier = -1
	# dim OutType3 divisor = 1
	# dim InType5 name = InRedirects
	# dim InType5 algorithm = incremental
	# dim InType5 multiplier = 1
	# dim InType5 divisor = 1
	# dim OutType5 name = OutRedirects
	# dim OutType5 algorithm = incremental
	# dim OutType5 multiplier = -1
	# dim OutType5 divisor = 1
	# dim InType8 name = InEchos
	# dim InType8 algorithm = incremental
	# dim InType8 multiplier = 1
	# dim InType8 divisor = 1
	# dim OutType8 name = OutEchos
	# dim OutType8 algorithm = incremental
	# dim OutType8 multiplier = -1
	# dim OutType8 divisor = 1
	# dim InType9 name = InRouterAdvert
	# dim InType9 algorithm = incremental
	# dim InType9 multiplier = 1
	# dim InType9 divisor = 1
	# dim OutType9 name = OutRouterAdvert
	# dim OutType9 algorithm = incremental
	# dim OutType9 multiplier = -1
	# dim OutType9 divisor = 1
	# dim InType10 name = InRouterSelect
	# dim InType10 algorithm = incremental
	# dim InType10 multiplier = 1
	# dim InType10 divisor = 1
	# dim OutType10 name = OutRouterSelect
	# dim OutType10 algorithm = incremental
	# dim OutType10 multiplier = -1
	# dim OutType10 divisor = 1
	# dim InType11 name = InTimeExcds
	# dim InType11 algorithm = incremental
	# dim InType11 multiplier = 1
	# dim InType11 divisor = 1
	# dim OutType11 name = OutTimeExcds
	# dim OutType11 algorithm = incremental
	# dim OutType11 multiplier = -1
	# dim OutType11 divisor = 1
	# dim InType12 name = InParmProbs
	# dim InType12 algorithm = incremental
	# dim InType12 multiplier = 1
	# dim InType12 divisor = 1
	# dim OutType12 name = OutParmProbs
	# dim OutType12 algorithm = incremental
	# dim OutType12 multiplier = -1
	# dim OutType12 divisor = 1
	# dim InType13 name = InTimestamps
	# dim InType13 algorithm = incremental
	# dim InType13 multiplier = 1
	# dim InType13 divisor = 1
	# dim OutType13 name = OutTimestamps
	# dim OutType13 algorithm = incremental
	# dim OutType13 multiplier = -1
	# dim OutType13 divisor = 1
	# dim InType14 name = InTimestampReps
	# dim InType14 algorithm = incremental
	# dim InType14 multiplier = 1
	# dim InType14 divisor = 1
	# dim OutType14 name = OutTimestampReps
	# dim OutType14 algorithm = incremental
	# dim OutType14 multiplier = -1
	# dim OutType14 divisor = 1

[users.threads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.threads
	# chart type = stacked
	# type = users
	# family = processes
	# units = threads
	# context = users.threads
	# priority = 20006
	# name = users.threads
	# title = Users Threads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[ipv4.tcpsock]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcpsock
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = active connections
	# context = ipv4.tcpsock
	# priority = 5200
	# name = ipv4.tcpsock
	# title = IPv4 TCP Connections
	# dim CurrEstab name = connections
	# dim CurrEstab algorithm = absolute
	# dim CurrEstab multiplier = 1
	# dim CurrEstab divisor = 1

[ipv4.tcppackets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcppackets
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = packets/s
	# context = ipv4.tcppackets
	# priority = 5204
	# name = ipv4.tcppackets
	# title = IPv4 TCP Packets
	# dim InSegs name = received
	# dim InSegs algorithm = incremental
	# dim InSegs multiplier = 1
	# dim InSegs divisor = 1
	# dim OutSegs name = sent
	# dim OutSegs algorithm = incremental
	# dim OutSegs multiplier = -1
	# dim OutSegs divisor = 1

[ipv4.tcperrors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcperrors
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = packets/s
	# context = ipv4.tcperrors
	# priority = 5220
	# name = ipv4.tcperrors
	# title = IPv4 TCP Errors
	# dim InErrs name = InErrs
	# dim InErrs algorithm = incremental
	# dim InErrs multiplier = 1
	# dim InErrs divisor = 1
	# dim InCsumErrors name = InCsumErrors
	# dim InCsumErrors algorithm = incremental
	# dim InCsumErrors multiplier = 1
	# dim InCsumErrors divisor = 1
	# dim RetransSegs name = RetransSegs
	# dim RetransSegs algorithm = incremental
	# dim RetransSegs multiplier = -1
	# dim RetransSegs divisor = 1

[ipv4.tcpopens]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcpopens
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = connections/s
	# context = ipv4.tcpopens
	# priority = 5205
	# name = ipv4.tcpopens
	# title = IPv4 TCP Opens
	# dim ActiveOpens name = active
	# dim ActiveOpens algorithm = incremental
	# dim ActiveOpens multiplier = 1
	# dim ActiveOpens divisor = 1
	# dim PassiveOpens name = passive
	# dim PassiveOpens algorithm = incremental
	# dim PassiveOpens multiplier = 1
	# dim PassiveOpens divisor = 1

[ipv4.tcphandshake]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.tcphandshake
	# chart type = line
	# type = ipv4
	# family = tcp
	# units = events/s
	# context = ipv4.tcphandshake
	# priority = 5230
	# name = ipv4.tcphandshake
	# title = IPv4 TCP Handshake Issues
	# dim EstabResets name = EstabResets
	# dim EstabResets algorithm = incremental
	# dim EstabResets multiplier = 1
	# dim EstabResets divisor = 1
	# dim OutRsts name = OutRsts
	# dim OutRsts algorithm = incremental
	# dim OutRsts multiplier = 1
	# dim OutRsts divisor = 1
	# dim AttemptFails name = AttemptFails
	# dim AttemptFails algorithm = incremental
	# dim AttemptFails multiplier = 1
	# dim AttemptFails divisor = 1
	# dim TCPSynRetrans name = SynRetrans
	# dim TCPSynRetrans algorithm = incremental
	# dim TCPSynRetrans multiplier = 1
	# dim TCPSynRetrans divisor = 1

[users.processes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.processes
	# chart type = stacked
	# type = users
	# family = processes
	# units = processes
	# context = users.processes
	# priority = 20007
	# name = users.processes
	# title = Users Processes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[ipv4.udppackets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.udppackets
	# chart type = line
	# type = ipv4
	# family = udp
	# units = packets/s
	# context = ipv4.udppackets
	# priority = 5300
	# name = ipv4.udppackets
	# title = IPv4 UDP Packets
	# dim InDatagrams name = received
	# dim InDatagrams algorithm = incremental
	# dim InDatagrams multiplier = 1
	# dim InDatagrams divisor = 1
	# dim OutDatagrams name = sent
	# dim OutDatagrams algorithm = incremental
	# dim OutDatagrams multiplier = -1
	# dim OutDatagrams divisor = 1

[system.ipv6]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ipv6
	# chart type = area
	# type = system
	# family = network
	# units = kilobits/s
	# context = system.ipv6
	# priority = 502
	# name = system.ipv6
	# title = IPv6 Bandwidth
	# dim InOctets name = received
	# dim InOctets algorithm = incremental
	# dim InOctets multiplier = 8
	# dim InOctets divisor = 1000
	# dim OutOctets name = sent
	# dim OutOctets algorithm = incremental
	# dim OutOctets multiplier = -8
	# dim OutOctets divisor = 1000

[ipv6.packets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.packets
	# chart type = line
	# type = ipv6
	# family = packets
	# units = packets/s
	# context = ipv6.packets
	# priority = 6200
	# name = ipv6.packets
	# title = IPv6 Packets
	# dim InReceives name = received
	# dim InReceives algorithm = incremental
	# dim InReceives multiplier = 1
	# dim InReceives divisor = 1
	# dim OutRequests name = sent
	# dim OutRequests algorithm = incremental
	# dim OutRequests multiplier = -1
	# dim OutRequests divisor = 1
	# dim OutForwDatagrams name = forwarded
	# dim OutForwDatagrams algorithm = incremental
	# dim OutForwDatagrams multiplier = -1
	# dim OutForwDatagrams divisor = 1
	# dim InDelivers name = delivers
	# dim InDelivers algorithm = incremental
	# dim InDelivers multiplier = 1
	# dim InDelivers divisor = 1

[ipv6.errors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.errors
	# chart type = line
	# type = ipv6
	# family = errors
	# units = packets/s
	# context = ipv6.errors
	# priority = 6300
	# name = ipv6.errors
	# title = IPv6 Errors
	# dim InDiscards name = InDiscards
	# dim InDiscards algorithm = incremental
	# dim InDiscards multiplier = 1
	# dim InDiscards divisor = 1
	# dim OutDiscards name = OutDiscards
	# dim OutDiscards algorithm = incremental
	# dim OutDiscards multiplier = -1
	# dim OutDiscards divisor = 1
	# dim InHdrErrors name = InHdrErrors
	# dim InHdrErrors algorithm = incremental
	# dim InHdrErrors multiplier = 1
	# dim InHdrErrors divisor = 1
	# dim InAddrErrors name = InAddrErrors
	# dim InAddrErrors algorithm = incremental
	# dim InAddrErrors multiplier = 1
	# dim InAddrErrors divisor = 1
	# dim InUnknownProtos name = InUnknownProtos
	# dim InUnknownProtos algorithm = incremental
	# dim InUnknownProtos multiplier = 1
	# dim InUnknownProtos divisor = 1
	# dim InTooBigErrors name = InTooBigErrors
	# dim InTooBigErrors algorithm = incremental
	# dim InTooBigErrors multiplier = 1
	# dim InTooBigErrors divisor = 1
	# dim InTruncatedPkts name = InTruncatedPkts
	# dim InTruncatedPkts algorithm = incremental
	# dim InTruncatedPkts multiplier = 1
	# dim InTruncatedPkts divisor = 1
	# dim InNoRoutes name = InNoRoutes
	# dim InNoRoutes algorithm = incremental
	# dim InNoRoutes multiplier = 1
	# dim InNoRoutes divisor = 1
	# dim OutNoRoutes name = OutNoRoutes
	# dim OutNoRoutes algorithm = incremental
	# dim OutNoRoutes multiplier = -1
	# dim OutNoRoutes divisor = 1

[users.cpu_user]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu_user
	# chart type = stacked
	# type = users
	# family = cpu
	# units = percentage
	# context = users.cpu_user
	# priority = 20020
	# name = users.cpu_user
	# title = Users CPU User Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[ipv6.mcast]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.mcast
	# chart type = area
	# type = ipv6
	# family = multicast6
	# units = kilobits/s
	# context = ipv6.mcast
	# priority = 6850
	# name = ipv6.mcast
	# title = IPv6 Multicast Bandwidth
	# dim InMcastOctets name = received
	# dim InMcastOctets algorithm = incremental
	# dim InMcastOctets multiplier = 8
	# dim InMcastOctets divisor = 1000
	# dim OutMcastOctets name = sent
	# dim OutMcastOctets algorithm = incremental
	# dim OutMcastOctets multiplier = -8
	# dim OutMcastOctets divisor = 1000

[ipv6.mcastpkts]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.mcastpkts
	# chart type = line
	# type = ipv6
	# family = multicast6
	# units = packets/s
	# context = ipv6.mcastpkts
	# priority = 6851
	# name = ipv6.mcastpkts
	# title = IPv6 Multicast Packets
	# dim InMcastPkts name = received
	# dim InMcastPkts algorithm = incremental
	# dim InMcastPkts multiplier = 1
	# dim InMcastPkts divisor = 1
	# dim OutMcastPkts name = sent
	# dim OutMcastPkts algorithm = incremental
	# dim OutMcastPkts multiplier = -1
	# dim OutMcastPkts divisor = 1

[ipv6.icmp]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmp
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.icmp
	# priority = 6900
	# name = ipv6.icmp
	# title = IPv6 ICMP Messages
	# dim InMsgs name = received
	# dim InMsgs algorithm = incremental
	# dim InMsgs multiplier = 1
	# dim InMsgs divisor = 1
	# dim OutMsgs name = sent
	# dim OutMsgs algorithm = incremental
	# dim OutMsgs multiplier = -1
	# dim OutMsgs divisor = 1

[ipv6.groupmemb]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.groupmemb
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.groupmemb
	# priority = 6940
	# name = ipv6.groupmemb
	# title = IPv6 ICMP Group Membership
	# dim InQueries name = InQueries
	# dim InQueries algorithm = incremental
	# dim InQueries multiplier = 1
	# dim InQueries divisor = 1
	# dim OutQueries name = OutQueries
	# dim OutQueries algorithm = incremental
	# dim OutQueries multiplier = -1
	# dim OutQueries divisor = 1
	# dim InResponses name = InResponses
	# dim InResponses algorithm = incremental
	# dim InResponses multiplier = 1
	# dim InResponses divisor = 1
	# dim OutResponses name = OutResponses
	# dim OutResponses algorithm = incremental
	# dim OutResponses multiplier = -1
	# dim OutResponses divisor = 1
	# dim InReductions name = InReductions
	# dim InReductions algorithm = incremental
	# dim InReductions multiplier = 1
	# dim InReductions divisor = 1
	# dim OutReductions name = OutReductions
	# dim OutReductions algorithm = incremental
	# dim OutReductions multiplier = -1
	# dim OutReductions divisor = 1

[users.cpu_system]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.cpu_system
	# chart type = stacked
	# type = users
	# family = cpu
	# units = percentage
	# context = users.cpu_system
	# priority = 20021
	# name = users.cpu_system
	# title = Users CPU System Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[ipv6.icmprouter]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmprouter
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.icmprouter
	# priority = 6950
	# name = ipv6.icmprouter
	# title = IPv6 Router Messages
	# dim InSolicits name = InSolicits
	# dim InSolicits algorithm = incremental
	# dim InSolicits multiplier = 1
	# dim InSolicits divisor = 1
	# dim OutSolicits name = OutSolicits
	# dim OutSolicits algorithm = incremental
	# dim OutSolicits multiplier = -1
	# dim OutSolicits divisor = 1
	# dim InAdvertisements name = InAdvertisements
	# dim InAdvertisements algorithm = incremental
	# dim InAdvertisements multiplier = 1
	# dim InAdvertisements divisor = 1
	# dim OutAdvertisements name = OutAdvertisements
	# dim OutAdvertisements algorithm = incremental
	# dim OutAdvertisements multiplier = -1
	# dim OutAdvertisements divisor = 1

[ipv6.icmpneighbor]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmpneighbor
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.icmpneighbor
	# priority = 6960
	# name = ipv6.icmpneighbor
	# title = IPv6 Neighbor Messages
	# dim InSolicits name = InSolicits
	# dim InSolicits algorithm = incremental
	# dim InSolicits multiplier = 1
	# dim InSolicits divisor = 1
	# dim OutSolicits name = OutSolicits
	# dim OutSolicits algorithm = incremental
	# dim OutSolicits multiplier = -1
	# dim OutSolicits divisor = 1
	# dim InAdvertisements name = InAdvertisements
	# dim InAdvertisements algorithm = incremental
	# dim InAdvertisements multiplier = 1
	# dim InAdvertisements divisor = 1
	# dim OutAdvertisements name = OutAdvertisements
	# dim OutAdvertisements algorithm = incremental
	# dim OutAdvertisements multiplier = -1
	# dim OutAdvertisements divisor = 1

[ipv6.icmpmldv2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmpmldv2
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = reports/s
	# context = ipv6.icmpmldv2
	# priority = 6970
	# name = ipv6.icmpmldv2
	# title = IPv6 ICMP MLDv2 Reports
	# dim InMLDv2Reports name = received
	# dim InMLDv2Reports algorithm = incremental
	# dim InMLDv2Reports multiplier = 1
	# dim InMLDv2Reports divisor = 1
	# dim OutMLDv2Reports name = sent
	# dim OutMLDv2Reports algorithm = incremental
	# dim OutMLDv2Reports multiplier = -1
	# dim OutMLDv2Reports divisor = 1

[ipv6.icmptypes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmptypes
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.icmptypes
	# priority = 6980
	# name = ipv6.icmptypes
	# title = IPv6 ICMP Types
	# dim InType1 name = InType1
	# dim InType1 algorithm = incremental
	# dim InType1 multiplier = 1
	# dim InType1 divisor = 1
	# dim InType128 name = InType128
	# dim InType128 algorithm = incremental
	# dim InType128 multiplier = 1
	# dim InType128 divisor = 1
	# dim InType129 name = InType129
	# dim InType129 algorithm = incremental
	# dim InType129 multiplier = 1
	# dim InType129 divisor = 1
	# dim InType136 name = InType136
	# dim InType136 algorithm = incremental
	# dim InType136 multiplier = 1
	# dim InType136 divisor = 1
	# dim OutType1 name = OutType1
	# dim OutType1 algorithm = incremental
	# dim OutType1 multiplier = -1
	# dim OutType1 divisor = 1
	# dim OutType128 name = OutType128
	# dim OutType128 algorithm = incremental
	# dim OutType128 multiplier = -1
	# dim OutType128 divisor = 1
	# dim OutType129 name = OutType129
	# dim OutType129 algorithm = incremental
	# dim OutType129 multiplier = -1
	# dim OutType129 divisor = 1
	# dim OutType133 name = OutType133
	# dim OutType133 algorithm = incremental
	# dim OutType133 multiplier = -1
	# dim OutType133 divisor = 1
	# dim OutType135 name = OutType135
	# dim OutType135 algorithm = incremental
	# dim OutType135 multiplier = -1
	# dim OutType135 divisor = 1
	# dim OutType143 name = OutType143
	# dim OutType143 algorithm = incremental
	# dim OutType143 multiplier = -1
	# dim OutType143 divisor = 1

[users.swap]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.swap
	# chart type = stacked
	# type = users
	# family = swap
	# units = MiB
	# context = users.swap
	# priority = 20011
	# name = users.swap
	# title = Users Swap Memory
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[ipv6.ect]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.ect
	# chart type = line
	# type = ipv6
	# family = packets
	# units = packets/s
	# context = ipv6.ect
	# priority = 6210
	# name = ipv6.ect
	# title = IPv6 ECT Packets
	# dim InNoECTPkts name = InNoECTPkts
	# dim InNoECTPkts algorithm = incremental
	# dim InNoECTPkts multiplier = 1
	# dim InNoECTPkts divisor = 1
	# dim InECT1Pkts name = InECT1Pkts
	# dim InECT1Pkts algorithm = incremental
	# dim InECT1Pkts multiplier = 1
	# dim InECT1Pkts divisor = 1
	# dim InECT0Pkts name = InECT0Pkts
	# dim InECT0Pkts algorithm = incremental
	# dim InECT0Pkts multiplier = 1
	# dim InECT0Pkts divisor = 1
	# dim InCEPkts name = InCEPkts
	# dim InCEPkts algorithm = incremental
	# dim InCEPkts multiplier = 1
	# dim InCEPkts divisor = 1

[system.softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.softnet_stat
	# chart type = line
	# type = system
	# family = softnet_stat
	# units = events/s
	# context = system.softnet_stat
	# priority = 955
	# name = system.softnet_stat
	# title = System softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu0_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu0_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4101
	# name = cpu.cpu0_softnet_stat
	# title = CPU0 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.major_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.major_faults
	# chart type = stacked
	# type = users
	# family = swap
	# units = page faults/s
	# context = users.major_faults
	# priority = 20012
	# name = users.major_faults
	# title = Users Major Page Faults (swap read)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu1_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu1_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4102
	# name = cpu.cpu1_softnet_stat
	# title = CPU1 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu2_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu2_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4103
	# name = cpu.cpu2_softnet_stat
	# title = CPU2 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu3_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu3_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4104
	# name = cpu.cpu3_softnet_stat
	# title = CPU3 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu4_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu4_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4105
	# name = cpu.cpu4_softnet_stat
	# title = CPU4 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.minor_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.minor_faults
	# chart type = stacked
	# type = users
	# family = mem
	# units = page faults/s
	# context = users.minor_faults
	# priority = 20011
	# name = users.minor_faults
	# title = Users Minor Page Faults
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu5_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu5_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4106
	# name = cpu.cpu5_softnet_stat
	# title = CPU5 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu6_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu6_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4107
	# name = cpu.cpu6_softnet_stat
	# title = CPU6 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu7_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu7_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4108
	# name = cpu.cpu7_softnet_stat
	# title = CPU7 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu8_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu8_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4109
	# name = cpu.cpu8_softnet_stat
	# title = CPU8 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.preads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.preads
	# chart type = stacked
	# type = users
	# family = disk
	# units = KiB/s
	# context = users.preads
	# priority = 20002
	# name = users.preads
	# title = Users Disk Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu9_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu9_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4110
	# name = cpu.cpu9_softnet_stat
	# title = CPU9 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu10_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu10_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4111
	# name = cpu.cpu10_softnet_stat
	# title = CPU10 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu11_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu11_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4112
	# name = cpu.cpu11_softnet_stat
	# title = CPU11 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu12_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu12_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4113
	# name = cpu.cpu12_softnet_stat
	# title = CPU12 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.pwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.pwrites
	# chart type = stacked
	# type = users
	# family = disk
	# units = KiB/s
	# context = users.pwrites
	# priority = 20002
	# name = users.pwrites
	# title = Users Disk Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu13_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu13_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4114
	# name = cpu.cpu13_softnet_stat
	# title = CPU13 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu14_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu14_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4115
	# name = cpu.cpu14_softnet_stat
	# title = CPU14 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu15_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu15_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4116
	# name = cpu.cpu15_softnet_stat
	# title = CPU15 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu16_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu16_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4117
	# name = cpu.cpu16_softnet_stat
	# title = CPU16 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.lreads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.lreads
	# chart type = stacked
	# type = users
	# family = disk
	# units = KiB/s
	# context = users.lreads
	# priority = 20042
	# name = users.lreads
	# title = Users Disk Logical Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu17_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu17_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4118
	# name = cpu.cpu17_softnet_stat
	# title = CPU17 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu18_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu18_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4119
	# name = cpu.cpu18_softnet_stat
	# title = CPU18 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu19_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu19_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4120
	# name = cpu.cpu19_softnet_stat
	# title = CPU19 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu20_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu20_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4121
	# name = cpu.cpu20_softnet_stat
	# title = CPU20 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.lwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.lwrites
	# chart type = stacked
	# type = users
	# family = disk
	# units = KiB/s
	# context = users.lwrites
	# priority = 20042
	# name = users.lwrites
	# title = Users I/O Logical Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu21_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu21_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4122
	# name = cpu.cpu21_softnet_stat
	# title = CPU21 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu22_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu22_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4123
	# name = cpu.cpu22_softnet_stat
	# title = CPU22 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu23_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu23_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4124
	# name = cpu.cpu23_softnet_stat
	# title = CPU23 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu24_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu24_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4125
	# name = cpu.cpu24_softnet_stat
	# title = CPU24 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.files]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.files
	# chart type = stacked
	# type = users
	# family = disk
	# units = open files
	# context = users.files
	# priority = 20050
	# name = users.files
	# title = Users Open Files
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu25_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu25_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4126
	# name = cpu.cpu25_softnet_stat
	# title = CPU25 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu26_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu26_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4127
	# name = cpu.cpu26_softnet_stat
	# title = CPU26 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu27_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu27_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4128
	# name = cpu.cpu27_softnet_stat
	# title = CPU27 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu28_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu28_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4129
	# name = cpu.cpu28_softnet_stat
	# title = CPU28 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.sockets
	# chart type = stacked
	# type = users
	# family = net
	# units = open sockets
	# context = users.sockets
	# priority = 20051
	# name = users.sockets
	# title = Users Open Sockets
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu29_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu29_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4130
	# name = cpu.cpu29_softnet_stat
	# title = CPU29 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu30_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu30_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4131
	# name = cpu.cpu30_softnet_stat
	# title = CPU30 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu31_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu31_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4132
	# name = cpu.cpu31_softnet_stat
	# title = CPU31 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu32_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu32_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4133
	# name = cpu.cpu32_softnet_stat
	# title = CPU32 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[users.pipes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.pipes
	# chart type = stacked
	# type = users
	# family = processes
	# units = open pipes
	# context = users.pipes
	# priority = 20053
	# name = users.pipes
	# title = Users Pipes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu33_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu33_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4134
	# name = cpu.cpu33_softnet_stat
	# title = CPU33 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu34_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu34_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4135
	# name = cpu.cpu34_softnet_stat
	# title = CPU34 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu35_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu35_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4136
	# name = cpu.cpu35_softnet_stat
	# title = CPU35 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu36_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu36_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4137
	# name = cpu.cpu36_softnet_stat
	# title = CPU36 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu
	# chart type = stacked
	# type = groups
	# family = cpu
	# units = percentage
	# context = groups.cpu
	# priority = 20001
	# name = groups.cpu
	# title = User Groups CPU Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu37_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu37_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4138
	# name = cpu.cpu37_softnet_stat
	# title = CPU37 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu38_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu38_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4139
	# name = cpu.cpu38_softnet_stat
	# title = CPU38 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu39_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu39_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4140
	# name = cpu.cpu39_softnet_stat
	# title = CPU39 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.mem
	# chart type = stacked
	# type = groups
	# family = mem
	# units = MiB
	# context = groups.mem
	# priority = 20003
	# name = groups.mem
	# title = User Groups Real Memory (w/o shared)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[cpu.cpu40_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu40_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4141
	# name = cpu.cpu40_softnet_stat
	# title = CPU40 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu41_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu41_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4142
	# name = cpu.cpu41_softnet_stat
	# title = CPU41 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu42_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu42_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4143
	# name = cpu.cpu42_softnet_stat
	# title = CPU42 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu43_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu43_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4144
	# name = cpu.cpu43_softnet_stat
	# title = CPU43 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.vmem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.vmem
	# chart type = stacked
	# type = groups
	# family = mem
	# units = MiB
	# context = groups.vmem
	# priority = 20005
	# name = groups.vmem
	# title = User Groups Virtual Memory Size
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[cpu.cpu44_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu44_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4145
	# name = cpu.cpu44_softnet_stat
	# title = CPU44 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu45_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu45_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4146
	# name = cpu.cpu45_softnet_stat
	# title = CPU45 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu46_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu46_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4147
	# name = cpu.cpu46_softnet_stat
	# title = CPU46 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu47_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu47_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4148
	# name = cpu.cpu47_softnet_stat
	# title = CPU47 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.threads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.threads
	# chart type = stacked
	# type = groups
	# family = processes
	# units = threads
	# context = groups.threads
	# priority = 20006
	# name = groups.threads
	# title = User Groups Threads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu48_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu48_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4149
	# name = cpu.cpu48_softnet_stat
	# title = CPU48 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu49_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu49_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4150
	# name = cpu.cpu49_softnet_stat
	# title = CPU49 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu50_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu50_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4151
	# name = cpu.cpu50_softnet_stat
	# title = CPU50 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu51_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu51_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4152
	# name = cpu.cpu51_softnet_stat
	# title = CPU51 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.processes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.processes
	# chart type = stacked
	# type = groups
	# family = processes
	# units = processes
	# context = groups.processes
	# priority = 20007
	# name = groups.processes
	# title = User Groups Processes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu52_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu52_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4153
	# name = cpu.cpu52_softnet_stat
	# title = CPU52 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu53_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu53_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4154
	# name = cpu.cpu53_softnet_stat
	# title = CPU53 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu54_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu54_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4155
	# name = cpu.cpu54_softnet_stat
	# title = CPU54 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu55_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu55_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4156
	# name = cpu.cpu55_softnet_stat
	# title = CPU55 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.cpu_user]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu_user
	# chart type = stacked
	# type = groups
	# family = cpu
	# units = percentage
	# context = groups.cpu_user
	# priority = 20020
	# name = groups.cpu_user
	# title = User Groups CPU User Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu56_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu56_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4157
	# name = cpu.cpu56_softnet_stat
	# title = CPU56 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu57_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu57_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4158
	# name = cpu.cpu57_softnet_stat
	# title = CPU57 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu58_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu58_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4159
	# name = cpu.cpu58_softnet_stat
	# title = CPU58 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu59_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu59_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4160
	# name = cpu.cpu59_softnet_stat
	# title = CPU59 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.cpu_system]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.cpu_system
	# chart type = stacked
	# type = groups
	# family = cpu
	# units = percentage
	# context = groups.cpu_system
	# priority = 20021
	# name = groups.cpu_system
	# title = User Groups CPU System Time (9600% = 96 cores)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu60_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu60_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4161
	# name = cpu.cpu60_softnet_stat
	# title = CPU60 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu61_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu61_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4162
	# name = cpu.cpu61_softnet_stat
	# title = CPU61 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu62_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu62_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4163
	# name = cpu.cpu62_softnet_stat
	# title = CPU62 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu63_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu63_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4164
	# name = cpu.cpu63_softnet_stat
	# title = CPU63 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.swap]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.swap
	# chart type = stacked
	# type = groups
	# family = swap
	# units = MiB
	# context = groups.swap
	# priority = 20011
	# name = groups.swap
	# title = User Groups Swap Memory
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1024
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1024
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1024
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1024
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1024
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1024
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1024
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1024
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1024
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1024
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1024
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1024
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1024
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1024
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1024
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1024
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1024
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1024
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1024
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1024
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1024
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1024
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1024
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1024
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1024

[cpu.cpu64_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu64_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4165
	# name = cpu.cpu64_softnet_stat
	# title = CPU64 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu65_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu65_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4166
	# name = cpu.cpu65_softnet_stat
	# title = CPU65 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu66_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu66_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4167
	# name = cpu.cpu66_softnet_stat
	# title = CPU66 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu67_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu67_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4168
	# name = cpu.cpu67_softnet_stat
	# title = CPU67 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.major_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.major_faults
	# chart type = stacked
	# type = groups
	# family = swap
	# units = page faults/s
	# context = groups.major_faults
	# priority = 20012
	# name = groups.major_faults
	# title = User Groups Major Page Faults (swap read)
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu68_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu68_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4169
	# name = cpu.cpu68_softnet_stat
	# title = CPU68 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu69_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu69_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4170
	# name = cpu.cpu69_softnet_stat
	# title = CPU69 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.minor_faults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.minor_faults
	# chart type = stacked
	# type = groups
	# family = mem
	# units = page faults/s
	# context = groups.minor_faults
	# priority = 20011
	# name = groups.minor_faults
	# title = User Groups Minor Page Faults
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10000

[cpu.cpu70_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu70_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4171
	# name = cpu.cpu70_softnet_stat
	# title = CPU70 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[netdata.web_thread2_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread2_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132001
	# name = netdata.web_thread2_cpu
	# title = NetData web server thread No 2 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[cpu.cpu71_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu71_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4172
	# name = cpu.cpu71_softnet_stat
	# title = CPU71 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[netdata.web_thread1_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread1_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132000
	# name = netdata.web_thread1_cpu
	# title = NetData web server thread No 1 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.web_thread3_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread3_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132002
	# name = netdata.web_thread3_cpu
	# title = NetData web server thread No 3 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.web_thread6_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread6_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132005
	# name = netdata.web_thread6_cpu
	# title = NetData web server thread No 6 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.web_thread4_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread4_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132003
	# name = netdata.web_thread4_cpu
	# title = NetData web server thread No 4 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.web_thread5_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.web_thread5_cpu
	# chart type = stacked
	# type = netdata
	# family = web
	# units = milliseconds/s
	# context = netdata.web_cpu
	# priority = 132004
	# name = netdata.web_thread5_cpu
	# title = NetData web server thread No 5 CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[cpu.cpu72_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu72_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4173
	# name = cpu.cpu72_softnet_stat
	# title = CPU72 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.preads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.preads
	# chart type = stacked
	# type = groups
	# family = disk
	# units = KiB/s
	# context = groups.preads
	# priority = 20002
	# name = groups.preads
	# title = User Groups Disk Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu73_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu73_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4174
	# name = cpu.cpu73_softnet_stat
	# title = CPU73 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu74_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu74_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4175
	# name = cpu.cpu74_softnet_stat
	# title = CPU74 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu75_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu75_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4176
	# name = cpu.cpu75_softnet_stat
	# title = CPU75 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu76_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu76_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4177
	# name = cpu.cpu76_softnet_stat
	# title = CPU76 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.pwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.pwrites
	# chart type = stacked
	# type = groups
	# family = disk
	# units = KiB/s
	# context = groups.pwrites
	# priority = 20002
	# name = groups.pwrites
	# title = User Groups Disk Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu77_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu77_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4178
	# name = cpu.cpu77_softnet_stat
	# title = CPU77 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu78_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu78_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4179
	# name = cpu.cpu78_softnet_stat
	# title = CPU78 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu79_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu79_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4180
	# name = cpu.cpu79_softnet_stat
	# title = CPU79 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu80_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu80_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4181
	# name = cpu.cpu80_softnet_stat
	# title = CPU80 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.lreads]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.lreads
	# chart type = stacked
	# type = groups
	# family = disk
	# units = KiB/s
	# context = groups.lreads
	# priority = 20042
	# name = groups.lreads
	# title = User Groups Disk Logical Reads
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu81_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu81_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4182
	# name = cpu.cpu81_softnet_stat
	# title = CPU81 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu82_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu82_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4183
	# name = cpu.cpu82_softnet_stat
	# title = CPU82 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu83_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu83_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4184
	# name = cpu.cpu83_softnet_stat
	# title = CPU83 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu84_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu84_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4185
	# name = cpu.cpu84_softnet_stat
	# title = CPU84 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.lwrites]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.lwrites
	# chart type = stacked
	# type = groups
	# family = disk
	# units = KiB/s
	# context = groups.lwrites
	# priority = 20042
	# name = groups.lwrites
	# title = User Groups I/O Logical Writes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 10240000
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 10240000
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 10240000
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 10240000
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 10240000
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 10240000
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 10240000
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 10240000
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 10240000
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 10240000
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 10240000
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 10240000
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 10240000
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 10240000
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 10240000
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 10240000
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 10240000
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 10240000
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 10240000
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 10240000
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 10240000
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 10240000
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 10240000
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 10240000
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 10240000

[cpu.cpu85_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu85_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4186
	# name = cpu.cpu85_softnet_stat
	# title = CPU85 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu86_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu86_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4187
	# name = cpu.cpu86_softnet_stat
	# title = CPU86 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu87_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu87_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4188
	# name = cpu.cpu87_softnet_stat
	# title = CPU87 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu88_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu88_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4189
	# name = cpu.cpu88_softnet_stat
	# title = CPU88 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.files]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.files
	# chart type = stacked
	# type = groups
	# family = disk
	# units = open files
	# context = groups.files
	# priority = 20050
	# name = groups.files
	# title = User Groups Open Files
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu89_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu89_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4190
	# name = cpu.cpu89_softnet_stat
	# title = CPU89 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu90_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu90_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4191
	# name = cpu.cpu90_softnet_stat
	# title = CPU90 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu91_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu91_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4192
	# name = cpu.cpu91_softnet_stat
	# title = CPU91 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu92_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu92_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4193
	# name = cpu.cpu92_softnet_stat
	# title = CPU92 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[groups.sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.sockets
	# chart type = stacked
	# type = groups
	# family = net
	# units = open sockets
	# context = groups.sockets
	# priority = 20051
	# name = groups.sockets
	# title = User Groups Open Sockets
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[cpu.cpu93_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu93_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4194
	# name = cpu.cpu93_softnet_stat
	# title = CPU93 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu94_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu94_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4195
	# name = cpu.cpu94_softnet_stat
	# title = CPU94 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[cpu.cpu95_softnet_stat]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cpu.cpu95_softnet_stat
	# chart type = line
	# type = cpu
	# family = softnet_stat
	# units = events/s
	# context = cpu.softnet_stat
	# priority = 4196
	# name = cpu.cpu95_softnet_stat
	# title = CPU95 softnet_stat
	# dim processed name = processed
	# dim processed algorithm = incremental
	# dim processed multiplier = 1
	# dim processed divisor = 1
	# dim dropped name = dropped
	# dim dropped algorithm = incremental
	# dim dropped multiplier = 1
	# dim dropped divisor = 1
	# dim squeezed name = squeezed
	# dim squeezed algorithm = incremental
	# dim squeezed multiplier = 1
	# dim squeezed divisor = 1
	# dim received_rps name = received_rps
	# dim received_rps algorithm = incremental
	# dim received_rps multiplier = 1
	# dim received_rps divisor = 1

[netfilter.conntrack_sockets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_sockets
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = active connections
	# context = netfilter.conntrack_sockets
	# priority = 8700
	# name = netfilter.conntrack_sockets
	# title = Connection Tracker Connections
	# dim connections name = connections
	# dim connections algorithm = absolute
	# dim connections multiplier = 1
	# dim connections divisor = 1

[groups.pipes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.pipes
	# chart type = stacked
	# type = groups
	# family = processes
	# units = open pipes
	# context = groups.pipes
	# priority = 20053
	# name = groups.pipes
	# title = User Groups Pipes
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[netfilter.conntrack_new]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_new
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = connections/s
	# context = netfilter.conntrack_new
	# priority = 8701
	# name = netfilter.conntrack_new
	# title = Connection Tracker New Connections
	# dim new name = new
	# dim new algorithm = incremental
	# dim new multiplier = 1
	# dim new divisor = 1
	# dim ignore name = ignore
	# dim ignore algorithm = incremental
	# dim ignore multiplier = -1
	# dim ignore divisor = 1
	# dim invalid name = invalid
	# dim invalid algorithm = incremental
	# dim invalid multiplier = -1
	# dim invalid divisor = 1

[netfilter.conntrack_changes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_changes
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = changes/s
	# context = netfilter.conntrack_changes
	# priority = 8702
	# name = netfilter.conntrack_changes
	# title = Connection Tracker Changes
	# dim inserted name = inserted
	# dim inserted algorithm = incremental
	# dim inserted multiplier = 1
	# dim inserted divisor = 1
	# dim deleted name = deleted
	# dim deleted algorithm = incremental
	# dim deleted multiplier = -1
	# dim deleted divisor = 1
	# dim delete_list name = delete_list
	# dim delete_list algorithm = incremental
	# dim delete_list multiplier = -1
	# dim delete_list divisor = 1

[netfilter.conntrack_expect]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_expect
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = expectations/s
	# context = netfilter.conntrack_expect
	# priority = 8703
	# name = netfilter.conntrack_expect
	# title = Connection Tracker Expectations
	# dim created name = created
	# dim created algorithm = incremental
	# dim created multiplier = 1
	# dim created divisor = 1
	# dim deleted name = deleted
	# dim deleted algorithm = incremental
	# dim deleted multiplier = -1
	# dim deleted divisor = 1
	# dim new name = new
	# dim new algorithm = incremental
	# dim new multiplier = 1
	# dim new divisor = 1

[netfilter.conntrack_search]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_search
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = searches/s
	# context = netfilter.conntrack_search
	# priority = 8710
	# name = netfilter.conntrack_search
	# title = Connection Tracker Searches
	# dim searched name = searched
	# dim searched algorithm = incremental
	# dim searched multiplier = 1
	# dim searched divisor = 1
	# dim restarted name = restarted
	# dim restarted algorithm = incremental
	# dim restarted multiplier = -1
	# dim restarted divisor = 1
	# dim found name = found
	# dim found algorithm = incremental
	# dim found multiplier = 1
	# dim found divisor = 1

[netfilter.conntrack_errors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netfilter.conntrack_errors
	# chart type = line
	# type = netfilter
	# family = conntrack
	# units = events/s
	# context = netfilter.conntrack_errors
	# priority = 8705
	# name = netfilter.conntrack_errors
	# title = Connection Tracker Errors
	# dim icmp_error name = icmp_error
	# dim icmp_error algorithm = incremental
	# dim icmp_error multiplier = 1
	# dim icmp_error divisor = 1
	# dim insert_failed name = insert_failed
	# dim insert_failed algorithm = incremental
	# dim insert_failed multiplier = -1
	# dim insert_failed divisor = 1
	# dim drop name = drop
	# dim drop algorithm = incremental
	# dim drop multiplier = -1
	# dim drop divisor = 1
	# dim early_drop name = early_drop
	# dim early_drop algorithm = incremental
	# dim early_drop multiplier = -1
	# dim early_drop divisor = 1

[disk.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.sda
	# chart type = area
	# type = disk
	# family = sda
	# units = KiB/s
	# context = disk.io
	# priority = 2000
	# name = disk.sda
	# title = Disk I/O Bandwidth
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.sda
	# chart type = line
	# type = disk_ops
	# family = sda
	# units = operations/s
	# context = disk.ops
	# priority = 2001
	# name = disk_ops.sda
	# title = Disk Completed I/O Operations
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_backlog.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.sda
	# chart type = area
	# type = disk_backlog
	# family = sda
	# units = milliseconds
	# context = disk.backlog
	# priority = 2003
	# name = disk_backlog.sda
	# title = Disk Backlog
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.sda
	# chart type = area
	# type = disk_util
	# family = sda
	# units = % of time working
	# context = disk.util
	# priority = 2004
	# name = disk_util.sda
	# title = Disk Utilization Time
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_mops.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_mops.sda
	# chart type = line
	# type = disk_mops
	# family = sda
	# units = merged operations/s
	# context = disk.mops
	# priority = 2021
	# name = disk_mops.sda
	# title = Disk Merged Operations
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_iotime.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.sda
	# chart type = line
	# type = disk_iotime
	# family = sda
	# units = milliseconds/s
	# context = disk.iotime
	# priority = 2022
	# name = disk_iotime.sda
	# title = Disk Total I/O Time
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk.dm_0
	# chart type = area
	# type = disk
	# family = /var/lib/nova
	# units = KiB/s
	# context = disk.io
	# priority = 2000
	# name = disk.vg00_var_lib_nova
	# title = Disk I/O Bandwidth
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_ops.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_ops.dm_0
	# chart type = line
	# type = disk_ops
	# family = /var/lib/nova
	# units = operations/s
	# context = disk.ops
	# priority = 2001
	# name = disk_ops.vg00_var_lib_nova
	# title = Disk Completed I/O Operations
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_backlog.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_backlog.dm_0
	# chart type = area
	# type = disk_backlog
	# family = /var/lib/nova
	# units = milliseconds
	# context = disk.backlog
	# priority = 2003
	# name = disk_backlog.vg00_var_lib_nova
	# title = Disk Backlog
	# dim backlog name = backlog
	# dim backlog algorithm = incremental
	# dim backlog multiplier = 1
	# dim backlog divisor = 10

[disk_util.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_util.dm_0
	# chart type = area
	# type = disk_util
	# family = /var/lib/nova
	# units = % of time working
	# context = disk.util
	# priority = 2004
	# name = disk_util.vg00_var_lib_nova
	# title = Disk Utilization Time
	# dim utilization name = utilization
	# dim utilization algorithm = incremental
	# dim utilization multiplier = 1
	# dim utilization divisor = 10

[disk_iotime.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_iotime.dm_0
	# chart type = line
	# type = disk_iotime
	# family = /var/lib/nova
	# units = milliseconds/s
	# context = disk.iotime
	# priority = 2022
	# name = disk_iotime.vg00_var_lib_nova
	# title = Disk Total I/O Time
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[system.io]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.io
	# chart type = area
	# type = system
	# family = disk
	# units = KiB/s
	# context = system.io
	# priority = 150
	# name = system.io
	# title = Disk I/O
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 1
	# dim in divisor = 1
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -1
	# dim out divisor = 1

[nfsd.readcache]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.readcache
	# chart type = stacked
	# type = nfsd
	# family = cache
	# units = reads/s
	# context = nfsd.readcache
	# priority = 2100
	# name = nfsd.readcache
	# title = NFS Server Read Cache
	# dim hits name = hits
	# dim hits algorithm = incremental
	# dim hits multiplier = 1
	# dim hits divisor = 1
	# dim misses name = misses
	# dim misses algorithm = incremental
	# dim misses multiplier = 1
	# dim misses divisor = 1
	# dim nocache name = nocache
	# dim nocache algorithm = incremental
	# dim nocache multiplier = 1
	# dim nocache divisor = 1

[nfsd.net]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.net
	# chart type = stacked
	# type = nfsd
	# family = network
	# units = packets/s
	# context = nfsd.net
	# priority = 2107
	# name = nfsd.net
	# title = NFS Server Network Statistics
	# dim udp name = udp
	# dim udp algorithm = incremental
	# dim udp multiplier = 1
	# dim udp divisor = 1
	# dim tcp name = tcp
	# dim tcp algorithm = incremental
	# dim tcp multiplier = 1
	# dim tcp divisor = 1

[nfsd.rpc]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.rpc
	# chart type = line
	# type = nfsd
	# family = rpc
	# units = calls/s
	# context = nfsd.rpc
	# priority = 2108
	# name = nfsd.rpc
	# title = NFS Server Remote Procedure Calls Statistics
	# dim calls name = calls
	# dim calls algorithm = incremental
	# dim calls multiplier = 1
	# dim calls divisor = 1
	# dim bad_format name = bad_format
	# dim bad_format algorithm = incremental
	# dim bad_format multiplier = -1
	# dim bad_format divisor = 1
	# dim bad_auth name = bad_auth
	# dim bad_auth algorithm = incremental
	# dim bad_auth multiplier = -1
	# dim bad_auth divisor = 1

[nfsd.proc4]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.proc4
	# chart type = stacked
	# type = nfsd
	# family = nfsv4rpc
	# units = calls/s
	# context = nfsd.proc4
	# priority = 2111
	# name = nfsd.proc4
	# title = NFS v4 Server Remote Procedure Calls
	# dim null name = null
	# dim null algorithm = incremental
	# dim null multiplier = 1
	# dim null divisor = 1
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1

[nfsd.proc4ops]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.proc4ops
	# chart type = stacked
	# type = nfsd
	# family = nfsv2ops
	# units = operations/s
	# context = nfsd.proc4ops
	# priority = 2112
	# name = nfsd.proc4ops
	# title = NFS v4 Server Operations
	# dim unused_op0 name = unused_op0
	# dim unused_op0 algorithm = incremental
	# dim unused_op0 multiplier = 1
	# dim unused_op0 divisor = 1
	# dim unused_op1 name = unused_op1
	# dim unused_op1 algorithm = incremental
	# dim unused_op1 multiplier = 1
	# dim unused_op1 divisor = 1
	# dim future_op2 name = future_op2
	# dim future_op2 algorithm = incremental
	# dim future_op2 multiplier = 1
	# dim future_op2 divisor = 1
	# dim access name = access
	# dim access algorithm = incremental
	# dim access multiplier = 1
	# dim access divisor = 1
	# dim close name = close
	# dim close algorithm = incremental
	# dim close multiplier = 1
	# dim close divisor = 1
	# dim commit name = commit
	# dim commit algorithm = incremental
	# dim commit multiplier = 1
	# dim commit divisor = 1
	# dim create name = create
	# dim create algorithm = incremental
	# dim create multiplier = 1
	# dim create divisor = 1
	# dim delegpurge name = delegpurge
	# dim delegpurge algorithm = incremental
	# dim delegpurge multiplier = 1
	# dim delegpurge divisor = 1
	# dim delegreturn name = delegreturn
	# dim delegreturn algorithm = incremental
	# dim delegreturn multiplier = 1
	# dim delegreturn divisor = 1
	# dim getattr name = getattr
	# dim getattr algorithm = incremental
	# dim getattr multiplier = 1
	# dim getattr divisor = 1
	# dim getfh name = getfh
	# dim getfh algorithm = incremental
	# dim getfh multiplier = 1
	# dim getfh divisor = 1
	# dim link name = link
	# dim link algorithm = incremental
	# dim link multiplier = 1
	# dim link divisor = 1
	# dim lock name = lock
	# dim lock algorithm = incremental
	# dim lock multiplier = 1
	# dim lock divisor = 1
	# dim lockt name = lockt
	# dim lockt algorithm = incremental
	# dim lockt multiplier = 1
	# dim lockt divisor = 1
	# dim locku name = locku
	# dim locku algorithm = incremental
	# dim locku multiplier = 1
	# dim locku divisor = 1
	# dim lookup name = lookup
	# dim lookup algorithm = incremental
	# dim lookup multiplier = 1
	# dim lookup divisor = 1
	# dim lookup_root name = lookup_root
	# dim lookup_root algorithm = incremental
	# dim lookup_root multiplier = 1
	# dim lookup_root divisor = 1
	# dim nverify name = nverify
	# dim nverify algorithm = incremental
	# dim nverify multiplier = 1
	# dim nverify divisor = 1
	# dim open name = open
	# dim open algorithm = incremental
	# dim open multiplier = 1
	# dim open divisor = 1
	# dim openattr name = openattr
	# dim openattr algorithm = incremental
	# dim openattr multiplier = 1
	# dim openattr divisor = 1
	# dim open_confirm name = open_confirm
	# dim open_confirm algorithm = incremental
	# dim open_confirm multiplier = 1
	# dim open_confirm divisor = 1
	# dim open_downgrade name = open_downgrade
	# dim open_downgrade algorithm = incremental
	# dim open_downgrade multiplier = 1
	# dim open_downgrade divisor = 1
	# dim putfh name = putfh
	# dim putfh algorithm = incremental
	# dim putfh multiplier = 1
	# dim putfh divisor = 1
	# dim putpubfh name = putpubfh
	# dim putpubfh algorithm = incremental
	# dim putpubfh multiplier = 1
	# dim putpubfh divisor = 1
	# dim putrootfh name = putrootfh
	# dim putrootfh algorithm = incremental
	# dim putrootfh multiplier = 1
	# dim putrootfh divisor = 1
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim readdir name = readdir
	# dim readdir algorithm = incremental
	# dim readdir multiplier = 1
	# dim readdir divisor = 1
	# dim readlink name = readlink
	# dim readlink algorithm = incremental
	# dim readlink multiplier = 1
	# dim readlink divisor = 1
	# dim remove name = remove
	# dim remove algorithm = incremental
	# dim remove multiplier = 1
	# dim remove divisor = 1
	# dim rename name = rename
	# dim rename algorithm = incremental
	# dim rename multiplier = 1
	# dim rename divisor = 1
	# dim renew name = renew
	# dim renew algorithm = incremental
	# dim renew multiplier = 1
	# dim renew divisor = 1
	# dim restorefh name = restorefh
	# dim restorefh algorithm = incremental
	# dim restorefh multiplier = 1
	# dim restorefh divisor = 1
	# dim savefh name = savefh
	# dim savefh algorithm = incremental
	# dim savefh multiplier = 1
	# dim savefh divisor = 1
	# dim secinfo name = secinfo
	# dim secinfo algorithm = incremental
	# dim secinfo multiplier = 1
	# dim secinfo divisor = 1
	# dim setattr name = setattr
	# dim setattr algorithm = incremental
	# dim setattr multiplier = 1
	# dim setattr divisor = 1
	# dim setclientid name = setclientid
	# dim setclientid algorithm = incremental
	# dim setclientid multiplier = 1
	# dim setclientid divisor = 1
	# dim setclientid_confirm name = setclientid_confirm
	# dim setclientid_confirm algorithm = incremental
	# dim setclientid_confirm multiplier = 1
	# dim setclientid_confirm divisor = 1
	# dim verify name = verify
	# dim verify algorithm = incremental
	# dim verify multiplier = 1
	# dim verify divisor = 1
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = 1
	# dim write divisor = 1
	# dim release_lockowner name = release_lockowner
	# dim release_lockowner algorithm = incremental
	# dim release_lockowner multiplier = 1
	# dim release_lockowner divisor = 1
	# dim backchannel_ctl name = backchannel_ctl
	# dim backchannel_ctl algorithm = incremental
	# dim backchannel_ctl multiplier = 1
	# dim backchannel_ctl divisor = 1
	# dim bind_conn_to_session name = bind_conn_to_session
	# dim bind_conn_to_session algorithm = incremental
	# dim bind_conn_to_session multiplier = 1
	# dim bind_conn_to_session divisor = 1
	# dim exchange_id name = exchange_id
	# dim exchange_id algorithm = incremental
	# dim exchange_id multiplier = 1
	# dim exchange_id divisor = 1
	# dim create_session name = create_session
	# dim create_session algorithm = incremental
	# dim create_session multiplier = 1
	# dim create_session divisor = 1
	# dim destroy_session name = destroy_session
	# dim destroy_session algorithm = incremental
	# dim destroy_session multiplier = 1
	# dim destroy_session divisor = 1
	# dim free_stateid name = free_stateid
	# dim free_stateid algorithm = incremental
	# dim free_stateid multiplier = 1
	# dim free_stateid divisor = 1
	# dim get_dir_delegation name = get_dir_delegation
	# dim get_dir_delegation algorithm = incremental
	# dim get_dir_delegation multiplier = 1
	# dim get_dir_delegation divisor = 1
	# dim getdeviceinfo name = getdeviceinfo
	# dim getdeviceinfo algorithm = incremental
	# dim getdeviceinfo multiplier = 1
	# dim getdeviceinfo divisor = 1
	# dim getdevicelist name = getdevicelist
	# dim getdevicelist algorithm = incremental
	# dim getdevicelist multiplier = 1
	# dim getdevicelist divisor = 1
	# dim layoutcommit name = layoutcommit
	# dim layoutcommit algorithm = incremental
	# dim layoutcommit multiplier = 1
	# dim layoutcommit divisor = 1
	# dim layoutget name = layoutget
	# dim layoutget algorithm = incremental
	# dim layoutget multiplier = 1
	# dim layoutget divisor = 1
	# dim layoutreturn name = layoutreturn
	# dim layoutreturn algorithm = incremental
	# dim layoutreturn multiplier = 1
	# dim layoutreturn divisor = 1
	# dim secinfo_no_name name = secinfo_no_name
	# dim secinfo_no_name algorithm = incremental
	# dim secinfo_no_name multiplier = 1
	# dim secinfo_no_name divisor = 1
	# dim sequence name = sequence
	# dim sequence algorithm = incremental
	# dim sequence multiplier = 1
	# dim sequence divisor = 1
	# dim set_ssv name = set_ssv
	# dim set_ssv algorithm = incremental
	# dim set_ssv multiplier = 1
	# dim set_ssv divisor = 1
	# dim test_stateid name = test_stateid
	# dim test_stateid algorithm = incremental
	# dim test_stateid multiplier = 1
	# dim test_stateid divisor = 1
	# dim want_delegation name = want_delegation
	# dim want_delegation algorithm = incremental
	# dim want_delegation multiplier = 1
	# dim want_delegation divisor = 1
	# dim destroy_clientid name = destroy_clientid
	# dim destroy_clientid algorithm = incremental
	# dim destroy_clientid multiplier = 1
	# dim destroy_clientid divisor = 1
	# dim reclaim_complete name = reclaim_complete
	# dim reclaim_complete algorithm = incremental
	# dim reclaim_complete multiplier = 1
	# dim reclaim_complete divisor = 1
	# dim allocate name = allocate
	# dim allocate algorithm = incremental
	# dim allocate multiplier = 1
	# dim allocate divisor = 1
	# dim copy name = copy
	# dim copy algorithm = incremental
	# dim copy multiplier = 1
	# dim copy divisor = 1
	# dim copy_notify name = copy_notify
	# dim copy_notify algorithm = incremental
	# dim copy_notify multiplier = 1
	# dim copy_notify divisor = 1
	# dim deallocate name = deallocate
	# dim deallocate algorithm = incremental
	# dim deallocate multiplier = 1
	# dim deallocate divisor = 1
	# dim ioadvise name = ioadvise
	# dim ioadvise algorithm = incremental
	# dim ioadvise multiplier = 1
	# dim ioadvise divisor = 1
	# dim layouterror name = layouterror
	# dim layouterror algorithm = incremental
	# dim layouterror multiplier = 1
	# dim layouterror divisor = 1
	# dim layoutstats name = layoutstats
	# dim layoutstats algorithm = incremental
	# dim layoutstats multiplier = 1
	# dim layoutstats divisor = 1
	# dim offload_cancel name = offload_cancel
	# dim offload_cancel algorithm = incremental
	# dim offload_cancel multiplier = 1
	# dim offload_cancel divisor = 1
	# dim offload_status name = offload_status
	# dim offload_status algorithm = incremental
	# dim offload_status multiplier = 1
	# dim offload_status divisor = 1
	# dim read_plus name = read_plus
	# dim read_plus algorithm = incremental
	# dim read_plus multiplier = 1
	# dim read_plus divisor = 1
	# dim seek name = seek
	# dim seek algorithm = incremental
	# dim seek multiplier = 1
	# dim seek divisor = 1
	# dim write_same name = write_same
	# dim write_same algorithm = incremental
	# dim write_same multiplier = 1
	# dim write_same divisor = 1

[nfs.rpc]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfs.rpc
	# chart type = line
	# type = nfs
	# family = rpc
	# units = calls/s
	# context = nfs.rpc
	# priority = 2208
	# name = nfs.rpc
	# title = NFS Client Remote Procedure Calls Statistics
	# dim calls name = calls
	# dim calls algorithm = incremental
	# dim calls multiplier = 1
	# dim calls divisor = 1
	# dim retransmits name = retransmits
	# dim retransmits algorithm = incremental
	# dim retransmits multiplier = -1
	# dim retransmits divisor = 1
	# dim auth_refresh name = auth_refresh
	# dim auth_refresh algorithm = incremental
	# dim auth_refresh multiplier = -1
	# dim auth_refresh divisor = 1

[nfs.proc4]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfs.proc4
	# chart type = stacked
	# type = nfs
	# family = nfsv4rpc
	# units = calls/s
	# context = nfs.proc4
	# priority = 2211
	# name = nfs.proc4
	# title = NFS v4 Client Remote Procedure Calls
	# dim null name = null
	# dim null algorithm = incremental
	# dim null multiplier = 1
	# dim null divisor = 1
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = 1
	# dim write divisor = 1
	# dim commit name = commit
	# dim commit algorithm = incremental
	# dim commit multiplier = 1
	# dim commit divisor = 1
	# dim open name = open
	# dim open algorithm = incremental
	# dim open multiplier = 1
	# dim open divisor = 1
	# dim open_conf name = open_conf
	# dim open_conf algorithm = incremental
	# dim open_conf multiplier = 1
	# dim open_conf divisor = 1
	# dim open_noat name = open_noat
	# dim open_noat algorithm = incremental
	# dim open_noat multiplier = 1
	# dim open_noat divisor = 1
	# dim open_dgrd name = open_dgrd
	# dim open_dgrd algorithm = incremental
	# dim open_dgrd multiplier = 1
	# dim open_dgrd divisor = 1
	# dim close name = close
	# dim close algorithm = incremental
	# dim close multiplier = 1
	# dim close divisor = 1
	# dim setattr name = setattr
	# dim setattr algorithm = incremental
	# dim setattr multiplier = 1
	# dim setattr divisor = 1
	# dim fsinfo name = fsinfo
	# dim fsinfo algorithm = incremental
	# dim fsinfo multiplier = 1
	# dim fsinfo divisor = 1
	# dim renew name = renew
	# dim renew algorithm = incremental
	# dim renew multiplier = 1
	# dim renew divisor = 1
	# dim setclntid name = setclntid
	# dim setclntid algorithm = incremental
	# dim setclntid multiplier = 1
	# dim setclntid divisor = 1
	# dim confirm name = confirm
	# dim confirm algorithm = incremental
	# dim confirm multiplier = 1
	# dim confirm divisor = 1
	# dim lock name = lock
	# dim lock algorithm = incremental
	# dim lock multiplier = 1
	# dim lock divisor = 1
	# dim lockt name = lockt
	# dim lockt algorithm = incremental
	# dim lockt multiplier = 1
	# dim lockt divisor = 1
	# dim locku name = locku
	# dim locku algorithm = incremental
	# dim locku multiplier = 1
	# dim locku divisor = 1
	# dim access name = access
	# dim access algorithm = incremental
	# dim access multiplier = 1
	# dim access divisor = 1
	# dim getattr name = getattr
	# dim getattr algorithm = incremental
	# dim getattr multiplier = 1
	# dim getattr divisor = 1
	# dim lookup name = lookup
	# dim lookup algorithm = incremental
	# dim lookup multiplier = 1
	# dim lookup divisor = 1
	# dim lookup_root name = lookup_root
	# dim lookup_root algorithm = incremental
	# dim lookup_root multiplier = 1
	# dim lookup_root divisor = 1
	# dim remove name = remove
	# dim remove algorithm = incremental
	# dim remove multiplier = 1
	# dim remove divisor = 1
	# dim rename name = rename
	# dim rename algorithm = incremental
	# dim rename multiplier = 1
	# dim rename divisor = 1
	# dim link name = link
	# dim link algorithm = incremental
	# dim link multiplier = 1
	# dim link divisor = 1
	# dim symlink name = symlink
	# dim symlink algorithm = incremental
	# dim symlink multiplier = 1
	# dim symlink divisor = 1
	# dim create name = create
	# dim create algorithm = incremental
	# dim create multiplier = 1
	# dim create divisor = 1
	# dim pathconf name = pathconf
	# dim pathconf algorithm = incremental
	# dim pathconf multiplier = 1
	# dim pathconf divisor = 1
	# dim statfs name = statfs
	# dim statfs algorithm = incremental
	# dim statfs multiplier = 1
	# dim statfs divisor = 1
	# dim readlink name = readlink
	# dim readlink algorithm = incremental
	# dim readlink multiplier = 1
	# dim readlink divisor = 1
	# dim readdir name = readdir
	# dim readdir algorithm = incremental
	# dim readdir multiplier = 1
	# dim readdir divisor = 1
	# dim server_caps name = server_caps
	# dim server_caps algorithm = incremental
	# dim server_caps multiplier = 1
	# dim server_caps divisor = 1
	# dim delegreturn name = delegreturn
	# dim delegreturn algorithm = incremental
	# dim delegreturn multiplier = 1
	# dim delegreturn divisor = 1
	# dim getacl name = getacl
	# dim getacl algorithm = incremental
	# dim getacl multiplier = 1
	# dim getacl divisor = 1
	# dim setacl name = setacl
	# dim setacl algorithm = incremental
	# dim setacl multiplier = 1
	# dim setacl divisor = 1
	# dim fs_locations name = fs_locations
	# dim fs_locations algorithm = incremental
	# dim fs_locations multiplier = 1
	# dim fs_locations divisor = 1
	# dim rel_lkowner name = rel_lkowner
	# dim rel_lkowner algorithm = incremental
	# dim rel_lkowner multiplier = 1
	# dim rel_lkowner divisor = 1
	# dim secinfo name = secinfo
	# dim secinfo algorithm = incremental
	# dim secinfo multiplier = 1
	# dim secinfo divisor = 1
	# dim fsid_present name = fsid_present
	# dim fsid_present algorithm = incremental
	# dim fsid_present multiplier = 1
	# dim fsid_present divisor = 1
	# dim exchange_id name = exchange_id
	# dim exchange_id algorithm = incremental
	# dim exchange_id multiplier = 1
	# dim exchange_id divisor = 1
	# dim create_session name = create_session
	# dim create_session algorithm = incremental
	# dim create_session multiplier = 1
	# dim create_session divisor = 1
	# dim destroy_session name = destroy_session
	# dim destroy_session algorithm = incremental
	# dim destroy_session multiplier = 1
	# dim destroy_session divisor = 1
	# dim sequence name = sequence
	# dim sequence algorithm = incremental
	# dim sequence multiplier = 1
	# dim sequence divisor = 1
	# dim get_lease_time name = get_lease_time
	# dim get_lease_time algorithm = incremental
	# dim get_lease_time multiplier = 1
	# dim get_lease_time divisor = 1
	# dim reclaim_comp name = reclaim_comp
	# dim reclaim_comp algorithm = incremental
	# dim reclaim_comp multiplier = 1
	# dim reclaim_comp divisor = 1
	# dim layoutget name = layoutget
	# dim layoutget algorithm = incremental
	# dim layoutget multiplier = 1
	# dim layoutget divisor = 1
	# dim getdevinfo name = getdevinfo
	# dim getdevinfo algorithm = incremental
	# dim getdevinfo multiplier = 1
	# dim getdevinfo divisor = 1
	# dim layoutcommit name = layoutcommit
	# dim layoutcommit algorithm = incremental
	# dim layoutcommit multiplier = 1
	# dim layoutcommit divisor = 1
	# dim layoutreturn name = layoutreturn
	# dim layoutreturn algorithm = incremental
	# dim layoutreturn multiplier = 1
	# dim layoutreturn divisor = 1
	# dim secinfo_no name = secinfo_no
	# dim secinfo_no algorithm = incremental
	# dim secinfo_no multiplier = 1
	# dim secinfo_no divisor = 1
	# dim test_stateid name = test_stateid
	# dim test_stateid algorithm = incremental
	# dim test_stateid multiplier = 1
	# dim test_stateid divisor = 1
	# dim free_stateid name = free_stateid
	# dim free_stateid algorithm = incremental
	# dim free_stateid multiplier = 1
	# dim free_stateid divisor = 1
	# dim getdevicelist name = getdevicelist
	# dim getdevicelist algorithm = incremental
	# dim getdevicelist multiplier = 1
	# dim getdevicelist divisor = 1
	# dim bind_conn_to_ses name = bind_conn_to_ses
	# dim bind_conn_to_ses algorithm = incremental
	# dim bind_conn_to_ses multiplier = 1
	# dim bind_conn_to_ses divisor = 1
	# dim destroy_clientid name = destroy_clientid
	# dim destroy_clientid algorithm = incremental
	# dim destroy_clientid multiplier = 1
	# dim destroy_clientid divisor = 1
	# dim seek name = seek
	# dim seek algorithm = incremental
	# dim seek multiplier = 1
	# dim seek divisor = 1
	# dim allocate name = allocate
	# dim allocate algorithm = incremental
	# dim allocate multiplier = 1
	# dim allocate divisor = 1
	# dim deallocate name = deallocate
	# dim deallocate algorithm = incremental
	# dim deallocate multiplier = 1
	# dim deallocate divisor = 1
	# dim layoutstats name = layoutstats
	# dim layoutstats algorithm = incremental
	# dim layoutstats multiplier = 1
	# dim layoutstats divisor = 1
	# dim clone name = clone
	# dim clone algorithm = incremental
	# dim clone multiplier = 1
	# dim clone divisor = 1

[system.ipc_semaphores]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ipc_semaphores
	# chart type = area
	# type = system
	# family = ipc semaphores
	# units = semaphores
	# context = system.ipc_semaphores
	# priority = 1203
	# name = system.ipc_semaphores
	# title = IPC Semaphores
	# dim semaphores name = semaphores
	# dim semaphores algorithm = absolute
	# dim semaphores multiplier = 1
	# dim semaphores divisor = 1

[system.ipc_semaphore_arrays]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.ipc_semaphore_arrays
	# chart type = area
	# type = system
	# family = ipc semaphores
	# units = arrays
	# context = system.ipc_semaphore_arrays
	# priority = 1204
	# name = system.ipc_semaphore_arrays
	# title = IPC Semaphore Arrays
	# dim arrays name = arrays
	# dim arrays algorithm = absolute
	# dim arrays multiplier = 1
	# dim arrays divisor = 1

[system.shared_memory_segments]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.shared_memory_segments
	# chart type = stacked
	# type = system
	# family = ipc shared memory
	# units = segments
	# context = system.shared_memory_segments
	# priority = 1205
	# name = system.shared_memory_segments
	# title = IPC Shared Memory Number of Segments
	# dim segments name = segments
	# dim segments algorithm = absolute
	# dim segments multiplier = 1
	# dim segments divisor = 1

[system.shared_memory_bytes]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.shared_memory_bytes
	# chart type = stacked
	# type = system
	# family = ipc shared memory
	# units = bytes
	# context = system.shared_memory_bytes
	# priority = 1206
	# name = system.shared_memory_bytes
	# title = IPC Shared Memory Used Bytes
	# dim bytes name = bytes
	# dim bytes algorithm = absolute
	# dim bytes multiplier = 1
	# dim bytes divisor = 1

[netdata.plugin_proc_modules]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_proc_modules
	# chart type = stacked
	# type = netdata
	# family = proc
	# units = milliseconds/run
	# context = netdata.plugin_proc_modules
	# priority = 132001
	# name = netdata.plugin_proc_modules
	# title = NetData Proc Plugin Modules Durations
	# dim stat name = stat
	# dim stat algorithm = absolute
	# dim stat multiplier = 1
	# dim stat divisor = 1000
	# dim uptime name = uptime
	# dim uptime algorithm = absolute
	# dim uptime multiplier = 1
	# dim uptime divisor = 1000
	# dim loadavg name = loadavg
	# dim loadavg algorithm = absolute
	# dim loadavg multiplier = 1
	# dim loadavg divisor = 1000
	# dim entropy name = entropy
	# dim entropy algorithm = absolute
	# dim entropy multiplier = 1
	# dim entropy divisor = 1000
	# dim interrupts name = interrupts
	# dim interrupts algorithm = absolute
	# dim interrupts multiplier = 1
	# dim interrupts divisor = 1000
	# dim softirqs name = softirqs
	# dim softirqs algorithm = absolute
	# dim softirqs multiplier = 1
	# dim softirqs divisor = 1000
	# dim vmstat name = vmstat
	# dim vmstat algorithm = absolute
	# dim vmstat multiplier = 1
	# dim vmstat divisor = 1000
	# dim meminfo name = meminfo
	# dim meminfo algorithm = absolute
	# dim meminfo multiplier = 1
	# dim meminfo divisor = 1000
	# dim ksm name = ksm
	# dim ksm algorithm = absolute
	# dim ksm multiplier = 1
	# dim ksm divisor = 1000
	# dim ecc name = ecc
	# dim ecc algorithm = absolute
	# dim ecc multiplier = 1
	# dim ecc divisor = 1000
	# dim numa name = numa
	# dim numa algorithm = absolute
	# dim numa multiplier = 1
	# dim numa divisor = 1000
	# dim netdev name = netdev
	# dim netdev algorithm = absolute
	# dim netdev multiplier = 1
	# dim netdev divisor = 1000
	# dim sockstat name = sockstat
	# dim sockstat algorithm = absolute
	# dim sockstat multiplier = 1
	# dim sockstat divisor = 1000
	# dim sockstat6 name = sockstat6
	# dim sockstat6 algorithm = absolute
	# dim sockstat6 multiplier = 1
	# dim sockstat6 divisor = 1000
	# dim netstat name = netstat
	# dim netstat algorithm = absolute
	# dim netstat multiplier = 1
	# dim netstat divisor = 1000
	# dim snmp name = snmp
	# dim snmp algorithm = absolute
	# dim snmp multiplier = 1
	# dim snmp divisor = 1000
	# dim snmp6 name = snmp6
	# dim snmp6 algorithm = absolute
	# dim snmp6 multiplier = 1
	# dim snmp6 divisor = 1000
	# dim softnet name = softnet
	# dim softnet algorithm = absolute
	# dim softnet multiplier = 1
	# dim softnet divisor = 1000
	# dim conntrack name = conntrack
	# dim conntrack algorithm = absolute
	# dim conntrack multiplier = 1
	# dim conntrack divisor = 1000
	# dim diskstats name = diskstats
	# dim diskstats algorithm = absolute
	# dim diskstats multiplier = 1
	# dim diskstats divisor = 1000
	# dim mdstat name = mdstat
	# dim mdstat algorithm = absolute
	# dim mdstat multiplier = 1
	# dim mdstat divisor = 1000
	# dim nfsd name = nfsd
	# dim nfsd algorithm = absolute
	# dim nfsd multiplier = 1
	# dim nfsd divisor = 1000
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1000
	# dim btrfs name = btrfs
	# dim btrfs algorithm = absolute
	# dim btrfs multiplier = 1
	# dim btrfs divisor = 1000
	# dim ipc name = ipc
	# dim ipc algorithm = absolute
	# dim ipc multiplier = 1
	# dim ipc divisor = 1000
	# dim power_supply name = power_supply
	# dim power_supply algorithm = absolute
	# dim power_supply multiplier = 1
	# dim power_supply divisor = 1000

[netdata.plugin_proc_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.plugin_proc_cpu
	# chart type = stacked
	# type = netdata
	# family = proc
	# units = milliseconds/s
	# context = netdata.plugin_proc_cpu
	# priority = 132000
	# name = netdata.plugin_proc_cpu
	# title = NetData Proc Plugin CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.server_cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.server_cpu
	# chart type = stacked
	# type = netdata
	# family = netdata
	# units = milliseconds/s
	# context = netdata.server_cpu
	# priority = 130000
	# name = netdata.server_cpu
	# title = NetData CPU usage
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 1
	# dim user divisor = 1000
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 1
	# dim system divisor = 1000

[netdata.clients]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.clients
	# chart type = line
	# type = netdata
	# family = netdata
	# units = connected clients
	# context = netdata.clients
	# priority = 130200
	# name = netdata.clients
	# title = NetData Web Clients
	# dim clients name = clients
	# dim clients algorithm = absolute
	# dim clients multiplier = 1
	# dim clients divisor = 1

[netdata.requests]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.requests
	# chart type = line
	# type = netdata
	# family = netdata
	# units = requests/s
	# context = netdata.requests
	# priority = 130300
	# name = netdata.requests
	# title = NetData Web Requests
	# dim requests name = requests
	# dim requests algorithm = incremental
	# dim requests multiplier = 1
	# dim requests divisor = 1

[netdata.net]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.net
	# chart type = area
	# type = netdata
	# family = netdata
	# units = kilobits/s
	# context = netdata.net
	# priority = 130000
	# name = netdata.net
	# title = NetData Network Traffic
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 8
	# dim in divisor = 1000
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -8
	# dim out divisor = 1000

[netdata.response_time]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.response_time
	# chart type = line
	# type = netdata
	# family = netdata
	# units = milliseconds/request
	# context = netdata.response_time
	# priority = 130400
	# name = netdata.response_time
	# title = NetData API Response Time
	# dim average name = average
	# dim average algorithm = absolute
	# dim average multiplier = 1
	# dim average divisor = 1000
	# dim max name = max
	# dim max algorithm = absolute
	# dim max multiplier = 1
	# dim max divisor = 1000

[netdata.compression_ratio]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.compression_ratio
	# chart type = line
	# type = netdata
	# family = netdata
	# units = percentage
	# context = netdata.compression_ratio
	# priority = 130500
	# name = netdata.compression_ratio
	# title = NetData API Responses Compression Savings Ratio
	# dim savings name = savings
	# dim savings algorithm = absolute
	# dim savings multiplier = 1
	# dim savings divisor = 1000

[ipv4.udperrors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv4.udperrors
	# chart type = line
	# type = ipv4
	# family = udp
	# units = events/s
	# context = ipv4.udperrors
	# priority = 5310
	# name = ipv4.udperrors
	# title = IPv4 UDP Errors
	# dim RcvbufErrors name = RcvbufErrors
	# dim RcvbufErrors algorithm = incremental
	# dim RcvbufErrors multiplier = 1
	# dim RcvbufErrors divisor = 1
	# dim SndbufErrors name = SndbufErrors
	# dim SndbufErrors algorithm = incremental
	# dim SndbufErrors multiplier = -1
	# dim SndbufErrors divisor = 1
	# dim InErrors name = InErrors
	# dim InErrors algorithm = incremental
	# dim InErrors multiplier = 1
	# dim InErrors divisor = 1
	# dim NoPorts name = NoPorts
	# dim NoPorts algorithm = incremental
	# dim NoPorts multiplier = 1
	# dim NoPorts divisor = 1
	# dim InCsumErrors name = InCsumErrors
	# dim InCsumErrors algorithm = incremental
	# dim InCsumErrors multiplier = 1
	# dim InCsumErrors divisor = 1
	# dim IgnoredMulti name = IgnoredMulti
	# dim IgnoredMulti algorithm = incremental
	# dim IgnoredMulti multiplier = 1
	# dim IgnoredMulti divisor = 1

[ipv6.udppackets]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.udppackets
	# chart type = line
	# type = ipv6
	# family = udp6
	# units = packets/s
	# context = ipv6.udppackets
	# priority = 6601
	# name = ipv6.udppackets
	# title = IPv6 UDP Packets
	# dim InDatagrams name = received
	# dim InDatagrams algorithm = incremental
	# dim InDatagrams multiplier = 1
	# dim InDatagrams divisor = 1
	# dim OutDatagrams name = sent
	# dim OutDatagrams algorithm = incremental
	# dim OutDatagrams multiplier = -1
	# dim OutDatagrams divisor = 1

[ipv6.udperrors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.udperrors
	# chart type = line
	# type = ipv6
	# family = udp6
	# units = events/s
	# context = ipv6.udperrors
	# priority = 6610
	# name = ipv6.udperrors
	# title = IPv6 UDP Errors
	# dim RcvbufErrors name = RcvbufErrors
	# dim RcvbufErrors algorithm = incremental
	# dim RcvbufErrors multiplier = 1
	# dim RcvbufErrors divisor = 1
	# dim SndbufErrors name = SndbufErrors
	# dim SndbufErrors algorithm = incremental
	# dim SndbufErrors multiplier = -1
	# dim SndbufErrors divisor = 1
	# dim InErrors name = InErrors
	# dim InErrors algorithm = incremental
	# dim InErrors multiplier = 1
	# dim InErrors divisor = 1
	# dim NoPorts name = NoPorts
	# dim NoPorts algorithm = incremental
	# dim NoPorts multiplier = 1
	# dim NoPorts divisor = 1
	# dim InCsumErrors name = InCsumErrors
	# dim InCsumErrors algorithm = incremental
	# dim InCsumErrors multiplier = 1
	# dim InCsumErrors divisor = 1
	# dim IgnoredMulti name = IgnoredMulti
	# dim IgnoredMulti algorithm = incremental
	# dim IgnoredMulti multiplier = 1
	# dim IgnoredMulti divisor = 1

[ipv6.icmperrors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmperrors
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = errors/s
	# context = ipv6.icmperrors
	# priority = 6920
	# name = ipv6.icmperrors
	# title = IPv6 ICMP Errors
	# dim InErrors name = InErrors
	# dim InErrors algorithm = incremental
	# dim InErrors multiplier = 1
	# dim InErrors divisor = 1
	# dim OutErrors name = OutErrors
	# dim OutErrors algorithm = incremental
	# dim OutErrors multiplier = -1
	# dim OutErrors divisor = 1
	# dim InCsumErrors name = InCsumErrors
	# dim InCsumErrors algorithm = incremental
	# dim InCsumErrors multiplier = 1
	# dim InCsumErrors divisor = 1
	# dim InDestUnreachs name = InDestUnreachs
	# dim InDestUnreachs algorithm = incremental
	# dim InDestUnreachs multiplier = 1
	# dim InDestUnreachs divisor = 1
	# dim InPktTooBigs name = InPktTooBigs
	# dim InPktTooBigs algorithm = incremental
	# dim InPktTooBigs multiplier = 1
	# dim InPktTooBigs divisor = 1
	# dim InTimeExcds name = InTimeExcds
	# dim InTimeExcds algorithm = incremental
	# dim InTimeExcds multiplier = 1
	# dim InTimeExcds divisor = 1
	# dim InParmProblems name = InParmProblems
	# dim InParmProblems algorithm = incremental
	# dim InParmProblems multiplier = 1
	# dim InParmProblems divisor = 1
	# dim OutDestUnreachs name = OutDestUnreachs
	# dim OutDestUnreachs algorithm = incremental
	# dim OutDestUnreachs multiplier = -1
	# dim OutDestUnreachs divisor = 1
	# dim OutPktTooBigs name = OutPktTooBigs
	# dim OutPktTooBigs algorithm = incremental
	# dim OutPktTooBigs multiplier = -1
	# dim OutPktTooBigs divisor = 1
	# dim OutTimeExcds name = OutTimeExcds
	# dim OutTimeExcds algorithm = incremental
	# dim OutTimeExcds multiplier = -1
	# dim OutTimeExcds divisor = 1
	# dim OutParmProblems name = OutParmProblems
	# dim OutParmProblems algorithm = incremental
	# dim OutParmProblems multiplier = -1
	# dim OutParmProblems divisor = 1

[disk_await.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.sda
	# chart type = line
	# type = disk_await
	# family = sda
	# units = milliseconds/operation
	# context = disk.await
	# priority = 2005
	# name = disk_await.sda
	# title = Average Completed I/O Operation Time
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.sda
	# chart type = area
	# type = disk_avgsz
	# family = sda
	# units = KiB/operation
	# context = disk.avgsz
	# priority = 2006
	# name = disk_avgsz.sda
	# title = Average Completed I/O Operation Bandwidth
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.sda
	# chart type = line
	# type = disk_svctm
	# family = sda
	# units = milliseconds/operation
	# context = disk.svctm
	# priority = 2007
	# name = disk_svctm.sda
	# title = Average Service Time
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[disk_await.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_await.dm_0
	# chart type = line
	# type = disk_await
	# family = /var/lib/nova
	# units = milliseconds/operation
	# context = disk.await
	# priority = 2005
	# name = disk_await.vg00_var_lib_nova
	# title = Average Completed I/O Operation Time
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -1
	# dim writes divisor = 1

[disk_avgsz.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_avgsz.dm_0
	# chart type = area
	# type = disk_avgsz
	# family = /var/lib/nova
	# units = KiB/operation
	# context = disk.avgsz
	# priority = 2006
	# name = disk_avgsz.vg00_var_lib_nova
	# title = Average Completed I/O Operation Bandwidth
	# dim reads name = reads
	# dim reads algorithm = absolute
	# dim reads multiplier = 512
	# dim reads divisor = 1024
	# dim writes name = writes
	# dim writes algorithm = absolute
	# dim writes multiplier = -512
	# dim writes divisor = 1024

[disk_svctm.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_svctm.dm_0
	# chart type = line
	# type = disk_svctm
	# family = /var/lib/nova
	# units = milliseconds/operation
	# context = disk.svctm
	# priority = 2007
	# name = disk_svctm.vg00_var_lib_nova
	# title = Average Service Time
	# dim svctm name = svctm
	# dim svctm algorithm = absolute
	# dim svctm multiplier = 1
	# dim svctm divisor = 1

[netdata.runtime_memcached_local]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.runtime_memcached_local
	# chart type = line
	# type = netdata
	# family = python.d
	# units = ms
	# context = netdata.pythond_runtime
	# priority = 145000
	# name = netdata.runtime_memcached_local
	# title = Execution time for memcached_local
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[netdata.runtime_postfix_local]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.runtime_postfix_local
	# chart type = line
	# type = netdata
	# family = python.d
	# units = ms
	# context = netdata.pythond_runtime
	# priority = 145000
	# name = netdata.runtime_postfix_local
	# title = Execution time for postfix_local
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[netdata.runtime_sensors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.runtime_sensors
	# chart type = line
	# type = netdata
	# family = python.d
	# units = ms
	# context = netdata.pythond_runtime
	# priority = 145000
	# name = netdata.runtime_sensors
	# title = Execution time for sensors
	# dim run_time name = run time
	# dim run_time algorithm = absolute
	# dim run_time multiplier = 1
	# dim run_time divisor = 1

[memcached_local.decrement]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.decrement
	# chart type = stacked
	# type = memcached_local
	# family = decrement ops
	# units = requests
	# context = memcached.decrement
	# priority = 60011
	# name = memcached_local.decrement
	# title = Requests
	# dim decr_hits name = hits
	# dim decr_hits algorithm = absolute
	# dim decr_hits multiplier = 1
	# dim decr_hits divisor = 1
	# dim decr_misses name = misses
	# dim decr_misses algorithm = absolute
	# dim decr_misses multiplier = 1
	# dim decr_misses divisor = 1

[memcached_local.get]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.get
	# chart type = stacked
	# type = memcached_local
	# family = get ops
	# units = requests
	# context = memcached.get
	# priority = 60005
	# name = memcached_local.get
	# title = Requests
	# dim get_hits name = hits
	# dim get_hits algorithm = absolute
	# dim get_hits multiplier = 1
	# dim get_hits divisor = 1
	# dim get_misses name = misses
	# dim get_misses algorithm = absolute
	# dim get_misses multiplier = 1
	# dim get_misses divisor = 1

[memcached_local.cas]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.cas
	# chart type = stacked
	# type = memcached_local
	# family = check and set ops
	# units = requests
	# context = memcached.cas
	# priority = 60008
	# name = memcached_local.cas
	# title = Requests
	# dim cas_hits name = hits
	# dim cas_hits algorithm = absolute
	# dim cas_hits multiplier = 1
	# dim cas_hits divisor = 1
	# dim cas_misses name = misses
	# dim cas_misses algorithm = absolute
	# dim cas_misses multiplier = 1
	# dim cas_misses divisor = 1
	# dim cas_badval name = bad value
	# dim cas_badval algorithm = absolute
	# dim cas_badval multiplier = 1
	# dim cas_badval divisor = 1

[memcached_local.items]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.items
	# chart type = line
	# type = memcached_local
	# family = items
	# units = items
	# context = memcached.items
	# priority = 60003
	# name = memcached_local.items
	# title = Items
	# dim curr_items name = current
	# dim curr_items algorithm = absolute
	# dim curr_items multiplier = 1
	# dim curr_items divisor = 1
	# dim total_items name = total
	# dim total_items algorithm = absolute
	# dim total_items multiplier = 1
	# dim total_items divisor = 1

[memcached_local.cache]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.cache
	# chart type = stacked
	# type = memcached_local
	# family = cache
	# units = MiB
	# context = memcached.cache
	# priority = 60000
	# name = memcached_local.cache
	# title = Cache Size
	# dim avail name = available
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1048576
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1048576

[memcached_local.evicted_reclaimed]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.evicted_reclaimed
	# chart type = line
	# type = memcached_local
	# family = items
	# units = items
	# context = memcached.evicted_reclaimed
	# priority = 60004
	# name = memcached_local.evicted_reclaimed
	# title = Items
	# dim reclaimed name = reclaimed
	# dim reclaimed algorithm = absolute
	# dim reclaimed multiplier = 1
	# dim reclaimed divisor = 1
	# dim evictions name = evicted
	# dim evictions algorithm = absolute
	# dim evictions multiplier = 1
	# dim evictions divisor = 1

[memcached_local.connections]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.connections
	# chart type = line
	# type = memcached_local
	# family = connections
	# units = connections/s
	# context = memcached.connections
	# priority = 60002
	# name = memcached_local.connections
	# title = Connections
	# dim curr_connections name = current
	# dim curr_connections algorithm = incremental
	# dim curr_connections multiplier = 1
	# dim curr_connections divisor = 1
	# dim rejected_connections name = rejected
	# dim rejected_connections algorithm = incremental
	# dim rejected_connections multiplier = 1
	# dim rejected_connections divisor = 1
	# dim total_connections name = total
	# dim total_connections algorithm = incremental
	# dim total_connections multiplier = 1
	# dim total_connections divisor = 1

[memcached_local.get_rate]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.get_rate
	# chart type = line
	# type = memcached_local
	# family = get ops
	# units = requests/s
	# context = memcached.get_rate
	# priority = 60006
	# name = memcached_local.get_rate
	# title = Rate
	# dim cmd_get name = rate
	# dim cmd_get algorithm = incremental
	# dim cmd_get multiplier = 1
	# dim cmd_get divisor = 1

[memcached_local.touch_rate]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.touch_rate
	# chart type = line
	# type = memcached_local
	# family = touch ops
	# units = requests/s
	# context = memcached.touch_rate
	# priority = 60013
	# name = memcached_local.touch_rate
	# title = Rate
	# dim cmd_touch name = rate
	# dim cmd_touch algorithm = incremental
	# dim cmd_touch multiplier = 1
	# dim cmd_touch divisor = 1

[memcached_local.increment]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.increment
	# chart type = stacked
	# type = memcached_local
	# family = increment ops
	# units = requests
	# context = memcached.increment
	# priority = 60010
	# name = memcached_local.increment
	# title = Requests
	# dim incr_hits name = hits
	# dim incr_hits algorithm = absolute
	# dim incr_hits multiplier = 1
	# dim incr_hits divisor = 1
	# dim incr_misses name = misses
	# dim incr_misses algorithm = absolute
	# dim incr_misses multiplier = 1
	# dim incr_misses divisor = 1

[memcached_local.touch]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.touch
	# chart type = stacked
	# type = memcached_local
	# family = touch ops
	# units = requests
	# context = memcached.touch
	# priority = 60012
	# name = memcached_local.touch
	# title = Requests
	# dim touch_hits name = hits
	# dim touch_hits algorithm = absolute
	# dim touch_hits multiplier = 1
	# dim touch_hits divisor = 1
	# dim touch_misses name = misses
	# dim touch_misses algorithm = absolute
	# dim touch_misses multiplier = 1
	# dim touch_misses divisor = 1

[memcached_local.net]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.net
	# chart type = area
	# type = memcached_local
	# family = network
	# units = kilobits/s
	# context = memcached.net
	# priority = 60001
	# name = memcached_local.net
	# title = Network
	# dim bytes_read name = in
	# dim bytes_read algorithm = incremental
	# dim bytes_read multiplier = 8
	# dim bytes_read divisor = 1000
	# dim bytes_written name = out
	# dim bytes_written algorithm = incremental
	# dim bytes_written multiplier = -8
	# dim bytes_written divisor = 1000

[memcached_local.set_rate]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.set_rate
	# chart type = line
	# type = memcached_local
	# family = set ops
	# units = requests/s
	# context = memcached.set_rate
	# priority = 60007
	# name = memcached_local.set_rate
	# title = Rate
	# dim cmd_set name = rate
	# dim cmd_set algorithm = incremental
	# dim cmd_set multiplier = 1
	# dim cmd_set divisor = 1

[memcached_local.delete]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/memcached_local.delete
	# chart type = stacked
	# type = memcached_local
	# family = delete ops
	# units = requests
	# context = memcached.delete
	# priority = 60009
	# name = memcached_local.delete
	# title = Requests
	# dim delete_hits name = hits
	# dim delete_hits algorithm = absolute
	# dim delete_hits multiplier = 1
	# dim delete_hits divisor = 1
	# dim delete_misses name = misses
	# dim delete_misses algorithm = absolute
	# dim delete_misses multiplier = 1
	# dim delete_misses divisor = 1

[sensors.coretemp-isa-0000_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.coretemp_isa_0000_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60000
	# name = sensors.coretemp_isa_0000_temperature
	# title = coretemp-isa-0000 temperature
	# dim coretemp-isa-0000_temp1 name = Physical id 0
	# dim coretemp-isa-0000_temp1 algorithm = absolute
	# dim coretemp-isa-0000_temp1 multiplier = 1
	# dim coretemp-isa-0000_temp1 divisor = 1000
	# dim coretemp-isa-0000_temp2 name = Core 0
	# dim coretemp-isa-0000_temp2 algorithm = absolute
	# dim coretemp-isa-0000_temp2 multiplier = 1
	# dim coretemp-isa-0000_temp2 divisor = 1000
	# dim coretemp-isa-0000_temp3 name = Core 1
	# dim coretemp-isa-0000_temp3 algorithm = absolute
	# dim coretemp-isa-0000_temp3 multiplier = 1
	# dim coretemp-isa-0000_temp3 divisor = 1000
	# dim coretemp-isa-0000_temp4 name = Core 2
	# dim coretemp-isa-0000_temp4 algorithm = absolute
	# dim coretemp-isa-0000_temp4 multiplier = 1
	# dim coretemp-isa-0000_temp4 divisor = 1000
	# dim coretemp-isa-0000_temp5 name = Core 3
	# dim coretemp-isa-0000_temp5 algorithm = absolute
	# dim coretemp-isa-0000_temp5 multiplier = 1
	# dim coretemp-isa-0000_temp5 divisor = 1000
	# dim coretemp-isa-0000_temp6 name = Core 4
	# dim coretemp-isa-0000_temp6 algorithm = absolute
	# dim coretemp-isa-0000_temp6 multiplier = 1
	# dim coretemp-isa-0000_temp6 divisor = 1000
	# dim coretemp-isa-0000_temp7 name = Core 5
	# dim coretemp-isa-0000_temp7 algorithm = absolute
	# dim coretemp-isa-0000_temp7 multiplier = 1
	# dim coretemp-isa-0000_temp7 divisor = 1000
	# dim coretemp-isa-0000_temp10 name = Core 8
	# dim coretemp-isa-0000_temp10 algorithm = absolute
	# dim coretemp-isa-0000_temp10 multiplier = 1
	# dim coretemp-isa-0000_temp10 divisor = 1000
	# dim coretemp-isa-0000_temp11 name = Core 9
	# dim coretemp-isa-0000_temp11 algorithm = absolute
	# dim coretemp-isa-0000_temp11 multiplier = 1
	# dim coretemp-isa-0000_temp11 divisor = 1000
	# dim coretemp-isa-0000_temp12 name = Core 10
	# dim coretemp-isa-0000_temp12 algorithm = absolute
	# dim coretemp-isa-0000_temp12 multiplier = 1
	# dim coretemp-isa-0000_temp12 divisor = 1000
	# dim coretemp-isa-0000_temp13 name = Core 11
	# dim coretemp-isa-0000_temp13 algorithm = absolute
	# dim coretemp-isa-0000_temp13 multiplier = 1
	# dim coretemp-isa-0000_temp13 divisor = 1000
	# dim coretemp-isa-0000_temp14 name = Core 12
	# dim coretemp-isa-0000_temp14 algorithm = absolute
	# dim coretemp-isa-0000_temp14 multiplier = 1
	# dim coretemp-isa-0000_temp14 divisor = 1000
	# dim coretemp-isa-0000_temp15 name = Core 13
	# dim coretemp-isa-0000_temp15 algorithm = absolute
	# dim coretemp-isa-0000_temp15 multiplier = 1
	# dim coretemp-isa-0000_temp15 divisor = 1000
	# dim coretemp-isa-0000_temp18 name = Core 16
	# dim coretemp-isa-0000_temp18 algorithm = absolute
	# dim coretemp-isa-0000_temp18 multiplier = 1
	# dim coretemp-isa-0000_temp18 divisor = 1000
	# dim coretemp-isa-0000_temp19 name = Core 17
	# dim coretemp-isa-0000_temp19 algorithm = absolute
	# dim coretemp-isa-0000_temp19 multiplier = 1
	# dim coretemp-isa-0000_temp19 divisor = 1000
	# dim coretemp-isa-0000_temp20 name = Core 18
	# dim coretemp-isa-0000_temp20 algorithm = absolute
	# dim coretemp-isa-0000_temp20 multiplier = 1
	# dim coretemp-isa-0000_temp20 divisor = 1000
	# dim coretemp-isa-0000_temp21 name = Core 19
	# dim coretemp-isa-0000_temp21 algorithm = absolute
	# dim coretemp-isa-0000_temp21 multiplier = 1
	# dim coretemp-isa-0000_temp21 divisor = 1000
	# dim coretemp-isa-0000_temp22 name = Core 20
	# dim coretemp-isa-0000_temp22 algorithm = absolute
	# dim coretemp-isa-0000_temp22 multiplier = 1
	# dim coretemp-isa-0000_temp22 divisor = 1000
	# dim coretemp-isa-0000_temp23 name = Core 21
	# dim coretemp-isa-0000_temp23 algorithm = absolute
	# dim coretemp-isa-0000_temp23 multiplier = 1
	# dim coretemp-isa-0000_temp23 divisor = 1000
	# dim coretemp-isa-0000_temp26 name = Core 24
	# dim coretemp-isa-0000_temp26 algorithm = absolute
	# dim coretemp-isa-0000_temp26 multiplier = 1
	# dim coretemp-isa-0000_temp26 divisor = 1000
	# dim coretemp-isa-0000_temp27 name = Core 25
	# dim coretemp-isa-0000_temp27 algorithm = absolute
	# dim coretemp-isa-0000_temp27 multiplier = 1
	# dim coretemp-isa-0000_temp27 divisor = 1000
	# dim coretemp-isa-0000_temp28 name = Core 26
	# dim coretemp-isa-0000_temp28 algorithm = absolute
	# dim coretemp-isa-0000_temp28 multiplier = 1
	# dim coretemp-isa-0000_temp28 divisor = 1000
	# dim coretemp-isa-0000_temp29 name = Core 27
	# dim coretemp-isa-0000_temp29 algorithm = absolute
	# dim coretemp-isa-0000_temp29 multiplier = 1
	# dim coretemp-isa-0000_temp29 divisor = 1000
	# dim coretemp-isa-0000_temp30 name = Core 28
	# dim coretemp-isa-0000_temp30 algorithm = absolute
	# dim coretemp-isa-0000_temp30 multiplier = 1
	# dim coretemp-isa-0000_temp30 divisor = 1000
	# dim coretemp-isa-0000_temp31 name = Core 29
	# dim coretemp-isa-0000_temp31 algorithm = absolute
	# dim coretemp-isa-0000_temp31 multiplier = 1
	# dim coretemp-isa-0000_temp31 divisor = 1000

[sensors.coretemp-isa-0002_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.coretemp_isa_0002_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60002
	# name = sensors.coretemp_isa_0002_temperature
	# title = coretemp-isa-0002 temperature
	# dim coretemp-isa-0002_temp1 name = Physical id 2
	# dim coretemp-isa-0002_temp1 algorithm = absolute
	# dim coretemp-isa-0002_temp1 multiplier = 1
	# dim coretemp-isa-0002_temp1 divisor = 1000
	# dim coretemp-isa-0002_temp2 name = Core 0
	# dim coretemp-isa-0002_temp2 algorithm = absolute
	# dim coretemp-isa-0002_temp2 multiplier = 1
	# dim coretemp-isa-0002_temp2 divisor = 1000
	# dim coretemp-isa-0002_temp3 name = Core 1
	# dim coretemp-isa-0002_temp3 algorithm = absolute
	# dim coretemp-isa-0002_temp3 multiplier = 1
	# dim coretemp-isa-0002_temp3 divisor = 1000
	# dim coretemp-isa-0002_temp4 name = Core 2
	# dim coretemp-isa-0002_temp4 algorithm = absolute
	# dim coretemp-isa-0002_temp4 multiplier = 1
	# dim coretemp-isa-0002_temp4 divisor = 1000
	# dim coretemp-isa-0002_temp5 name = Core 3
	# dim coretemp-isa-0002_temp5 algorithm = absolute
	# dim coretemp-isa-0002_temp5 multiplier = 1
	# dim coretemp-isa-0002_temp5 divisor = 1000
	# dim coretemp-isa-0002_temp6 name = Core 4
	# dim coretemp-isa-0002_temp6 algorithm = absolute
	# dim coretemp-isa-0002_temp6 multiplier = 1
	# dim coretemp-isa-0002_temp6 divisor = 1000
	# dim coretemp-isa-0002_temp7 name = Core 5
	# dim coretemp-isa-0002_temp7 algorithm = absolute
	# dim coretemp-isa-0002_temp7 multiplier = 1
	# dim coretemp-isa-0002_temp7 divisor = 1000
	# dim coretemp-isa-0002_temp10 name = Core 8
	# dim coretemp-isa-0002_temp10 algorithm = absolute
	# dim coretemp-isa-0002_temp10 multiplier = 1
	# dim coretemp-isa-0002_temp10 divisor = 1000
	# dim coretemp-isa-0002_temp11 name = Core 9
	# dim coretemp-isa-0002_temp11 algorithm = absolute
	# dim coretemp-isa-0002_temp11 multiplier = 1
	# dim coretemp-isa-0002_temp11 divisor = 1000
	# dim coretemp-isa-0002_temp12 name = Core 10
	# dim coretemp-isa-0002_temp12 algorithm = absolute
	# dim coretemp-isa-0002_temp12 multiplier = 1
	# dim coretemp-isa-0002_temp12 divisor = 1000
	# dim coretemp-isa-0002_temp13 name = Core 11
	# dim coretemp-isa-0002_temp13 algorithm = absolute
	# dim coretemp-isa-0002_temp13 multiplier = 1
	# dim coretemp-isa-0002_temp13 divisor = 1000
	# dim coretemp-isa-0002_temp14 name = Core 12
	# dim coretemp-isa-0002_temp14 algorithm = absolute
	# dim coretemp-isa-0002_temp14 multiplier = 1
	# dim coretemp-isa-0002_temp14 divisor = 1000
	# dim coretemp-isa-0002_temp15 name = Core 13
	# dim coretemp-isa-0002_temp15 algorithm = absolute
	# dim coretemp-isa-0002_temp15 multiplier = 1
	# dim coretemp-isa-0002_temp15 divisor = 1000
	# dim coretemp-isa-0002_temp18 name = Core 16
	# dim coretemp-isa-0002_temp18 algorithm = absolute
	# dim coretemp-isa-0002_temp18 multiplier = 1
	# dim coretemp-isa-0002_temp18 divisor = 1000
	# dim coretemp-isa-0002_temp19 name = Core 17
	# dim coretemp-isa-0002_temp19 algorithm = absolute
	# dim coretemp-isa-0002_temp19 multiplier = 1
	# dim coretemp-isa-0002_temp19 divisor = 1000
	# dim coretemp-isa-0002_temp20 name = Core 18
	# dim coretemp-isa-0002_temp20 algorithm = absolute
	# dim coretemp-isa-0002_temp20 multiplier = 1
	# dim coretemp-isa-0002_temp20 divisor = 1000
	# dim coretemp-isa-0002_temp21 name = Core 19
	# dim coretemp-isa-0002_temp21 algorithm = absolute
	# dim coretemp-isa-0002_temp21 multiplier = 1
	# dim coretemp-isa-0002_temp21 divisor = 1000
	# dim coretemp-isa-0002_temp22 name = Core 20
	# dim coretemp-isa-0002_temp22 algorithm = absolute
	# dim coretemp-isa-0002_temp22 multiplier = 1
	# dim coretemp-isa-0002_temp22 divisor = 1000
	# dim coretemp-isa-0002_temp23 name = Core 21
	# dim coretemp-isa-0002_temp23 algorithm = absolute
	# dim coretemp-isa-0002_temp23 multiplier = 1
	# dim coretemp-isa-0002_temp23 divisor = 1000
	# dim coretemp-isa-0002_temp26 name = Core 24
	# dim coretemp-isa-0002_temp26 algorithm = absolute
	# dim coretemp-isa-0002_temp26 multiplier = 1
	# dim coretemp-isa-0002_temp26 divisor = 1000
	# dim coretemp-isa-0002_temp27 name = Core 25
	# dim coretemp-isa-0002_temp27 algorithm = absolute
	# dim coretemp-isa-0002_temp27 multiplier = 1
	# dim coretemp-isa-0002_temp27 divisor = 1000
	# dim coretemp-isa-0002_temp28 name = Core 26
	# dim coretemp-isa-0002_temp28 algorithm = absolute
	# dim coretemp-isa-0002_temp28 multiplier = 1
	# dim coretemp-isa-0002_temp28 divisor = 1000
	# dim coretemp-isa-0002_temp29 name = Core 27
	# dim coretemp-isa-0002_temp29 algorithm = absolute
	# dim coretemp-isa-0002_temp29 multiplier = 1
	# dim coretemp-isa-0002_temp29 divisor = 1000
	# dim coretemp-isa-0002_temp30 name = Core 28
	# dim coretemp-isa-0002_temp30 algorithm = absolute
	# dim coretemp-isa-0002_temp30 multiplier = 1
	# dim coretemp-isa-0002_temp30 divisor = 1000
	# dim coretemp-isa-0002_temp31 name = Core 29
	# dim coretemp-isa-0002_temp31 algorithm = absolute
	# dim coretemp-isa-0002_temp31 multiplier = 1
	# dim coretemp-isa-0002_temp31 divisor = 1000

[sensors.coretemp-isa-0001_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.coretemp_isa_0001_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60001
	# name = sensors.coretemp_isa_0001_temperature
	# title = coretemp-isa-0001 temperature
	# dim coretemp-isa-0001_temp1 name = Physical id 1
	# dim coretemp-isa-0001_temp1 algorithm = absolute
	# dim coretemp-isa-0001_temp1 multiplier = 1
	# dim coretemp-isa-0001_temp1 divisor = 1000
	# dim coretemp-isa-0001_temp2 name = Core 0
	# dim coretemp-isa-0001_temp2 algorithm = absolute
	# dim coretemp-isa-0001_temp2 multiplier = 1
	# dim coretemp-isa-0001_temp2 divisor = 1000
	# dim coretemp-isa-0001_temp3 name = Core 1
	# dim coretemp-isa-0001_temp3 algorithm = absolute
	# dim coretemp-isa-0001_temp3 multiplier = 1
	# dim coretemp-isa-0001_temp3 divisor = 1000
	# dim coretemp-isa-0001_temp4 name = Core 2
	# dim coretemp-isa-0001_temp4 algorithm = absolute
	# dim coretemp-isa-0001_temp4 multiplier = 1
	# dim coretemp-isa-0001_temp4 divisor = 1000
	# dim coretemp-isa-0001_temp5 name = Core 3
	# dim coretemp-isa-0001_temp5 algorithm = absolute
	# dim coretemp-isa-0001_temp5 multiplier = 1
	# dim coretemp-isa-0001_temp5 divisor = 1000
	# dim coretemp-isa-0001_temp6 name = Core 4
	# dim coretemp-isa-0001_temp6 algorithm = absolute
	# dim coretemp-isa-0001_temp6 multiplier = 1
	# dim coretemp-isa-0001_temp6 divisor = 1000
	# dim coretemp-isa-0001_temp7 name = Core 5
	# dim coretemp-isa-0001_temp7 algorithm = absolute
	# dim coretemp-isa-0001_temp7 multiplier = 1
	# dim coretemp-isa-0001_temp7 divisor = 1000
	# dim coretemp-isa-0001_temp10 name = Core 8
	# dim coretemp-isa-0001_temp10 algorithm = absolute
	# dim coretemp-isa-0001_temp10 multiplier = 1
	# dim coretemp-isa-0001_temp10 divisor = 1000
	# dim coretemp-isa-0001_temp11 name = Core 9
	# dim coretemp-isa-0001_temp11 algorithm = absolute
	# dim coretemp-isa-0001_temp11 multiplier = 1
	# dim coretemp-isa-0001_temp11 divisor = 1000
	# dim coretemp-isa-0001_temp12 name = Core 10
	# dim coretemp-isa-0001_temp12 algorithm = absolute
	# dim coretemp-isa-0001_temp12 multiplier = 1
	# dim coretemp-isa-0001_temp12 divisor = 1000
	# dim coretemp-isa-0001_temp13 name = Core 11
	# dim coretemp-isa-0001_temp13 algorithm = absolute
	# dim coretemp-isa-0001_temp13 multiplier = 1
	# dim coretemp-isa-0001_temp13 divisor = 1000
	# dim coretemp-isa-0001_temp14 name = Core 12
	# dim coretemp-isa-0001_temp14 algorithm = absolute
	# dim coretemp-isa-0001_temp14 multiplier = 1
	# dim coretemp-isa-0001_temp14 divisor = 1000
	# dim coretemp-isa-0001_temp15 name = Core 13
	# dim coretemp-isa-0001_temp15 algorithm = absolute
	# dim coretemp-isa-0001_temp15 multiplier = 1
	# dim coretemp-isa-0001_temp15 divisor = 1000
	# dim coretemp-isa-0001_temp18 name = Core 16
	# dim coretemp-isa-0001_temp18 algorithm = absolute
	# dim coretemp-isa-0001_temp18 multiplier = 1
	# dim coretemp-isa-0001_temp18 divisor = 1000
	# dim coretemp-isa-0001_temp19 name = Core 17
	# dim coretemp-isa-0001_temp19 algorithm = absolute
	# dim coretemp-isa-0001_temp19 multiplier = 1
	# dim coretemp-isa-0001_temp19 divisor = 1000
	# dim coretemp-isa-0001_temp20 name = Core 18
	# dim coretemp-isa-0001_temp20 algorithm = absolute
	# dim coretemp-isa-0001_temp20 multiplier = 1
	# dim coretemp-isa-0001_temp20 divisor = 1000
	# dim coretemp-isa-0001_temp21 name = Core 19
	# dim coretemp-isa-0001_temp21 algorithm = absolute
	# dim coretemp-isa-0001_temp21 multiplier = 1
	# dim coretemp-isa-0001_temp21 divisor = 1000
	# dim coretemp-isa-0001_temp22 name = Core 20
	# dim coretemp-isa-0001_temp22 algorithm = absolute
	# dim coretemp-isa-0001_temp22 multiplier = 1
	# dim coretemp-isa-0001_temp22 divisor = 1000
	# dim coretemp-isa-0001_temp23 name = Core 21
	# dim coretemp-isa-0001_temp23 algorithm = absolute
	# dim coretemp-isa-0001_temp23 multiplier = 1
	# dim coretemp-isa-0001_temp23 divisor = 1000
	# dim coretemp-isa-0001_temp26 name = Core 24
	# dim coretemp-isa-0001_temp26 algorithm = absolute
	# dim coretemp-isa-0001_temp26 multiplier = 1
	# dim coretemp-isa-0001_temp26 divisor = 1000
	# dim coretemp-isa-0001_temp27 name = Core 25
	# dim coretemp-isa-0001_temp27 algorithm = absolute
	# dim coretemp-isa-0001_temp27 multiplier = 1
	# dim coretemp-isa-0001_temp27 divisor = 1000
	# dim coretemp-isa-0001_temp28 name = Core 26
	# dim coretemp-isa-0001_temp28 algorithm = absolute
	# dim coretemp-isa-0001_temp28 multiplier = 1
	# dim coretemp-isa-0001_temp28 divisor = 1000
	# dim coretemp-isa-0001_temp29 name = Core 27
	# dim coretemp-isa-0001_temp29 algorithm = absolute
	# dim coretemp-isa-0001_temp29 multiplier = 1
	# dim coretemp-isa-0001_temp29 divisor = 1000
	# dim coretemp-isa-0001_temp30 name = Core 28
	# dim coretemp-isa-0001_temp30 algorithm = absolute
	# dim coretemp-isa-0001_temp30 multiplier = 1
	# dim coretemp-isa-0001_temp30 divisor = 1000
	# dim coretemp-isa-0001_temp31 name = Core 29
	# dim coretemp-isa-0001_temp31 algorithm = absolute
	# dim coretemp-isa-0001_temp31 multiplier = 1
	# dim coretemp-isa-0001_temp31 divisor = 1000

[sensors.tg3-pci-0300_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.tg3_pci_0300_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60004
	# name = sensors.tg3_pci_0300_temperature
	# title = tg3-pci-0300 temperature
	# dim tg3-pci-0300_temp1 name = temp1
	# dim tg3-pci-0300_temp1 algorithm = absolute
	# dim tg3-pci-0300_temp1 multiplier = 1
	# dim tg3-pci-0300_temp1 divisor = 1000

[sensors.coretemp-isa-0003_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.coretemp_isa_0003_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60003
	# name = sensors.coretemp_isa_0003_temperature
	# title = coretemp-isa-0003 temperature
	# dim coretemp-isa-0003_temp1 name = Physical id 3
	# dim coretemp-isa-0003_temp1 algorithm = absolute
	# dim coretemp-isa-0003_temp1 multiplier = 1
	# dim coretemp-isa-0003_temp1 divisor = 1000
	# dim coretemp-isa-0003_temp2 name = Core 0
	# dim coretemp-isa-0003_temp2 algorithm = absolute
	# dim coretemp-isa-0003_temp2 multiplier = 1
	# dim coretemp-isa-0003_temp2 divisor = 1000
	# dim coretemp-isa-0003_temp3 name = Core 1
	# dim coretemp-isa-0003_temp3 algorithm = absolute
	# dim coretemp-isa-0003_temp3 multiplier = 1
	# dim coretemp-isa-0003_temp3 divisor = 1000
	# dim coretemp-isa-0003_temp4 name = Core 2
	# dim coretemp-isa-0003_temp4 algorithm = absolute
	# dim coretemp-isa-0003_temp4 multiplier = 1
	# dim coretemp-isa-0003_temp4 divisor = 1000
	# dim coretemp-isa-0003_temp5 name = Core 3
	# dim coretemp-isa-0003_temp5 algorithm = absolute
	# dim coretemp-isa-0003_temp5 multiplier = 1
	# dim coretemp-isa-0003_temp5 divisor = 1000
	# dim coretemp-isa-0003_temp6 name = Core 4
	# dim coretemp-isa-0003_temp6 algorithm = absolute
	# dim coretemp-isa-0003_temp6 multiplier = 1
	# dim coretemp-isa-0003_temp6 divisor = 1000
	# dim coretemp-isa-0003_temp7 name = Core 5
	# dim coretemp-isa-0003_temp7 algorithm = absolute
	# dim coretemp-isa-0003_temp7 multiplier = 1
	# dim coretemp-isa-0003_temp7 divisor = 1000
	# dim coretemp-isa-0003_temp10 name = Core 8
	# dim coretemp-isa-0003_temp10 algorithm = absolute
	# dim coretemp-isa-0003_temp10 multiplier = 1
	# dim coretemp-isa-0003_temp10 divisor = 1000
	# dim coretemp-isa-0003_temp11 name = Core 9
	# dim coretemp-isa-0003_temp11 algorithm = absolute
	# dim coretemp-isa-0003_temp11 multiplier = 1
	# dim coretemp-isa-0003_temp11 divisor = 1000
	# dim coretemp-isa-0003_temp12 name = Core 10
	# dim coretemp-isa-0003_temp12 algorithm = absolute
	# dim coretemp-isa-0003_temp12 multiplier = 1
	# dim coretemp-isa-0003_temp12 divisor = 1000
	# dim coretemp-isa-0003_temp13 name = Core 11
	# dim coretemp-isa-0003_temp13 algorithm = absolute
	# dim coretemp-isa-0003_temp13 multiplier = 1
	# dim coretemp-isa-0003_temp13 divisor = 1000
	# dim coretemp-isa-0003_temp14 name = Core 12
	# dim coretemp-isa-0003_temp14 algorithm = absolute
	# dim coretemp-isa-0003_temp14 multiplier = 1
	# dim coretemp-isa-0003_temp14 divisor = 1000
	# dim coretemp-isa-0003_temp15 name = Core 13
	# dim coretemp-isa-0003_temp15 algorithm = absolute
	# dim coretemp-isa-0003_temp15 multiplier = 1
	# dim coretemp-isa-0003_temp15 divisor = 1000
	# dim coretemp-isa-0003_temp18 name = Core 16
	# dim coretemp-isa-0003_temp18 algorithm = absolute
	# dim coretemp-isa-0003_temp18 multiplier = 1
	# dim coretemp-isa-0003_temp18 divisor = 1000
	# dim coretemp-isa-0003_temp19 name = Core 17
	# dim coretemp-isa-0003_temp19 algorithm = absolute
	# dim coretemp-isa-0003_temp19 multiplier = 1
	# dim coretemp-isa-0003_temp19 divisor = 1000
	# dim coretemp-isa-0003_temp20 name = Core 18
	# dim coretemp-isa-0003_temp20 algorithm = absolute
	# dim coretemp-isa-0003_temp20 multiplier = 1
	# dim coretemp-isa-0003_temp20 divisor = 1000
	# dim coretemp-isa-0003_temp21 name = Core 19
	# dim coretemp-isa-0003_temp21 algorithm = absolute
	# dim coretemp-isa-0003_temp21 multiplier = 1
	# dim coretemp-isa-0003_temp21 divisor = 1000
	# dim coretemp-isa-0003_temp22 name = Core 20
	# dim coretemp-isa-0003_temp22 algorithm = absolute
	# dim coretemp-isa-0003_temp22 multiplier = 1
	# dim coretemp-isa-0003_temp22 divisor = 1000
	# dim coretemp-isa-0003_temp23 name = Core 21
	# dim coretemp-isa-0003_temp23 algorithm = absolute
	# dim coretemp-isa-0003_temp23 multiplier = 1
	# dim coretemp-isa-0003_temp23 divisor = 1000
	# dim coretemp-isa-0003_temp26 name = Core 24
	# dim coretemp-isa-0003_temp26 algorithm = absolute
	# dim coretemp-isa-0003_temp26 multiplier = 1
	# dim coretemp-isa-0003_temp26 divisor = 1000
	# dim coretemp-isa-0003_temp27 name = Core 25
	# dim coretemp-isa-0003_temp27 algorithm = absolute
	# dim coretemp-isa-0003_temp27 multiplier = 1
	# dim coretemp-isa-0003_temp27 divisor = 1000
	# dim coretemp-isa-0003_temp28 name = Core 26
	# dim coretemp-isa-0003_temp28 algorithm = absolute
	# dim coretemp-isa-0003_temp28 multiplier = 1
	# dim coretemp-isa-0003_temp28 divisor = 1000
	# dim coretemp-isa-0003_temp29 name = Core 27
	# dim coretemp-isa-0003_temp29 algorithm = absolute
	# dim coretemp-isa-0003_temp29 multiplier = 1
	# dim coretemp-isa-0003_temp29 divisor = 1000
	# dim coretemp-isa-0003_temp30 name = Core 28
	# dim coretemp-isa-0003_temp30 algorithm = absolute
	# dim coretemp-isa-0003_temp30 multiplier = 1
	# dim coretemp-isa-0003_temp30 divisor = 1000
	# dim coretemp-isa-0003_temp31 name = Core 29
	# dim coretemp-isa-0003_temp31 algorithm = absolute
	# dim coretemp-isa-0003_temp31 multiplier = 1
	# dim coretemp-isa-0003_temp31 divisor = 1000

[disk_space._var_lib_glance]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_space._var_lib_glance
	# chart type = stacked
	# type = disk_space
	# family = /var/lib/glance
	# units = GiB
	# context = disk.space
	# priority = 2023
	# name = disk_space._var_lib_glance
	# title = Disk Space Usage for /var/lib/glance [/dev/mapper/VG00-var_lib_nova]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 4096
	# dim avail divisor = 1073741824
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 4096
	# dim used divisor = 1073741824
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 4096
	# dim reserved_for_root divisor = 1073741824

[disk_inodes._var_lib_glance]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_inodes._var_lib_glance
	# chart type = stacked
	# type = disk_inodes
	# family = /var/lib/glance
	# units = inodes
	# context = disk.inodes
	# priority = 2024
	# name = disk_inodes._var_lib_glance
	# title = Disk Files (inodes) Usage for /var/lib/glance [/dev/mapper/VG00-var_lib_nova]
	# dim avail name = avail
	# dim avail algorithm = absolute
	# dim avail multiplier = 1
	# dim avail divisor = 1
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1
	# dim reserved_for_root name = reserved for root
	# dim reserved_for_root algorithm = absolute
	# dim reserved_for_root multiplier = 1
	# dim reserved_for_root divisor = 1

[apps.uptime]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.uptime
	# chart type = line
	# type = apps
	# family = processes
	# units = seconds
	# context = apps.uptime
	# priority = 20008
	# name = apps.uptime
	# title = Apps Carried Over Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[apps.uptime_min]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.uptime_min
	# chart type = line
	# type = apps
	# family = processes
	# units = seconds
	# context = apps.uptime_min
	# priority = 20009
	# name = apps.uptime_min
	# title = Apps Minimum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[apps.uptime_avg]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.uptime_avg
	# chart type = line
	# type = apps
	# family = processes
	# units = seconds
	# context = apps.uptime_avg
	# priority = 20010
	# name = apps.uptime_avg
	# title = Apps Average Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[apps.uptime_max]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/apps.uptime_max
	# chart type = line
	# type = apps
	# family = processes
	# units = seconds
	# context = apps.uptime_max
	# priority = 20011
	# name = apps.uptime_max
	# title = Apps Maximum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim apps.plugin name = apps.plugin
	# dim apps.plugin algorithm = absolute
	# dim apps.plugin multiplier = 1
	# dim apps.plugin divisor = 1
	# dim python.d.plugin name = python.d.plugin
	# dim python.d.plugin algorithm = absolute
	# dim python.d.plugin multiplier = 1
	# dim python.d.plugin divisor = 1
	# dim tc-qos-helper name = tc-qos-helper
	# dim tc-qos-helper algorithm = absolute
	# dim tc-qos-helper multiplier = 1
	# dim tc-qos-helper divisor = 1
	# dim go.d.plugin name = go.d.plugin
	# dim go.d.plugin algorithm = absolute
	# dim go.d.plugin multiplier = 1
	# dim go.d.plugin divisor = 1
	# dim httpd name = httpd
	# dim httpd algorithm = absolute
	# dim httpd multiplier = 1
	# dim httpd divisor = 1
	# dim sql name = sql
	# dim sql algorithm = absolute
	# dim sql multiplier = 1
	# dim sql divisor = 1
	# dim nosql name = nosql
	# dim nosql algorithm = absolute
	# dim nosql multiplier = 1
	# dim nosql divisor = 1
	# dim email name = email
	# dim email algorithm = absolute
	# dim email multiplier = 1
	# dim email divisor = 1
	# dim modem name = modem
	# dim modem algorithm = absolute
	# dim modem multiplier = 1
	# dim modem divisor = 1
	# dim logs name = logs
	# dim logs algorithm = absolute
	# dim logs multiplier = 1
	# dim logs divisor = 1
	# dim nfs name = nfs
	# dim nfs algorithm = absolute
	# dim nfs multiplier = 1
	# dim nfs divisor = 1
	# dim VMs name = VMs
	# dim VMs algorithm = absolute
	# dim VMs multiplier = 1
	# dim VMs divisor = 1
	# dim ssh name = ssh
	# dim ssh algorithm = absolute
	# dim ssh multiplier = 1
	# dim ssh divisor = 1
	# dim print name = print
	# dim print algorithm = absolute
	# dim print multiplier = 1
	# dim print divisor = 1
	# dim time name = time
	# dim time algorithm = absolute
	# dim time multiplier = 1
	# dim time divisor = 1
	# dim dhcp name = dhcp
	# dim dhcp algorithm = absolute
	# dim dhcp multiplier = 1
	# dim dhcp divisor = 1
	# dim cron name = cron
	# dim cron algorithm = absolute
	# dim cron multiplier = 1
	# dim cron divisor = 1
	# dim media name = media
	# dim media algorithm = absolute
	# dim media multiplier = 1
	# dim media divisor = 1
	# dim ksmd name = ksmd
	# dim ksmd algorithm = absolute
	# dim ksmd multiplier = 1
	# dim ksmd divisor = 1
	# dim system name = system
	# dim system algorithm = absolute
	# dim system multiplier = 1
	# dim system divisor = 1
	# dim kernel name = kernel
	# dim kernel algorithm = absolute
	# dim kernel multiplier = 1
	# dim kernel divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim other name = other
	# dim other algorithm = absolute
	# dim other multiplier = 1
	# dim other divisor = 1

[users.uptime]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.uptime
	# chart type = line
	# type = users
	# family = processes
	# units = seconds
	# context = users.uptime
	# priority = 20008
	# name = users.uptime
	# title = Users Carried Over Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[users.uptime_min]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.uptime_min
	# chart type = line
	# type = users
	# family = processes
	# units = seconds
	# context = users.uptime_min
	# priority = 20009
	# name = users.uptime_min
	# title = Users Minimum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[users.uptime_avg]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.uptime_avg
	# chart type = line
	# type = users
	# family = processes
	# units = seconds
	# context = users.uptime_avg
	# priority = 20010
	# name = users.uptime_avg
	# title = Users Average Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[users.uptime_max]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/users.uptime_max
	# chart type = line
	# type = users
	# family = processes
	# units = seconds
	# context = users.uptime_max
	# priority = 20011
	# name = users.uptime_max
	# title = Users Maximum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim openvswitch name = openvswitch
	# dim openvswitch algorithm = absolute
	# dim openvswitch multiplier = 1
	# dim openvswitch divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim nobody name = nobody
	# dim nobody algorithm = absolute
	# dim nobody multiplier = 1
	# dim nobody divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[groups.uptime]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.uptime
	# chart type = line
	# type = groups
	# family = processes
	# units = seconds
	# context = groups.uptime
	# priority = 20008
	# name = groups.uptime
	# title = User Groups Carried Over Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[groups.uptime_min]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.uptime_min
	# chart type = line
	# type = groups
	# family = processes
	# units = seconds
	# context = groups.uptime_min
	# priority = 20009
	# name = groups.uptime_min
	# title = User Groups Minimum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[groups.uptime_avg]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.uptime_avg
	# chart type = line
	# type = groups
	# family = processes
	# units = seconds
	# context = groups.uptime_avg
	# priority = 20010
	# name = groups.uptime_avg
	# title = User Groups Average Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[groups.uptime_max]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/groups.uptime_max
	# chart type = line
	# type = groups
	# family = processes
	# units = seconds
	# context = groups.uptime_max
	# priority = 20011
	# name = groups.uptime_max
	# title = User Groups Maximum Uptime
	# dim netdata name = netdata
	# dim netdata algorithm = absolute
	# dim netdata multiplier = 1
	# dim netdata divisor = 1
	# dim libstoragemgmt name = libstoragemgmt
	# dim libstoragemgmt algorithm = absolute
	# dim libstoragemgmt multiplier = 1
	# dim libstoragemgmt divisor = 1
	# dim dbus name = dbus
	# dim dbus algorithm = absolute
	# dim dbus multiplier = 1
	# dim dbus divisor = 1
	# dim rpc name = rpc
	# dim rpc algorithm = absolute
	# dim rpc multiplier = 1
	# dim rpc divisor = 1
	# dim polkitd name = polkitd
	# dim polkitd algorithm = absolute
	# dim polkitd multiplier = 1
	# dim polkitd divisor = 1
	# dim chrony name = chrony
	# dim chrony algorithm = absolute
	# dim chrony multiplier = 1
	# dim chrony divisor = 1
	# dim avahi name = avahi
	# dim avahi algorithm = absolute
	# dim avahi multiplier = 1
	# dim avahi divisor = 1
	# dim hugetlbfs name = hugetlbfs
	# dim hugetlbfs algorithm = absolute
	# dim hugetlbfs multiplier = 1
	# dim hugetlbfs divisor = 1
	# dim memcached name = memcached
	# dim memcached algorithm = absolute
	# dim memcached multiplier = 1
	# dim memcached divisor = 1
	# dim rpcuser name = rpcuser
	# dim rpcuser algorithm = absolute
	# dim rpcuser multiplier = 1
	# dim rpcuser divisor = 1
	# dim mysql name = mysql
	# dim mysql algorithm = absolute
	# dim mysql multiplier = 1
	# dim mysql divisor = 1
	# dim epmd name = epmd
	# dim epmd algorithm = absolute
	# dim epmd multiplier = 1
	# dim epmd divisor = 1
	# dim glance name = glance
	# dim glance algorithm = absolute
	# dim glance multiplier = 1
	# dim glance divisor = 1
	# dim rabbitmq name = rabbitmq
	# dim rabbitmq algorithm = absolute
	# dim rabbitmq multiplier = 1
	# dim rabbitmq divisor = 1
	# dim cinder name = cinder
	# dim cinder algorithm = absolute
	# dim cinder multiplier = 1
	# dim cinder divisor = 1
	# dim neutron name = neutron
	# dim neutron algorithm = absolute
	# dim neutron multiplier = 1
	# dim neutron divisor = 1
	# dim keystone name = keystone
	# dim keystone algorithm = absolute
	# dim keystone multiplier = 1
	# dim keystone divisor = 1
	# dim dip name = dip
	# dim dip algorithm = absolute
	# dim dip multiplier = 1
	# dim dip divisor = 1
	# dim qemu name = qemu
	# dim qemu algorithm = absolute
	# dim qemu multiplier = 1
	# dim qemu divisor = 1
	# dim screen name = screen
	# dim screen algorithm = absolute
	# dim screen multiplier = 1
	# dim screen divisor = 1
	# dim apache name = apache
	# dim apache algorithm = absolute
	# dim apache multiplier = 1
	# dim apache divisor = 1
	# dim nova name = nova
	# dim nova algorithm = absolute
	# dim nova multiplier = 1
	# dim nova divisor = 1
	# dim postfix name = postfix
	# dim postfix algorithm = absolute
	# dim postfix multiplier = 1
	# dim postfix divisor = 1
	# dim root name = root
	# dim root algorithm = absolute
	# dim root multiplier = 1
	# dim root divisor = 1
	# dim pcp name = pcp
	# dim pcp algorithm = absolute
	# dim pcp multiplier = 1
	# dim pcp divisor = 1

[system.swapio]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/system.swapio
	# chart type = area
	# type = system
	# family = swap
	# units = KiB/s
	# context = system.swapio
	# priority = 250
	# name = system.swapio
	# title = Swap I/O
	# dim in name = in
	# dim in algorithm = incremental
	# dim in multiplier = 4096
	# dim in divisor = 1024
	# dim out name = out
	# dim out algorithm = incremental
	# dim out multiplier = -4096
	# dim out divisor = 1024

[net.bond1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.bond1
	# chart type = area
	# type = net
	# family = bond1
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.bond1
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.bond1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.bond1
	# chart type = line
	# type = net_packets
	# family = bond1
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.bond1
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.bond1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.bond1
	# chart type = line
	# type = net_drops
	# family = bond1
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.bond1
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.ens6f3]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.ens6f3
	# chart type = area
	# type = net
	# family = ens6f3
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.ens6f3
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.ens6f3]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.ens6f3
	# chart type = line
	# type = net_packets
	# family = ens6f3
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.ens6f3
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.ens6f3]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.ens6f3
	# chart type = line
	# type = net_drops
	# family = ens6f3
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.ens6f3
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net_drops.bond0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.bond0
	# chart type = line
	# type = net_drops
	# family = bond0
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.bond0
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net_drops.eno49]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.eno49
	# chart type = line
	# type = net_drops
	# family = eno49
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.eno49
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.virbr0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.virbr0
	# chart type = area
	# type = net
	# family = virbr0
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.virbr0
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.virbr0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.virbr0
	# chart type = line
	# type = net_packets
	# family = virbr0
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.virbr0
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.br-bond1]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.br_bond1
	# chart type = line
	# type = net_drops
	# family = br-bond1
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.br_bond1
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.bond2
	# chart type = area
	# type = net
	# family = bond2
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.bond2
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.bond2
	# chart type = line
	# type = net_packets
	# family = bond2
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.bond2
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.bond2
	# chart type = line
	# type = net_drops
	# family = bond2
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.bond2
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.br-bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.br_bond2
	# chart type = area
	# type = net
	# family = br-bond2
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.br_bond2
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.br-bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.br_bond2
	# chart type = line
	# type = net_packets
	# family = br-bond2
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.br_bond2
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.br-bond2]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.br_bond2
	# chart type = line
	# type = net_drops
	# family = br-bond2
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.br_bond2
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.eno50]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.eno50
	# chart type = area
	# type = net
	# family = eno50
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.eno50
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.eno50]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.eno50
	# chart type = line
	# type = net_packets
	# family = eno50
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.eno50
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_drops.eno50]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.eno50
	# chart type = line
	# type = net_drops
	# family = eno50
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.eno50
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net.macvtap0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net.macvtap0
	# chart type = area
	# type = net
	# family = macvtap0
	# units = kilobits/s
	# context = net.net
	# priority = 7000
	# name = net.macvtap0
	# title = Bandwidth
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 8
	# dim received divisor = 1000
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -8
	# dim sent divisor = 1000

[net_packets.macvtap0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_packets.macvtap0
	# chart type = line
	# type = net_packets
	# family = macvtap0
	# units = packets/s
	# context = net.packets
	# priority = 7001
	# name = net_packets.macvtap0
	# title = Packets
	# dim received name = received
	# dim received algorithm = incremental
	# dim received multiplier = 1
	# dim received divisor = 1
	# dim sent name = sent
	# dim sent algorithm = incremental
	# dim sent multiplier = -1
	# dim sent divisor = 1
	# dim multicast name = multicast
	# dim multicast algorithm = incremental
	# dim multicast multiplier = 1
	# dim multicast divisor = 1

[net_errors.macvtap0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_errors.macvtap0
	# chart type = line
	# type = net_errors
	# family = macvtap0
	# units = errors/s
	# context = net.errors
	# priority = 7002
	# name = net_errors.macvtap0
	# title = Interface Errors
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[net_drops.macvtap0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/net_drops.macvtap0
	# chart type = line
	# type = net_drops
	# family = macvtap0
	# units = drops/s
	# context = net.drops
	# priority = 7003
	# name = net_drops.macvtap0
	# title = Interface Drops
	# dim inbound name = inbound
	# dim inbound algorithm = incremental
	# dim inbound multiplier = 1
	# dim inbound divisor = 1
	# dim outbound name = outbound
	# dim outbound algorithm = incremental
	# dim outbound multiplier = -1
	# dim outbound divisor = 1

[ip.tcpreorders]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ip.tcpreorders
	# chart type = line
	# type = ip
	# family = tcp
	# units = packets/s
	# context = ip.tcpreorders
	# priority = 4220
	# name = ip.tcpreorders
	# title = TCP Reordered Packets by Detection Method
	# dim TCPTSReorder name = timestamp
	# dim TCPTSReorder algorithm = incremental
	# dim TCPTSReorder multiplier = 1
	# dim TCPTSReorder divisor = 1
	# dim TCPSACKReorder name = sack
	# dim TCPSACKReorder algorithm = incremental
	# dim TCPSACKReorder multiplier = 1
	# dim TCPSACKReorder divisor = 1
	# dim TCPFACKReorder name = fack
	# dim TCPFACKReorder algorithm = incremental
	# dim TCPFACKReorder multiplier = 1
	# dim TCPFACKReorder divisor = 1
	# dim TCPRenoReorder name = reno
	# dim TCPRenoReorder algorithm = incremental
	# dim TCPRenoReorder multiplier = 1
	# dim TCPRenoReorder divisor = 1

[ipv6.icmpechos]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/ipv6.icmpechos
	# chart type = line
	# type = ipv6
	# family = icmp6
	# units = messages/s
	# context = ipv6.icmpechos
	# priority = 6930
	# name = ipv6.icmpechos
	# title = IPv6 ICMP Echo
	# dim InEchos name = InEchos
	# dim InEchos algorithm = incremental
	# dim InEchos multiplier = 1
	# dim InEchos divisor = 1
	# dim OutEchos name = OutEchos
	# dim OutEchos algorithm = incremental
	# dim OutEchos multiplier = -1
	# dim OutEchos divisor = 1
	# dim InEchoReplies name = InEchoReplies
	# dim InEchoReplies algorithm = incremental
	# dim InEchoReplies multiplier = 1
	# dim InEchoReplies divisor = 1
	# dim OutEchoReplies name = OutEchoReplies
	# dim OutEchoReplies algorithm = incremental
	# dim OutEchoReplies multiplier = -1
	# dim OutEchoReplies divisor = 1

[nfsd.io]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/nfsd.io
	# chart type = area
	# type = nfsd
	# family = io
	# units = kilobytes/s
	# context = nfsd.io
	# priority = 2102
	# name = nfsd.io
	# title = NFS Server I/O
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1000
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1000

[netdata.dbengine_compression_ratio]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_compression_ratio
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = percentage
	# context = netdata.dbengine_compression_ratio
	# priority = 130502
	# name = netdata.dbengine_compression_ratio
	# title = NetData DB engine data extents' compression savings ratio
	# dim savings name = savings
	# dim savings algorithm = absolute
	# dim savings multiplier = 1
	# dim savings divisor = 1000

[netdata.page_cache_hit_ratio]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.page_cache_hit_ratio
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = percentage
	# context = netdata.page_cache_hit_ratio
	# priority = 130503
	# name = netdata.page_cache_hit_ratio
	# title = NetData DB engine page cache hit ratio
	# dim ratio name = ratio
	# dim ratio algorithm = absolute
	# dim ratio multiplier = 1
	# dim ratio divisor = 1000

[netdata.page_cache_stats]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.page_cache_stats
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = pages
	# context = netdata.page_cache_stats
	# priority = 130504
	# name = netdata.page_cache_stats
	# title = NetData dbengine page cache statistics
	# dim descriptors name = descriptors
	# dim descriptors algorithm = absolute
	# dim descriptors multiplier = 1
	# dim descriptors divisor = 1
	# dim populated name = populated
	# dim populated algorithm = absolute
	# dim populated multiplier = 1
	# dim populated divisor = 1
	# dim dirty name = dirty
	# dim dirty algorithm = absolute
	# dim dirty multiplier = 1
	# dim dirty divisor = 1
	# dim backfills name = backfills
	# dim backfills algorithm = incremental
	# dim backfills multiplier = 1
	# dim backfills divisor = 1
	# dim evictions name = evictions
	# dim evictions algorithm = incremental
	# dim evictions multiplier = -1
	# dim evictions divisor = 1
	# dim used_by_collectors name = used_by_collectors
	# dim used_by_collectors algorithm = absolute
	# dim used_by_collectors multiplier = 1
	# dim used_by_collectors divisor = 1

[netdata.dbengine_long_term_page_stats]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_long_term_page_stats
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = pages
	# context = netdata.dbengine_long_term_page_stats
	# priority = 130505
	# name = netdata.dbengine_long_term_page_stats
	# title = NetData dbengine long-term page statistics
	# dim total name = total
	# dim total algorithm = absolute
	# dim total multiplier = 1
	# dim total divisor = 1
	# dim insertions name = insertions
	# dim insertions algorithm = incremental
	# dim insertions multiplier = 1
	# dim insertions divisor = 1
	# dim deletions name = deletions
	# dim deletions algorithm = incremental
	# dim deletions multiplier = -1
	# dim deletions divisor = 1
	# dim flushing_pressure_deletions name = flushing_pressure_deletions
	# dim flushing_pressure_deletions algorithm = incremental
	# dim flushing_pressure_deletions multiplier = -1
	# dim flushing_pressure_deletions divisor = 1

[netdata.dbengine_io_throughput]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_io_throughput
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = MiB/s
	# context = netdata.dbengine_io_throughput
	# priority = 130506
	# name = netdata.dbengine_io_throughput
	# title = NetData DB engine I/O throughput
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1048576
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1048576

[netdata.dbengine_io_operations]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_io_operations
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = operations/s
	# context = netdata.dbengine_io_operations
	# priority = 130507
	# name = netdata.dbengine_io_operations
	# title = NetData DB engine I/O operations
	# dim reads name = reads
	# dim reads algorithm = incremental
	# dim reads multiplier = 1
	# dim reads divisor = 1
	# dim writes name = writes
	# dim writes algorithm = incremental
	# dim writes multiplier = -1
	# dim writes divisor = 1

[netdata.dbengine_global_errors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_global_errors
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = errors/s
	# context = netdata.dbengine_global_errors
	# priority = 130508
	# name = netdata.dbengine_global_errors
	# title = NetData DB engine errors
	# dim io_errors name = io_errors
	# dim io_errors algorithm = incremental
	# dim io_errors multiplier = 1
	# dim io_errors divisor = 1
	# dim fs_errors name = fs_errors
	# dim fs_errors algorithm = incremental
	# dim fs_errors multiplier = 1
	# dim fs_errors divisor = 1
	# dim pg_cache_over_half_dirty_events name = pg_cache_over_half_dirty_events
	# dim pg_cache_over_half_dirty_events algorithm = incremental
	# dim pg_cache_over_half_dirty_events multiplier = 1
	# dim pg_cache_over_half_dirty_events divisor = 1

[netdata.dbengine_global_file_descriptors]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_global_file_descriptors
	# chart type = line
	# type = netdata
	# family = dbengine
	# units = descriptors
	# context = netdata.dbengine_global_file_descriptors
	# priority = 130509
	# name = netdata.dbengine_global_file_descriptors
	# title = NetData DB engine File Descriptors
	# dim current name = current
	# dim current algorithm = absolute
	# dim current multiplier = 1
	# dim current divisor = 1
	# dim max name = max
	# dim max algorithm = absolute
	# dim max multiplier = 1
	# dim max divisor = 1

[netdata.dbengine_ram]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.dbengine_ram
	# chart type = stacked
	# type = netdata
	# family = dbengine
	# units = MiB
	# context = netdata.dbengine_ram
	# priority = 130510
	# name = netdata.dbengine_ram
	# title = NetData DB engine RAM usage
	# dim cache name = cache
	# dim cache algorithm = absolute
	# dim cache multiplier = 1
	# dim cache divisor = 256
	# dim collectors name = collectors
	# dim collectors algorithm = absolute
	# dim collectors multiplier = 1
	# dim collectors divisor = 256
	# dim metadata name = metadata
	# dim metadata algorithm = absolute
	# dim metadata multiplier = 1
	# dim metadata divisor = 1048576

[sensors.tg3-pci-0301_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.tg3_pci_0301_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60005
	# name = sensors.tg3_pci_0301_temperature
	# title = tg3-pci-0301 temperature
	# dim tg3-pci-0301_temp1 name = temp1
	# dim tg3-pci-0301_temp1 algorithm = absolute
	# dim tg3-pci-0301_temp1 multiplier = 1
	# dim tg3-pci-0301_temp1 divisor = 1000

[sensors.tg3-pci-4103_temperature]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/sensors.tg3_pci_4103_temperature
	# chart type = line
	# type = sensors
	# family = temperature
	# units = Celsius
	# context = sensors.temperature
	# priority = 60006
	# name = sensors.tg3_pci_4103_temperature
	# title = tg3-pci-4103 temperature
	# dim tg3-pci-4103_temp1 name = temp1
	# dim tg3-pci-4103_temp1 algorithm = absolute
	# dim tg3-pci-4103_temp1 multiplier = 1
	# dim tg3-pci-4103_temp1 divisor = 1000

[postfix_local.qsize]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/postfix_local.qsize
	# chart type = area
	# type = postfix_local
	# family = queue
	# units = KiB
	# context = postfix.qsize
	# priority = 60001
	# name = postfix_local.qsize
	# title = Postfix Queue Emails Size
	# dim size name = size
	# dim size algorithm = absolute
	# dim size multiplier = 1
	# dim size divisor = 1

[postfix_local.qemails]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/postfix_local.qemails
	# chart type = line
	# type = postfix_local
	# family = queue
	# units = emails
	# context = postfix.qemails
	# priority = 60000
	# name = postfix_local.qemails
	# title = Postfix Queue Emails
	# dim emails name = emails
	# dim emails algorithm = absolute
	# dim emails multiplier = 1
	# dim emails divisor = 1

[netdata.queries]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.queries
	# chart type = line
	# type = netdata
	# family = queries
	# units = queries/s
	# context = netdata.queries
	# priority = 130500
	# name = netdata.queries
	# title = NetData API Queries
	# dim queries name = queries
	# dim queries algorithm = incremental
	# dim queries multiplier = 1
	# dim queries divisor = 1

[netdata.db_points]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/netdata.db_points
	# chart type = area
	# type = netdata
	# family = queries
	# units = points/s
	# context = netdata.db_points
	# priority = 130501
	# name = netdata.db_points
	# title = NetData API Points
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim generated name = generated
	# dim generated algorithm = incremental
	# dim generated multiplier = -1
	# dim generated divisor = 1

[cgroup_qemu_perflabems01.cpu]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.cpu
	# chart type = stacked
	# type = cgroup_qemu_perflabems01
	# family = cpu
	# units = percentage
	# context = cgroup.cpu
	# priority = 40000
	# name = cgroup_qemu_perflabems01.cpu
	# title = CPU Usage (9600% = 96 cores) for cgroup qemu_perflabems01
	# dim user name = user
	# dim user algorithm = incremental
	# dim user multiplier = 100
	# dim user divisor = 100
	# dim system name = system
	# dim system algorithm = incremental
	# dim system multiplier = 100
	# dim system divisor = 100

[cgroup_qemu_perflabems01.cpu_per_core]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.cpu_per_core
	# chart type = stacked
	# type = cgroup_qemu_perflabems01
	# family = cpu
	# units = percentage
	# context = cgroup.cpu_per_core
	# priority = 40100
	# name = cgroup_qemu_perflabems01.cpu_per_core
	# title = CPU Usage (9600% = 96 cores) Per Core for cgroup qemu_perflabems01
	# dim cpu0 name = cpu0
	# dim cpu0 algorithm = incremental
	# dim cpu0 multiplier = 100
	# dim cpu0 divisor = 1000000000
	# dim cpu1 name = cpu1
	# dim cpu1 algorithm = incremental
	# dim cpu1 multiplier = 100
	# dim cpu1 divisor = 1000000000
	# dim cpu2 name = cpu2
	# dim cpu2 algorithm = incremental
	# dim cpu2 multiplier = 100
	# dim cpu2 divisor = 1000000000
	# dim cpu3 name = cpu3
	# dim cpu3 algorithm = incremental
	# dim cpu3 multiplier = 100
	# dim cpu3 divisor = 1000000000
	# dim cpu4 name = cpu4
	# dim cpu4 algorithm = incremental
	# dim cpu4 multiplier = 100
	# dim cpu4 divisor = 1000000000
	# dim cpu5 name = cpu5
	# dim cpu5 algorithm = incremental
	# dim cpu5 multiplier = 100
	# dim cpu5 divisor = 1000000000
	# dim cpu6 name = cpu6
	# dim cpu6 algorithm = incremental
	# dim cpu6 multiplier = 100
	# dim cpu6 divisor = 1000000000
	# dim cpu7 name = cpu7
	# dim cpu7 algorithm = incremental
	# dim cpu7 multiplier = 100
	# dim cpu7 divisor = 1000000000
	# dim cpu8 name = cpu8
	# dim cpu8 algorithm = incremental
	# dim cpu8 multiplier = 100
	# dim cpu8 divisor = 1000000000
	# dim cpu9 name = cpu9
	# dim cpu9 algorithm = incremental
	# dim cpu9 multiplier = 100
	# dim cpu9 divisor = 1000000000
	# dim cpu10 name = cpu10
	# dim cpu10 algorithm = incremental
	# dim cpu10 multiplier = 100
	# dim cpu10 divisor = 1000000000
	# dim cpu11 name = cpu11
	# dim cpu11 algorithm = incremental
	# dim cpu11 multiplier = 100
	# dim cpu11 divisor = 1000000000
	# dim cpu12 name = cpu12
	# dim cpu12 algorithm = incremental
	# dim cpu12 multiplier = 100
	# dim cpu12 divisor = 1000000000
	# dim cpu13 name = cpu13
	# dim cpu13 algorithm = incremental
	# dim cpu13 multiplier = 100
	# dim cpu13 divisor = 1000000000
	# dim cpu14 name = cpu14
	# dim cpu14 algorithm = incremental
	# dim cpu14 multiplier = 100
	# dim cpu14 divisor = 1000000000
	# dim cpu15 name = cpu15
	# dim cpu15 algorithm = incremental
	# dim cpu15 multiplier = 100
	# dim cpu15 divisor = 1000000000
	# dim cpu16 name = cpu16
	# dim cpu16 algorithm = incremental
	# dim cpu16 multiplier = 100
	# dim cpu16 divisor = 1000000000
	# dim cpu17 name = cpu17
	# dim cpu17 algorithm = incremental
	# dim cpu17 multiplier = 100
	# dim cpu17 divisor = 1000000000
	# dim cpu18 name = cpu18
	# dim cpu18 algorithm = incremental
	# dim cpu18 multiplier = 100
	# dim cpu18 divisor = 1000000000
	# dim cpu19 name = cpu19
	# dim cpu19 algorithm = incremental
	# dim cpu19 multiplier = 100
	# dim cpu19 divisor = 1000000000
	# dim cpu20 name = cpu20
	# dim cpu20 algorithm = incremental
	# dim cpu20 multiplier = 100
	# dim cpu20 divisor = 1000000000
	# dim cpu21 name = cpu21
	# dim cpu21 algorithm = incremental
	# dim cpu21 multiplier = 100
	# dim cpu21 divisor = 1000000000
	# dim cpu22 name = cpu22
	# dim cpu22 algorithm = incremental
	# dim cpu22 multiplier = 100
	# dim cpu22 divisor = 1000000000
	# dim cpu23 name = cpu23
	# dim cpu23 algorithm = incremental
	# dim cpu23 multiplier = 100
	# dim cpu23 divisor = 1000000000
	# dim cpu24 name = cpu24
	# dim cpu24 algorithm = incremental
	# dim cpu24 multiplier = 100
	# dim cpu24 divisor = 1000000000
	# dim cpu25 name = cpu25
	# dim cpu25 algorithm = incremental
	# dim cpu25 multiplier = 100
	# dim cpu25 divisor = 1000000000
	# dim cpu26 name = cpu26
	# dim cpu26 algorithm = incremental
	# dim cpu26 multiplier = 100
	# dim cpu26 divisor = 1000000000
	# dim cpu27 name = cpu27
	# dim cpu27 algorithm = incremental
	# dim cpu27 multiplier = 100
	# dim cpu27 divisor = 1000000000
	# dim cpu28 name = cpu28
	# dim cpu28 algorithm = incremental
	# dim cpu28 multiplier = 100
	# dim cpu28 divisor = 1000000000
	# dim cpu29 name = cpu29
	# dim cpu29 algorithm = incremental
	# dim cpu29 multiplier = 100
	# dim cpu29 divisor = 1000000000
	# dim cpu30 name = cpu30
	# dim cpu30 algorithm = incremental
	# dim cpu30 multiplier = 100
	# dim cpu30 divisor = 1000000000
	# dim cpu31 name = cpu31
	# dim cpu31 algorithm = incremental
	# dim cpu31 multiplier = 100
	# dim cpu31 divisor = 1000000000
	# dim cpu32 name = cpu32
	# dim cpu32 algorithm = incremental
	# dim cpu32 multiplier = 100
	# dim cpu32 divisor = 1000000000
	# dim cpu33 name = cpu33
	# dim cpu33 algorithm = incremental
	# dim cpu33 multiplier = 100
	# dim cpu33 divisor = 1000000000
	# dim cpu34 name = cpu34
	# dim cpu34 algorithm = incremental
	# dim cpu34 multiplier = 100
	# dim cpu34 divisor = 1000000000
	# dim cpu35 name = cpu35
	# dim cpu35 algorithm = incremental
	# dim cpu35 multiplier = 100
	# dim cpu35 divisor = 1000000000
	# dim cpu36 name = cpu36
	# dim cpu36 algorithm = incremental
	# dim cpu36 multiplier = 100
	# dim cpu36 divisor = 1000000000
	# dim cpu37 name = cpu37
	# dim cpu37 algorithm = incremental
	# dim cpu37 multiplier = 100
	# dim cpu37 divisor = 1000000000
	# dim cpu38 name = cpu38
	# dim cpu38 algorithm = incremental
	# dim cpu38 multiplier = 100
	# dim cpu38 divisor = 1000000000
	# dim cpu39 name = cpu39
	# dim cpu39 algorithm = incremental
	# dim cpu39 multiplier = 100
	# dim cpu39 divisor = 1000000000
	# dim cpu40 name = cpu40
	# dim cpu40 algorithm = incremental
	# dim cpu40 multiplier = 100
	# dim cpu40 divisor = 1000000000
	# dim cpu41 name = cpu41
	# dim cpu41 algorithm = incremental
	# dim cpu41 multiplier = 100
	# dim cpu41 divisor = 1000000000
	# dim cpu42 name = cpu42
	# dim cpu42 algorithm = incremental
	# dim cpu42 multiplier = 100
	# dim cpu42 divisor = 1000000000
	# dim cpu43 name = cpu43
	# dim cpu43 algorithm = incremental
	# dim cpu43 multiplier = 100
	# dim cpu43 divisor = 1000000000
	# dim cpu44 name = cpu44
	# dim cpu44 algorithm = incremental
	# dim cpu44 multiplier = 100
	# dim cpu44 divisor = 1000000000
	# dim cpu45 name = cpu45
	# dim cpu45 algorithm = incremental
	# dim cpu45 multiplier = 100
	# dim cpu45 divisor = 1000000000
	# dim cpu46 name = cpu46
	# dim cpu46 algorithm = incremental
	# dim cpu46 multiplier = 100
	# dim cpu46 divisor = 1000000000
	# dim cpu47 name = cpu47
	# dim cpu47 algorithm = incremental
	# dim cpu47 multiplier = 100
	# dim cpu47 divisor = 1000000000
	# dim cpu48 name = cpu48
	# dim cpu48 algorithm = incremental
	# dim cpu48 multiplier = 100
	# dim cpu48 divisor = 1000000000
	# dim cpu49 name = cpu49
	# dim cpu49 algorithm = incremental
	# dim cpu49 multiplier = 100
	# dim cpu49 divisor = 1000000000
	# dim cpu50 name = cpu50
	# dim cpu50 algorithm = incremental
	# dim cpu50 multiplier = 100
	# dim cpu50 divisor = 1000000000
	# dim cpu51 name = cpu51
	# dim cpu51 algorithm = incremental
	# dim cpu51 multiplier = 100
	# dim cpu51 divisor = 1000000000
	# dim cpu52 name = cpu52
	# dim cpu52 algorithm = incremental
	# dim cpu52 multiplier = 100
	# dim cpu52 divisor = 1000000000
	# dim cpu53 name = cpu53
	# dim cpu53 algorithm = incremental
	# dim cpu53 multiplier = 100
	# dim cpu53 divisor = 1000000000
	# dim cpu54 name = cpu54
	# dim cpu54 algorithm = incremental
	# dim cpu54 multiplier = 100
	# dim cpu54 divisor = 1000000000
	# dim cpu55 name = cpu55
	# dim cpu55 algorithm = incremental
	# dim cpu55 multiplier = 100
	# dim cpu55 divisor = 1000000000
	# dim cpu56 name = cpu56
	# dim cpu56 algorithm = incremental
	# dim cpu56 multiplier = 100
	# dim cpu56 divisor = 1000000000
	# dim cpu57 name = cpu57
	# dim cpu57 algorithm = incremental
	# dim cpu57 multiplier = 100
	# dim cpu57 divisor = 1000000000
	# dim cpu58 name = cpu58
	# dim cpu58 algorithm = incremental
	# dim cpu58 multiplier = 100
	# dim cpu58 divisor = 1000000000
	# dim cpu59 name = cpu59
	# dim cpu59 algorithm = incremental
	# dim cpu59 multiplier = 100
	# dim cpu59 divisor = 1000000000
	# dim cpu60 name = cpu60
	# dim cpu60 algorithm = incremental
	# dim cpu60 multiplier = 100
	# dim cpu60 divisor = 1000000000
	# dim cpu61 name = cpu61
	# dim cpu61 algorithm = incremental
	# dim cpu61 multiplier = 100
	# dim cpu61 divisor = 1000000000
	# dim cpu62 name = cpu62
	# dim cpu62 algorithm = incremental
	# dim cpu62 multiplier = 100
	# dim cpu62 divisor = 1000000000
	# dim cpu63 name = cpu63
	# dim cpu63 algorithm = incremental
	# dim cpu63 multiplier = 100
	# dim cpu63 divisor = 1000000000
	# dim cpu64 name = cpu64
	# dim cpu64 algorithm = incremental
	# dim cpu64 multiplier = 100
	# dim cpu64 divisor = 1000000000
	# dim cpu65 name = cpu65
	# dim cpu65 algorithm = incremental
	# dim cpu65 multiplier = 100
	# dim cpu65 divisor = 1000000000
	# dim cpu66 name = cpu66
	# dim cpu66 algorithm = incremental
	# dim cpu66 multiplier = 100
	# dim cpu66 divisor = 1000000000
	# dim cpu67 name = cpu67
	# dim cpu67 algorithm = incremental
	# dim cpu67 multiplier = 100
	# dim cpu67 divisor = 1000000000
	# dim cpu68 name = cpu68
	# dim cpu68 algorithm = incremental
	# dim cpu68 multiplier = 100
	# dim cpu68 divisor = 1000000000
	# dim cpu69 name = cpu69
	# dim cpu69 algorithm = incremental
	# dim cpu69 multiplier = 100
	# dim cpu69 divisor = 1000000000
	# dim cpu70 name = cpu70
	# dim cpu70 algorithm = incremental
	# dim cpu70 multiplier = 100
	# dim cpu70 divisor = 1000000000
	# dim cpu71 name = cpu71
	# dim cpu71 algorithm = incremental
	# dim cpu71 multiplier = 100
	# dim cpu71 divisor = 1000000000
	# dim cpu72 name = cpu72
	# dim cpu72 algorithm = incremental
	# dim cpu72 multiplier = 100
	# dim cpu72 divisor = 1000000000
	# dim cpu73 name = cpu73
	# dim cpu73 algorithm = incremental
	# dim cpu73 multiplier = 100
	# dim cpu73 divisor = 1000000000
	# dim cpu74 name = cpu74
	# dim cpu74 algorithm = incremental
	# dim cpu74 multiplier = 100
	# dim cpu74 divisor = 1000000000
	# dim cpu75 name = cpu75
	# dim cpu75 algorithm = incremental
	# dim cpu75 multiplier = 100
	# dim cpu75 divisor = 1000000000
	# dim cpu76 name = cpu76
	# dim cpu76 algorithm = incremental
	# dim cpu76 multiplier = 100
	# dim cpu76 divisor = 1000000000
	# dim cpu77 name = cpu77
	# dim cpu77 algorithm = incremental
	# dim cpu77 multiplier = 100
	# dim cpu77 divisor = 1000000000
	# dim cpu78 name = cpu78
	# dim cpu78 algorithm = incremental
	# dim cpu78 multiplier = 100
	# dim cpu78 divisor = 1000000000
	# dim cpu79 name = cpu79
	# dim cpu79 algorithm = incremental
	# dim cpu79 multiplier = 100
	# dim cpu79 divisor = 1000000000
	# dim cpu80 name = cpu80
	# dim cpu80 algorithm = incremental
	# dim cpu80 multiplier = 100
	# dim cpu80 divisor = 1000000000
	# dim cpu81 name = cpu81
	# dim cpu81 algorithm = incremental
	# dim cpu81 multiplier = 100
	# dim cpu81 divisor = 1000000000
	# dim cpu82 name = cpu82
	# dim cpu82 algorithm = incremental
	# dim cpu82 multiplier = 100
	# dim cpu82 divisor = 1000000000
	# dim cpu83 name = cpu83
	# dim cpu83 algorithm = incremental
	# dim cpu83 multiplier = 100
	# dim cpu83 divisor = 1000000000
	# dim cpu84 name = cpu84
	# dim cpu84 algorithm = incremental
	# dim cpu84 multiplier = 100
	# dim cpu84 divisor = 1000000000
	# dim cpu85 name = cpu85
	# dim cpu85 algorithm = incremental
	# dim cpu85 multiplier = 100
	# dim cpu85 divisor = 1000000000
	# dim cpu86 name = cpu86
	# dim cpu86 algorithm = incremental
	# dim cpu86 multiplier = 100
	# dim cpu86 divisor = 1000000000
	# dim cpu87 name = cpu87
	# dim cpu87 algorithm = incremental
	# dim cpu87 multiplier = 100
	# dim cpu87 divisor = 1000000000
	# dim cpu88 name = cpu88
	# dim cpu88 algorithm = incremental
	# dim cpu88 multiplier = 100
	# dim cpu88 divisor = 1000000000
	# dim cpu89 name = cpu89
	# dim cpu89 algorithm = incremental
	# dim cpu89 multiplier = 100
	# dim cpu89 divisor = 1000000000
	# dim cpu90 name = cpu90
	# dim cpu90 algorithm = incremental
	# dim cpu90 multiplier = 100
	# dim cpu90 divisor = 1000000000
	# dim cpu91 name = cpu91
	# dim cpu91 algorithm = incremental
	# dim cpu91 multiplier = 100
	# dim cpu91 divisor = 1000000000
	# dim cpu92 name = cpu92
	# dim cpu92 algorithm = incremental
	# dim cpu92 multiplier = 100
	# dim cpu92 divisor = 1000000000
	# dim cpu93 name = cpu93
	# dim cpu93 algorithm = incremental
	# dim cpu93 multiplier = 100
	# dim cpu93 divisor = 1000000000
	# dim cpu94 name = cpu94
	# dim cpu94 algorithm = incremental
	# dim cpu94 multiplier = 100
	# dim cpu94 divisor = 1000000000
	# dim cpu95 name = cpu95
	# dim cpu95 algorithm = incremental
	# dim cpu95 multiplier = 100
	# dim cpu95 divisor = 1000000000

[cgroup_qemu_perflabems01.mem]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.mem
	# chart type = stacked
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB
	# context = cgroup.mem
	# priority = 40210
	# name = cgroup_qemu_perflabems01.mem
	# title = Memory Usage for cgroup qemu_perflabems01
	# dim cache name = cache
	# dim cache algorithm = absolute
	# dim cache multiplier = 1
	# dim cache divisor = 1048576
	# dim rss name = rss
	# dim rss algorithm = absolute
	# dim rss multiplier = 1
	# dim rss divisor = 1048576
	# dim swap name = swap
	# dim swap algorithm = absolute
	# dim swap multiplier = 1
	# dim swap divisor = 1048576
	# dim rss_huge name = rss_huge
	# dim rss_huge algorithm = absolute
	# dim rss_huge multiplier = 1
	# dim rss_huge divisor = 1048576
	# dim mapped_file name = mapped_file
	# dim mapped_file algorithm = absolute
	# dim mapped_file multiplier = 1
	# dim mapped_file divisor = 1048576

[cgroup_qemu_perflabems01.writeback]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.writeback
	# chart type = area
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB
	# context = cgroup.writeback
	# priority = 40300
	# name = cgroup_qemu_perflabems01.writeback
	# title = Writeback Memory for cgroup qemu_perflabems01
	# dim writeback name = writeback
	# dim writeback algorithm = absolute
	# dim writeback multiplier = 1
	# dim writeback divisor = 1048576

[cgroup_qemu_perflabems01.mem_activity]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.mem_activity
	# chart type = line
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB/s
	# context = cgroup.mem_activity
	# priority = 40400
	# name = cgroup_qemu_perflabems01.mem_activity
	# title = Memory Activity for cgroup qemu_perflabems01
	# dim pgpgin name = in
	# dim pgpgin algorithm = incremental
	# dim pgpgin multiplier = 4096
	# dim pgpgin divisor = 1048576
	# dim pgpgout name = out
	# dim pgpgout algorithm = incremental
	# dim pgpgout multiplier = -4096
	# dim pgpgout divisor = 1048576

[cgroup_qemu_perflabems01.pgfaults]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.pgfaults
	# chart type = line
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB/s
	# context = cgroup.pgfaults
	# priority = 40500
	# name = cgroup_qemu_perflabems01.pgfaults
	# title = Memory Page Faults for cgroup qemu_perflabems01
	# dim pgfault name = pgfault
	# dim pgfault algorithm = incremental
	# dim pgfault multiplier = 4096
	# dim pgfault divisor = 1048576
	# dim pgmajfault name = swap
	# dim pgmajfault algorithm = incremental
	# dim pgmajfault multiplier = -4096
	# dim pgmajfault divisor = 1048576

[cgroup_qemu_perflabems01.mem_usage]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.mem_usage
	# chart type = stacked
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB
	# context = cgroup.mem_usage
	# priority = 40200
	# name = cgroup_qemu_perflabems01.mem_usage
	# title = Used Memory without Cache for cgroup qemu_perflabems01
	# dim ram name = ram
	# dim ram algorithm = absolute
	# dim ram multiplier = 1
	# dim ram divisor = 1048576
	# dim swap name = swap
	# dim swap algorithm = absolute
	# dim swap multiplier = 1
	# dim swap divisor = 1048576

[cgroup_qemu_perflabems01.mem_usage_limit]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.mem_usage_limit
	# chart type = stacked
	# type = cgroup_qemu_perflabems01
	# family = mem
	# units = MiB
	# context = cgroup.mem_usage_limit
	# priority = 40199
	# name = cgroup_qemu_perflabems01.mem_usage_limit
	# title = Used RAM without Cache within the limits for cgroup qemu_perflabems01
	# dim available name = available
	# dim available algorithm = absolute
	# dim available multiplier = 1
	# dim available divisor = 1048576
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 1048576

[cgroup_qemu_perflabems01.throttle_io]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.throttle_io
	# chart type = area
	# type = cgroup_qemu_perflabems01
	# family = disk
	# units = KiB/s
	# context = cgroup.throttle_io
	# priority = 41200
	# name = cgroup_qemu_perflabems01.throttle_io
	# title = Throttle I/O Bandwidth (all disks) for cgroup qemu_perflabems01
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1024
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1024

[cgroup_qemu_perflabems01.throttle_serviced_ops]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.throttle_serviced_ops
	# chart type = line
	# type = cgroup_qemu_perflabems01
	# family = disk
	# units = operations/s
	# context = cgroup.throttle_serviced_ops
	# priority = 41200
	# name = cgroup_qemu_perflabems01.throttle_serviced_ops
	# title = Throttle Serviced I/O Operations (all disks) for cgroup qemu_perflabems01
	# dim read name = read
	# dim read algorithm = incremental
	# dim read multiplier = 1
	# dim read divisor = 1
	# dim write name = write
	# dim write algorithm = incremental
	# dim write multiplier = -1
	# dim write divisor = 1

[cgroup_qemu_perflabems01.cpu_limit]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/cgroup_qemu_perflabems01.cpu_limit
	# chart type = line
	# type = cgroup_qemu_perflabems01
	# family = cpu
	# units = percentage
	# context = cgroup.cpu_limit
	# priority = 39999
	# name = cgroup_qemu_perflabems01.cpu_limit
	# title = CPU Usage within the limits for cgroup qemu_perflabems01
	# dim used name = used
	# dim used algorithm = absolute
	# dim used multiplier = 1
	# dim used divisor = 100

[disk_qops.sda]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.sda
	# chart type = line
	# type = disk_qops
	# family = sda
	# units = operations
	# context = disk.qops
	# priority = 2002
	# name = disk_qops.sda
	# title = Disk Current I/O Operations
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1

[disk_qops.dm-0]
	# history = 5
	# enabled = yes
	# cache directory = /var/cache/netdata/disk_qops.dm_0
	# chart type = line
	# type = disk_qops
	# family = /var/lib/nova
	# units = operations
	# context = disk.qops
	# priority = 2002
	# name = disk_qops.vg00_var_lib_nova
	# title = Disk Current I/O Operations
	# dim operations name = operations
	# dim operations algorithm = absolute
	# dim operations multiplier = 1
	# dim operations divisor = 1
